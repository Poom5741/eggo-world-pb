---
phase: 41-framework-setup-docker-env
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [docker-compose.e2e.yml, .env.e2e.example]
autonomous: false
requirements: [ENV-01, ENV-02, ENV-03]
user_setup:
  - service: anvil
    why: "Blockchain node for E2E tests"
    env_vars:
      - name: BSC_TESTNET_RPC
        source: "Foundry profile or public RPC endpoint"
    dashboard_config: []

must_haves:
  truths:
    - "Developer can run `docker-compose -f docker-compose.e2e.yml up` and all services start with healthy status"
    - "Tests can connect to PocketBase, wallet-api, and Anvil from the test runner"
    - "Anvil is accessible with configured BSC-forked state"
  artifacts:
    - path: "docker-compose.e2e.yml"
      provides: "Isolated E2E test environment"
      min_lines: 80
      contains: "anvil"
    - path: ".env.e2e.example"
      provides: "E2E environment template"
      min_lines: 20
  key_links:
    - from: "docker-compose.e2e.yml"
      to: "PocketBase"
      via: "pocketbase service"
      pattern: "pocketbase.*8090"
    - from: "docker-compose.e2e.yml"
      to: "Anvil"
      via: "anvil service"
      pattern: "anvil.*8545"
---

<objective>
Create isolated Docker Compose test environment with all services (PocketBase, wallet-api, Anvil, frontend preview) and health checks.

Purpose: Enable `docker-compose -f docker-compose.e2e.yml up` to start all services in healthy state for E2E test execution.
Output: docker-compose.e2e.yml, .env.e2e.example
</objective>

<execution_context>
@/Users/poom-work/tokenine/eggo-pocketbase/.qwen/get-shit-done/workflows/execute-plan.md
@/Users/poom-work/tokenine/eggo-pocketbase/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docker-compose.yml
@contracts/foundry.toml
@tests/e2e/README.md
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From docker-compose.yml (reference):

```yaml
services:
  pocketbase:
    ports: ["8090:8090"]
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8090/api/health"]
    volumes: ["./apps/backend/pb_data:/pb/pb_data"]

  wallet-api:
    ports: ["3001:3001"]
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:3001/health || exit 1"]
```

From contracts/foundry.toml:

```toml
[rpc_endpoints]
bsc_testnet = "https://data-seed-prebsc-1-s1.binance.org:8545"
```

Pattern: Use wget/curl for health checks, port mapping follows existing convention.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create docker-compose.e2e.yml with isolated test services</name>
  <files>docker-compose.e2e.yml</files>
  <behavior>
    - Test 1: File exists with services: pocketbase, wallet-api, anvil, frontend
    - Test 2: All services have healthcheck configurations
    - Test 3: Anvil service has fork-url configuration for BSC testnet
  </behavior>
  <action>
    Per D-05: Create standalone docker-compose.e2e.yml (not extending existing docker-compose.yml).
    Per D-06: Isolated test environment with clean teardown.
    Per D-07: Services: PocketBase, wallet-api, Anvil, frontend preview.
    
    Service configurations:
    
    1. **PocketBase** (port 8090):
       - Isolated pb_data.e2e/ volume (per specifics)
       - Health check: wget http://localhost:8090/api/health (per D-08)
       - Environment: WALLET_SRV_URL=http://wallet-api:3001
       
    2. **wallet-api** (port 3001):
       - Health check: curl http://localhost:3001/health (per D-08)
       - Environment: PB_ADMIN_EMAIL/PASSWORD for test user injection
       
    3. **Anvil** (port 8545):
       - Per D-09: BSC Testnet fork from foundry.toml bsc_testnet RPC
       - Command: `anvil --fork-url https://data-seed-prebsc-1-s1.binance.org:8545`
       - Health check: curl JSON-RPC eth_blockNumber to localhost:8545
       - Per D-10/11: Real contract addresses, matches production behavior
       
    4. **frontend** (port 3000):
       - Static file server from apps/web/out/
       - Use nginx:alpine or serve static files
       - Health check: wget http://localhost:3000
    
    Networks:
    - Create isolated e2e-test-network (not shared with dev compose)
    
    Avoid: Extending existing docker-compose.yml (per D-05).
    Avoid: Using production pb_data volume (per D-06, use pb_data.e2e/).
    Avoid: VRF mocking (per D-12, deferred to Phase 42).
  </action>
  <verify>
    <automated>grep -q "pb_data.e2e" docker-compose.e2e.yml && grep -q "anvil.*fork-url" docker-compose.e2e.yml && grep -c "healthcheck" docker-compose.e2e.yml | grep -q "4"</automated>
  </verify>
  <done>
    - docker-compose.e2e.yml exists with 4 services
    - All services have health checks
    - Anvil configured with BSC fork
    - Isolated pb_data.e2e volume
    - Isolated e2e-test-network
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .env.e2e.example with required environment variables</name>
  <files>.env.e2e.example</files>
  <action>
    Create environment template file for E2E test configuration.
    
    Required variables:
    - POCKETBASE_URL=http://localhost:8090
    - WALLET_API_URL=http://localhost:3001
    - ANVIL_RPC_URL=http://localhost:8545
    - EGG_NFT_ADDRESS=0xb2FE193523A1E6A240141331A80755f5642e7A44 (per foundry deployment)
    - PB_ADMIN_EMAIL=test@e2e.local
    - PB_ADMIN_PASSWORD=test-password-change-me
    - WALLET_MASTER_KEY=development-key-change-in-production
    
    Include instructions:
    - Copy to .env.e2e and modify for local testing
    - Never commit .env.e2e with real credentials
    
    Avoid: Hardcoding production credentials.
    Avoid: Including secrets from .env files (use template only).
  </action>
  <verify>
    <automated>grep -q "POCKETBASE_URL" .env.e2e.example && grep -q "ANVIL_RPC_URL" .env.e2e.example</automated>
  </verify>
  <done>
    - .env.e2e.example exists with all required variables
    - Template ready for developer customization
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Docker Compose E2E environment with Anvil BSC fork, health checks, and isolated volumes</what-built>
  <how-to-verify>
    1. Run `docker-compose -f docker-compose.e2e.yml up -d`
    2. Wait 30 seconds for services to start
    3. Run `docker-compose -f docker-compose.e2e.yml ps` — verify all services show "healthy" status
    4. Test Anvil: `curl -X POST http://localhost:8545 -d '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}' | jq .result`
    5. Test PocketBase: `curl http://localhost:8090/api/health | jq .code` (should return 200)
    6. Test wallet-api: `curl http://localhost:3001/health | jq .status` (should return "ok")
  </how-to-verify>
  <resume-signal>Type "approved" if all services healthy, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                           |
| ----------------------------- | ------------------------------------- |
| Test runner → Docker services | Test code accessing isolated services |
| Docker services → BSC testnet | Anvil forking from external RPC       |
| Docker network → Host network | Isolated e2e-test-network             |

## STRIDE Threat Register

| Threat ID | Category            | Component          | Disposition | Mitigation Plan                                          |
| --------- | ------------------- | ------------------ | ----------- | -------------------------------------------------------- |
| T-41-04   | S (Spoofing)        | Anvil RPC          | accept      | Test environment, BSC testnet fork is public             |
| T-41-05   | T (Tampering)       | pb_data.e2e        | accept      | Isolated volume, no production data                      |
| T-41-06   | I (Info Disclosure) | wallet-api secrets | mitigate    | Use .env.e2e.example template, never commit real secrets |
| T-41-07   | D (DoS)             | Anvil fork         | accept      | Local test only, external RPC may be rate-limited        |

</threat_model>

<verification>
1. docker-compose.e2e.yml exists with 4 services
2. All services have healthcheck configurations
3. Anvil configured with BSC fork URL
4. .env.e2e.example exists with required variables
5. Services start and report healthy status
</verification>

<success_criteria>

- `docker-compose -f docker-compose.e2e.yml up` starts all services
- All services report healthy status within 60 seconds
- Anvil accessible at localhost:8545 with BSC fork state
- PocketBase accessible at localhost:8090
- wallet-api accessible at localhost:3001
  </success_criteria>

<output>
After completion, create `.planning/phases/41-framework-setup-docker-env/41-02-SUMMARY.md`
</output>
