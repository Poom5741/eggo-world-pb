---
phase: 50-high-severity-security-fixes
plan: "01"
type: execute
wave: 1
subsystem: smart-contracts
tags:
  - security
  - high-severity
  - self-referral
  - SEC-07
requirements_addressed:
  - SEC-07
dependency_graph:
  provides:
    - Self-referral prevention in all mint/breed flows
  requires: []
  blocking:
    - "Phase 50 all plans (milestone progression)"
tech_stack:
  added: []
  patterns:
    - "msg.sender vs referrer comparison"
    - "Referral chain self-check loop"
key_files:
  modified:
    - contracts/src/EggNFT.sol
    - contracts/src/FoodNFT.sol
  created:
    - contracts/test/SelfReferralGuard.t.sol
autonomous: true
---

# Plan 50-01: Self-Referral Guards (SEC-07)

**Objective:** Prevent users from earning commission by referring themselves in mintEgg, mintFood, and breedAnimals flows.

## Tasks

### Task 1: Implement self-referral guards across all mint/breed flows

TDD Workflow: RED → GREEN

#### STEP A: RED — Add failing self-referral tests

<read_first>

- contracts/src/EggNFT.sol (mintEgg line 119, \_mintEggWithChain line 130, breedAnimals line 394)
- contracts/src/FoodNFT.sol (mintFood line 62)
- contracts/src/CommissionDistribution.sol (distributeCommission line 58)
- contracts/test/EggNFT.t.sol (existing test patterns)
  </read_first>

<action>
Create `contracts/test/SelfReferralGuard.t.sol` with 3 failing tests:

1. `testMintEggSelfReferralReverts()` — Call mintEgg(msg.sender) with msg.sender as referrer; expect revert with "Self-referral"
2. `testMintFoodSelfReferralReverts()` — Call mintFood(1, msg.sender); expect revert with "Self-referral"
3. `testBreedAnimalsSelfReferralReverts()` — Call breedAnimals(p1, p2, msg.sender); expect revert with "Self-referral"

Test setup:

- Use same pattern as existing Phase 49 tests (SecurityFixes.t.sol, SecurityFixes0506.t.sol)
- Import MockERC20 or use existing test infrastructure
- Deploy contracts with test addresses
- For breedAnimals test: first mint two eggs, hatch them into animals, then attempt breeding with self-referral

Use exact revert messages: "Self-referral"
</action>

<acceptance_criteria>

- contracts/test/SelfReferralGuard.t.sol exists
- forge test --match-contract SelfReferralGuardTest exits with non-zero (tests fail, no fix yet)
- Tests compile successfully (only runtime failures expected)
  </acceptance_criteria>

#### STEP B: RED — Add failing referral chain tests

<read_first>

- contracts/test/SelfReferralGuard.t.sol (from STEP A)
- contracts/src/EggNFT.sol (\_mintEggWithChain line 130)
  </read_first>

<action>
Add 2 more failing tests to SelfReferralGuard.t.sol:

4. `testMintEggWithChainSelfReferralInChain()` — Call mintEggWithChain([user, user, addr3, addr4]) where first element equals msg.sender; expect revert
5. `testMintEggWithChainDeepSelfReferral()` — Call mintEggWithChain([addr1, addr2, msg.sender, addr4]); expect revert (mid-chain self-referral)
   </action>

<acceptance_criteria>

- 5 tests total in SelfReferralGuard.t.sol all compile
- First 3 fail at runtime (no contract fix yet)
- Chain-based tests fail at runtime (no chain check yet)
  </acceptance_criteria>

#### STEP C: GREEN — Fix EggNFT.sol — mintEgg and \_mintEggWithChain

<read_first>

- contracts/src/EggNFT.sol (lines 119-169)
- contracts/src/EggNFT.sol (breedAnimals lines 394-455)
- contracts/src/EggNFT.sol (line 22 where MINT_PRICE is defined)
  </read_first>

<action>
Add self-referral checks to EggNFT.sol:

In `mintEgg(address referrer)` (after line 119, before line 120):

```solidity
require(referrer != msg.sender, "Self-referral");
```

In `_mintEggWithChain(address buyer, address[4] memory referralChain)` (after line 130, add at line 131):

```solidity
for (uint256 i = 0; i < 4; i++) {
    require(referralChain[i] != msg.sender, "Self-referral");
}
```

In `breedAnimals` (after line 394, before line 400):

```solidity
require(referrer != msg.sender, "Self-referral");
```

</action>

<acceptance_criteria>

- contracts/src/EggNFT.sol contains `require(referrer != msg.sender, "Self-referral")` after mintEgg line 119
- contracts/src/EggNFT.sol contains for loop with `require(referralChain[i] != msg.sender, "Self-referral")` in \_mintEggWithChain
- contracts/src/EggNFT.sol contains `require(referrer != msg.sender, "Self-referral")` in breedAnimals
- forge build --silent succeeds
  </acceptance_criteria>

#### STEP D: GREEN — Fix FoodNFT.sol — mintFood

<read_first>

- contracts/src/FoodNFT.sol (mintFood lines 62-103)
  </read_first>

<action>
Add self-referral check to mintFood in FoodNFT.sol (after line 67, before line 69):
```solidity
require(referrer != msg.sender, "Self-referral");
```
</action>

<acceptance_criteria>

- contracts/src/FoodNFT.sol contains `require(referrer != msg.sender, "Self-referral")` in mintFood function
- forge build --silent succeeds
  </acceptance_criteria>

#### STEP E: Verify all tests pass

<read_first>

- contracts/test/SelfReferralGuard.t.sol
- contracts/src/EggNFT.sol (self-referral checks)
- contracts/src/FoodNFT.sol (self-referral check)
  </read_first>

<action>
Run test suite:
```bash
cd contracts && forge test --match-contract SelfReferralGuardTest -vvv
```
Expected: All 5 tests pass (self-referral reverts trigger correctly)
Then run full suite to verify no regressions:
```bash
cd contracts && forge test --silent
```
</action>

<acceptance_criteria>

- forge test --match-contract SelfReferralGuardTest: 5/5 passed
- Full forge test: >= 0 failures (only pre-existing allowed)
- Self-referral prevented in: mintEgg, mintEggWithChain, mintFood, breedAnimals
  </acceptance_criteria>
