---
phase: 50-high-severity-security-fixes
plan: "05"
type: execute
wave: 2
subsystem: smart-contracts
tags:
  - security
  - high-severity
  - VRF
  - duplicate-prevention
  - SEC-11
requirements_addressed:
  - SEC-11
dependency_graph:
  provides:
    - Duplicate VRF request prevention in hatchEgg
  requires: []
  blocking:
    - "Phase 50 all plans (milestone progression)"
tech_stack:
  added: []
  patterns:
    - "Duplicate request prevention guard"
key_files:
  modified:
    - contracts/src/EggNFT.sol
  created:
    - contracts/test/VRFSecurity.t.sol
autonomous: true
---

# Plan 50-05: Prevent Duplicate VRF Requests (SEC-11)

**Objective:** Add guard in `hatchEgg` to prevent calling it twice on the same egg (duplicate VRF requests that waste LINK tokens).

## Tasks

### Task 1: Add duplicate VRF request prevention

TDD Workflow: RED → GREEN

#### STEP A: RED — Add failing test

<read_first>

- contracts/src/EggNFT.sol (hatchEgg lines 206-245, tokenToRequestId mapping line 103)
- contracts/test/EggHatching.t.sol (existing VRF tests)
  </read_first>

<action>
Create `contracts/test/VRFSecurity.t.sol` with 1 failing test:

1. `testDuplicateHatchRequestReverts()` — Hatch egg (VRF request) → attempt hatchEgg again; expect revert "Hatch already requested"

Test setup:

- Mint egg, feed to MAX_FOOD_COUNT
- Call hatchEgg (first request succeeds)
- Call hatchEgg again on same tokenId
- Expect revert before second VRF request is made
  </action>

<acceptance_criteria>

- contracts/test/VRFSecurity.t.sol exists with 1 test
- forge test --match-contract VRFSecurityTest compiles and fails
  </acceptance_criteria>

#### STEP B: GREEN — Add guard to hatchEgg

<read_first>

- contracts/src/EggNFT.sol (hatchEgg lines 206-215)
  </read_first>

<action>
Add duplicate prevention check to hatchEgg in EggNFT.sol (after line 214 `require(s_subscriptionId != 0, "VRF subscription not set");`, add at line 215):

```solidity
require(tokenToRequestId[tokenId] == 0, "Hatch already requested");
```

</action>

<acceptance_criteria>

- contracts/src/EggNFT.sol hatchEgg contains `require(tokenToRequestId[tokenId] == 0, "Hatch already requested");`
- forge build --silent succeeds
- Cannot call hatchEgg twice on same tokenId
  </acceptance_criteria>

#### STEP C: Verify tests pass

<read_first>

- contracts/test/VRFSecurity.t.sol
- contracts/src/EggNFT.sol (from STEP B)
  </read_first>

<action>
```bash
cd contracts && forge test --match-contract VRFSecurityTest -vvv
cd contracts && forge test --silent
```
</action>

<acceptance_criteria>

- forge test --match-contract VRFSecurityTest: 1/1 passed
- Full forge test: >= 0 new failures
  </acceptance_criteria>
