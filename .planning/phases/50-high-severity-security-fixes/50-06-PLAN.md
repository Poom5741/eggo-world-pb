---
phase: 50-high-severity-security-fixes
plan: "06"
type: execute
wave: 2
subsystem: smart-contracts
tags:
  - security
  - high-severity
  - VRF
  - transfer-prevention
  - SEC-12
requirements_addressed:
  - SEC-12
dependency_graph:
  provides:
    - NFT transfer restriction during VRF pending (prevents double claiming)
  requires: []
  blocking:
    - "Phase 50 all plans (milestone progression)"
tech_stack:
  added: []
  patterns:
    - "Transfer blocking during pending operation"
key_files:
  modified:
    - contracts/src/EggNFT.sol
  created:
    - contracts/test/VRFSecurity.t.sol (append to VRFSecurity plan 50-05)
autonomous: true
---

# Plan 50-06: NFT Transfer During VRF Pending (SEC-12)

**Objective:** Prevent NFT transfers while VRF request is pending — protects LINK cost investment and prevents double-claiming exploits where transferred NFT could be claimed by new owner.

## Tasks

### Task 1: Block NFT transfers during VRF pending

TDD Workflow: RED → GREEN

#### STEP A: RED — Add failing test

<read_first>

- contracts/src/EggNFT.sol (\_update lines 520-532, tokenToRequestId mapping line 103)
- contracts/test/VRFSecurity.t.sol (from plan 50-05)
  </read_first>

<action>
Add test to `contracts/test/VRFSecurity.t.sol`:

1. `testTransferDuringVRFPendingReverts()` — Hatch egg (VRF pending) → attempt transferFrom; expect revert "Cannot transfer during VRF pending"

2. `testTransferAfterClaimSucceeds()` — Hatch egg → fulfill VRF → claimHatch → transfer; expect success

Test setup:

- Mint and feed egg to MAX_FOOD_COUNT
- Call hatchEgg (VRF pending)
- Attempt approve + transferFrom
- Expect revert
  </action>

<acceptance_criteria>

- contracts/test/VRFSecurity.t.sol has 2 tests (1 from plan 50-05 + 1 new + 1 positive)
- forge test --match-contract VRFSecurityTest compiles
- Transfer test fails (no transfer restriction yet)
  </acceptance_criteria>

#### STEP B: GREEN — Add guard to \_update

<read_first>

- contracts/src/EggNFT.sol (\_update lines 520-532)
- contracts/src/EggNFT.sol (EggProperties struct lines 35-48, is_hatched field)
  </read_first>

<action>
Add VRF pending check to _update in EggNFT.sol (after line 525 `address from = _ownerOf(tokenId);`, add at line 526):

```solidity
if (from != address(0)) {
    require(tokenToRequestId[tokenId] == 0, "Cannot transfer during VRF pending");
}
```

This blocks transfers:

1. For eggs with pending VRF (tokenToRequestId != 0) and not hatched yet
2. Allows minting (from == address(0)) to proceed normally
3. After VRF fulfilled and claim completed, tokenToRequestId is cleared (line 310 in claimHatch)
   </action>

<acceptance_criteria>

- contracts/src/EggNFT.sol \_update contains `require(tokenToRequestId[tokenId] == 0, "Cannot transfer during VRF pending");`
- forge build --silent succeeds
- Transfers blocked during VRF pending, allowed after claim
  </acceptance_criteria>

#### STEP C: Verify tests pass

<read_first>

- contracts/test/VRFSecurity.t.sol
- contracts/src/EggNFT.sol (from STEP B)
  </read_first>

<action>
```bash
cd contracts && forge test --match-contract VRFSecurityTest -vvv
cd contracts && forge test --silent
```
</action>

<acceptance_criteria>

- forge test --match-contract VRFSecurityTest: 3/3 passed (1 from plan 50-05 + 2 from this plan)
- Full forge test: >= 0 new failures
  </acceptance_criteria>
