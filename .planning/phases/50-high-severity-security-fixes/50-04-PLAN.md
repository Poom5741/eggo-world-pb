---
phase: 50-high-severity-security-fixes
plan: "04"
type: execute
wave: 2
subsystem: smart-contracts
tags:
  - security
  - high-severity
  - breeding-validation
  - food-count
  - SEC-10
requirements_addressed:
  - SEC-10
dependency_graph:
  provides:
    - Food consumption validation for breeding eggs before hatching
  requires: []
  blocking:
    - "Phase 50 all plans (milestone progression)"
tech_stack:
  added: []
  patterns:
    - "Input validation guard pattern"
key_files:
  modified:
    - contracts/src/EggNFT.sol
  created:
    - contracts/test/BreedingEggValidation.t.sol
autonomous: true
---

# Plan 50-04: Food Count Check for Breeding Eggs (SEC-10)

**Objective:** Add food consumption validation to `hatchBreedingEgg` to ensure breeding eggs consume required food before hatching, preventing premature hatching with guaranteed rarity.

## Tasks

### Task 1: Add food count validation to hatchBreedingEgg

TDD Workflow: RED → GREEN

#### STEP A: RED — Add failing test

<read_first>

- contracts/src/EggNFT.sol (hatchBreedingEgg lines 316-364)
- contracts/src/EggNFT.sol (MAX_FOOD_COUNT on line 25)
- contracts/test/VRFBreedingAndFood.t.sol (from plan 50-02, may have breeding egg tests)
- contracts/test/EggNFT.t.sol (existing breeding tests)
  </read_first>

<action>
Create `contracts/test/BreedingEggValidation.t.sol` with 2 failing tests:

1. `testHatchBreedingEggWithoutFoodFails()` — Breed two animals to create breeding egg, attempt hatchBreedingEgg without feeding; expect revert "Not enough food consumed"

2. `testHatchBreedingEggWithFoodSucceeds()` — Breed animals → feed egg to MAX_FOOD_COUNT (10) → hatchBreedingEgg; expect success

Test setup:

- Same infrastructure as Phase 49 tests
- Need authorized CommissionDistribution, USDT mock, AnimalNFT deployed
- Breed two animals to get a breeding egg
- For test 2: feed using mintFood + feedEgg pattern
  </action>

<acceptance_criteria>

- contracts/test/BreedingEggValidation.t.sol exists with 2 tests
- forge test --match-contract BreedingEggValidationTest compiles
- Tests fail at runtime (no food check in hatchBreedingEgg yet)
  </acceptance_criteria>

#### STEP B: GREEN — Fix EggNFT.sol

<read_first>

- contracts/src/EggNFT.sol (hatchBreedingEgg lines 316-323)
  </read_first>

<action>
Add food count check to hatchBreedingEgg in EggNFT.sol (after line 321 `require(props.is_breeding_egg, "Not a breeding egg");`, add at line 322):

```solidity
require(props.food_count >= MAX_FOOD_COUNT, "Not enough food consumed");
```

This matches the existing check in hatchEgg (line 212) for consistency.
</action>

<acceptance_criteria>

- contracts/src/EggNFT.sol hatchBreedingEgg contains `require(props.food_count >= MAX_FOOD_COUNT, "Not enough food consumed");`
- forge build --silent succeeds
- hatchBreedingEgg requires same MIN_FOOD_COUNT as hatchEgg
  </acceptance_criteria>

#### STEP C: Verify tests pass

<read_first>

- contracts/test/BreedingEggValidation.t.sol
- contracts/src/EggNFT.sol (from STEP B)
  </read_first>

<action>
Run test suite:
```bash
cd contracts && forge test --match-contract BreedingEggValidationTest -vvv
cd contracts && forge test --silent
```
</action>

<acceptance_criteria>

- forge test --match-contract BreedingEggValidationTest: 2/2 passed
- Full forge test: >= 0 new failures
  </acceptance_criteria>
