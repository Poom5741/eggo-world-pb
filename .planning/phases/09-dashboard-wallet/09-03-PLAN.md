---
phase: 09-dashboard-wallet
plan: 03
type: tdd
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  [
    apps/web/app/dashboard/page.test.tsx,
    apps/web/components/dashboard/quick-actions.tsx,
    apps/web/components/dashboard/activity-feed.tsx,
    apps/web/app/dashboard/page.tsx,
  ]
autonomous: true
requirements: [DASH-03, DASH-04, DASH-05]
user_setup: []

must_haves:
  truths:
    - "Quick action buttons trigger correct navigation flows (Feed All, Hatch Ready, Buy Food)"
    - "Recent activity shows last 10 transactions from PocketBase"
    - "Active eggs count displays correctly with egg preview avatars"
  artifacts:
    - path: "apps/web/components/dashboard/quick-actions.tsx"
      provides: "Quick action cards with navigation"
      exports: ["QuickActions"]
    - path: "apps/web/components/dashboard/activity-feed.tsx"
      provides: "Recent activity feed with transaction history"
      exports: ["ActivityFeed"]
    - path: "apps/web/app/dashboard/page.tsx"
      provides: "Dashboard page with all components integrated"
      contains: "QuickActions|ActivityFeed"
  key_links:
    - from: "apps/web/components/dashboard/quick-actions.tsx"
      to: "apps/web/app/dashboard/page.tsx"
      via: "Navigation router.push"
      pattern: "router\\.push\\('/(mint|wallet)"
    - from: "apps/web/components/dashboard/activity-feed.tsx"
      to: "apps/web/lib/pocketbase/client.ts"
      via: "PocketBase transaction query"
      pattern: "collection\\('.*transactions?'\\)|getList"
---

<objective>
Create quick action cards and recent activity feed using TDD workflow

Purpose: Users can quickly access key flows (Feed All, Hatch Ready, Buy Food) and view their last 10 transactions with proper categorization and styling, matching Jules design

Output: Test specifications, QuickActions component, ActivityFeed component, integrated dashboard
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-dashboard-wallet/09-CONTEXT.md
@resources/eggo-world-uxui-jules/src/app/dashboard/page.tsx
@apps/web/components/ui/card.tsx
@apps/web/components/ui/button.tsx

<interfaces>
<!-- Key types and contracts -->

From quick actions:

```typescript
interface QuickAction {
  id: string
  title: string
  description: string
  icon: string // Material Symbols name
  href: string // Navigation path
  color: "primary" | "secondary" | "tertiary"
}
```

From activity feed:

```typescript
interface Transaction {
  id: string
  type: "hatch" | "mint_egg" | "mint_food" | "commission" | "sale"
  title: string
  timestamp: string
  amount: number
  icon: string // Material Symbols name
  color: "primary" | "secondary" | "tertiary"
}

interface ActivityFeedProps {
  transactions: Transaction[]
  loading?: boolean
  error?: string | null
}
```

</interfaces>
</context>

<feature>
  <name>Quick Actions and Activity Feed</name>
  <files>apps/web/components/dashboard/quick-actions.tsx, apps/web/components/dashboard/activity-feed.tsx, apps/web/app/dashboard/page.test.tsx</files>
  <behavior>
    - Test 1: Quick actions displays 3 cards (Feed All, Hatch Ready, Buy Food)
    - Test 2: Each action card has correct icon per D-02: restaurant, auto_fix_high, shopping_basket
    - Test 3: Action cards use colored containers: primary, secondary, tertiary per D-10
    - Test 4: Click action navigates to correct page (/mint, /mint/food, dashboard/eggs)
    - Test 5: Activity feed shows last 10 transactions from PocketBase
    - Test 6: Transactions categorized per D-25: hatch=egg_alt, mint_egg=egg, commission=group, sale=payments
    - Test 7: Activity cards have hover animation: hover:translate-x-2 per D-12
    - Test 8: Active eggs count displays with egg preview avatars (+N overflow indicator)
  </behavior>
  <implementation>
    1. Write test file (RED) - file content tests for QuickActions and ActivityFeed
    2. Create QuickActions component:
       - 3 action cards with colored backgrounds
       - Material Symbols icons on left
       - Title + description in center
       - Chevron on right
       - hover:scale-[1.02], active:scale-[0.98] per D-11
    3. Create ActivityFeed component:
       - Query last 10 transactions from PocketBase
       - Map transaction types to icons/colors per D-25
       - Slide animation on hover
       - "View All History" button in header
    4. Create ActiveEggs card with avatar previews
    5. Integrate all into dashboard page
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Write Quick Actions test specifications (RED)</name>
  <files>apps/web/app/dashboard/page.test.tsx</files>
  <action>
    Add test cases for QuickActions:
    - Verify 3 action cards rendered (Feed All Eggs, Hatch Ready Eggs, Buy Food Bundle)
    - Verify icons: restaurant, auto_fix_high, shopping_basket per D-02
    - Verify container colors: primary-container, secondary-container, tertiary-container per D-10
    - Verify hover/active states: scale-[1.02] and scale-[0.98] per D-11
    - Verify navigation: onClick → router.push to correct paths
    - Verify text descriptions: "Requires X units of food", "3 eggs ready", "Refill supplies"
    
    Use file content assertions with fs.readFileSync pattern.
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx --dry-run</automated>
  </verify>
  <done>Test cases added, failing (RED phase), committed with "red: test spec for Quick Actions (#issue)"</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Create QuickActions component (GREEN)</name>
  <files>apps/web/components/dashboard/quick-actions.tsx</files>
  <action>
    Create QuickActions component:
    - 3 action cards in flex col layout (xl:col-span-4 per Jules)
    - Each card:
      - Colored container: bg-primary-container, bg-secondary-container, bg-tertiary-container
      - Icon circle: w-12 h-12 bg-white/40 rounded-2xl with Material Symbol
      - Title + description text on left
      - Chevron icon on right
      - Transition: hover:scale-[1.02] active:scale-[0.98]
    - Thai comments throughout
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx</automated>
  </verify>
  <done>QuickActions component created, tests pass, committed with "green: implement Quick Actions component (#issue)"</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Write Activity Feed test specifications (RED)</name>
  <files>apps/web/app/dashboard/page.test.tsx</files>
  <action>
    Add test cases for ActivityFeed:
    - Verify last 10 transactions displayed (limit query)
    - Verify transaction categorization per D-25:
      - hatch → "Hatched Egg" + egg_alt icon + tertiary color
      - mint_egg → "Minted Egg" + egg icon
      - commission → "Referral Commission" + group icon + primary color
      - sale → "NFT Sale" + payments icon
    - Verify hover animation: hover:translate-x-2 per D-12
    - Verify "View All History" button in header per D-15
    - Verify card structure: icon circle (left), title + timestamp (center), amount (right)
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx --dry-run</automated>
  </verify>
  <done>Test cases added, failing (RED phase), committed with "red: test spec for Activity Feed (#issue)"</done>
</task>

<task type="auto" tdd="true">
  <name>task 4: Create ActivityFeed component and Active Eggs card (GREEN)</name>
  <files>apps/web/components/dashboard/activity-feed.tsx, apps/web/components/dashboard/active-eggs-card.tsx</files>
  <action>
    1. Create ActivityFeed component:
       - Fetch last 10 transactions from PocketBase (collection TBD - use transactions or commission_records)
       - Map transaction types to icons/colors per D-25
       - Render cards with:
         - Icon circle: w-10 h-10 rounded-full with colored bg
         - Title + timestamp (uppercase, small font)
         - Amount (right aligned, colored)
       - Hover: hover:translate-x-2 transition-transform duration-300
       - "View All History" button in header
    
    2. Create ActiveEggsCard component:
       - Display total egg count from egg_nfts collection
       - Show 3 egg preview avatars + "+N" overflow indicator
       - Use Jules design: -space-x-2 with w-8 h-8 rounded-full
       - Border colors: primary-container, secondary-container, tertiary-container
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx</automated>
  </verify>
  <done>ActivityFeed and ActiveEggsCard created, tests pass, committed with "green: implement Activity Feed (#issue)" and "green: implement Active Eggs card (#issue)"</done>
</task>

<task type="auto" tdd="true">
  <name>task 5: Integrate all components into dashboard page</name>
  <files>apps/web/app/dashboard/page.tsx</files>
  <action>
    Update dashboard page to integrate all components:
    - Import QuickActions, ActivityFeed, ActiveEggsCard
    - Layout per Jules design:
      - Left column (xl:col-span-4): QuickActions
      - Right column (xl:col-span-8): BuddyChain (from Plan 02) + ActivityFeed
      - Stats grid: Include ActiveEggsCard
    - Pass data props from PocketBase queries
    - Handle loading/error states for all components
    - Preserve hydration safety with useIsHydrated
    
    Ensure all components render within LayoutWrapper.
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx</automated>
  </verify>
  <done>All components integrated, tests pass, committed with "green: integrate dashboard components (#issue)"</done>
</task>

<task type="auto">
  <name>Refactor: Optimize and clean up</name>
  <files>apps/web/components/dashboard/quick-actions.tsx, apps/web/components/dashboard/activity-feed.tsx, apps/web/app/dashboard/page.tsx</files>
  <action>
    Clean up code:
    - Extract transaction type mapping to constants (type → icon, color, title)
    - Add TypeScript interfaces for all component props
    - Optimize data fetching with Promise.all for parallel queries
    - Add loading skeletons for all components
    - Ensure consistent Thai comments
    - Remove hardcoded values
    
    Do NOT change functionality - only improve code quality.
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx && bun run build</automated>
  </verify>
  <done>Build succeeds, tests pass, committed with "refactor: optimize dashboard components (#issue)"</done>
</task>

</tasks>

<verification>
- [ ] All tests pass (bun test app/dashboard/page.test.tsx)
- [ ] Build succeeds with zero warnings
- [ ] QuickActions: 3 cards with correct icons and colors
- [ ] ActivityFeed: Last 10 transactions with categorization
- [ ] ActiveEggsCard: Egg count with avatar previews
- [ ] Navigation works: router.push to correct paths
- [ ] Hover animations: scale-[1.02] and translate-x-2
</verification>

<success_criteria>

- DASH-03: Quick action buttons trigger correct navigation flows ✓
- DASH-04: Recent activity shows last 10 transactions from PocketBase ✓
- DASH-05: Active eggs count displays with egg preview avatars ✓
- Test coverage: 12+ test cases for all components ✓
- All components follow Jules design patterns ✓
  </success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-wallet/09-03-SUMMARY.md` documenting:
- QuickActions component structure and navigation
- ActivityFeed transaction categorization mapping
- Data fetching strategy for transactions
- Integration patterns for dashboard layout
</output>
