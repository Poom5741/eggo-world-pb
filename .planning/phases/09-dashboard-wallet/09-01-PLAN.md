---
phase: 09-dashboard-wallet
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  [
    apps/web/app/dashboard/page.test.tsx,
    apps/web/app/dashboard/page.tsx,
    apps/web/components/dashboard/balance-card.tsx,
  ]
autonomous: true
requirements: [FOUND-07, DASH-01]
user_setup: []

must_haves:
  truths:
    - "Dashboard displays user's USDT balance from PocketBase"
    - "Wallet balance auto-polls every 30 seconds with 'Updating...' indicator"
  artifacts:
    - path: "apps/web/app/dashboard/page.tsx"
      provides: "Dashboard page with balance card"
      min_lines: 100
    - path: "apps/web/components/dashboard/balance-card.tsx"
      provides: "Reusable balance card component with polling indicator"
      exports: ["BalanceCard"]
    - path: "apps/web/app/dashboard/page.test.tsx"
      provides: "Test specifications for dashboard balance"
      contains: "describe.*Dashboard"
  key_links:
    - from: "apps/web/app/dashboard/page.tsx"
      to: "apps/web/hooks/use-wallet-poll.ts"
      via: "useWalletPoll hook import"
      pattern: "useWalletPoll.*user.*wallet"
    - from: "apps/web/components/dashboard/balance-card.tsx"
      to: "apps/web/lib/pocketbase/client.ts"
      via: "PocketBase client for balance fetching"
      pattern: "createClient|getUser"
---

<objective>
Create dashboard balance card with auto-polling using TDD workflow

Purpose: Users can see their real-time USDT balance with automatic updates every 30 seconds, matching Jules design with claymorphism styling and Material Symbols icons

Output: Test specifications, balance card component, integrated dashboard page
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-dashboard-wallet/09-CONTEXT.md
@.planning/STATE.md
@resources/eggo-world-uxui-jules/src/app/dashboard/page.tsx
@apps/web/hooks/use-wallet-poll.ts
@apps/web/components/ui/card.tsx

<interfaces>
<!-- Key types and contracts from existing code -->

From apps/web/hooks/use-wallet-poll.ts:

```typescript
interface WalletBalance {
  usdt: string
  native: string
}

interface UseWalletPollReturn {
  balance: WalletBalance
  loading: boolean
  error: string | null
  refresh: () => Promise<void>
}

export function useWalletPoll(
  walletAddress: string | undefined,
  intervalMs?: number
): UseWalletPollReturn
```

From apps/web/lib/pocketbase/client.ts:

```typescript
export function createClient(): PocketBase
export function getUser(): User | null
export function isAuthenticated(): boolean
```

</interfaces>
</context>

<feature>
  <name>Dashboard Balance Card with Auto-Polling</name>
  <files>apps/web/app/dashboard/page.test.tsx, apps/web/components/dashboard/balance-card.tsx, apps/web/app/dashboard/page.tsx</files>
  <behavior>
    - Test 1: Balance card displays USDT balance from useWalletPoll hook
    - Test 2: Shows "Updating..." badge when loading=true from hook
    - Test 3: Uses Material Symbols icon (payments) per D-01, D-02
    - Test 4: Uses clay-xl variant with gradient background per D-20
    - Test 5: Balance updates automatically every 30 seconds (via useWalletPoll)
    - Test 6: Handles error state with retry button per D-22
    - Test 7: Empty wallet state shows "0.00 USDT" (not error)
  </behavior>
  <implementation>
    1. Write test file first (RED) - file content tests checking component structure
    2. Create BalanceCard component with:
       - Gradient background (from-primary/20 via-primary/10 to-transparent)
       - Material Symbols icon: payments
       - "Updating..." badge with pulse animation when loading
       - Balance display: {usdtBalance.toFixed(2)} USDT
       - Error state with retry button
    3. Integrate into dashboard page
    4. Wire up useWalletPoll hook with user.wallet address
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Write dashboard balance test specifications (RED)</name>
  <files>apps/web/app/dashboard/page.test.tsx</files>
  <action>
    Create test file with file content assertions following Phase 8 pattern:
    - Verify page uses useWalletPoll hook
    - Verify "Updating..." badge appears during loading
    - Verify balance displays USDT value with 2 decimal places
    - Verify Material Symbols icon usage (payments)
    - Verify gradient background classes
    - Verify clay-xl variant on balance card
    - Verify error state handling with retry button
    
    Use fs.readFileSync pattern from Phase 8 callback tests. Do NOT run tests yet - they will fail (RED phase).
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx --dry-run</automated>
  </verify>
  <done>Test file exists with 6+ failing test cases, committed with "red: test spec for dashboard balance (#issue)"</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Create BalanceCard component (GREEN)</name>
  <files>apps/web/components/dashboard/balance-card.tsx</files>
  <action>
    Create BalanceCard component implementing minimum code to pass tests:
    - Accept balance, loading, error, refresh as props
    - Render Card with clay-xl variant
    - Apply gradient: from-primary/20 via-primary/10 to-transparent
    - Show Material Symbols icon: <span className="material-symbols-outlined">payments</span>
    - Display "Updating..." badge with animate-pulse when loading=true
    - Show balance as {balance.toFixed(2)} USDT
    - Handle error state: show message + retry button
    - Add Thai comments throughout
    
    Component structure per Jules design:
    - CardHeader: "USDT BALANCE" label + updating badge
    - CardTitle: Large balance amount
    - CardContent: "Available for minting and purchases" subtitle
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx</automated>
  </verify>
  <done>All tests pass, committed with "green: implement BalanceCard component (#issue)"</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Integrate balance card into dashboard page</name>
  <files>apps/web/app/dashboard/page.tsx</files>
  <action>
    Update existing dashboard page to use new BalanceCard component:
    - Import BalanceCard from @/components/dashboard/balance-card
    - Use useWalletPoll hook with user?.wallet address
    - Replace existing balance card section with <BalanceCard /> component
    - Pass balance, loading, error, refresh props
    - Preserve existing header, stats grid, quick actions sections
    
    Ensure hydration safety: only render BalanceCard after isHydrated && user exists
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx</automated>
  </verify>
  <done>Dashboard page imports and renders BalanceCard, all tests pass, committed with "green: integrate balance card into dashboard (#issue)"</done>
</task>

<task type="auto">
  <name>Refactor: Optimize component structure and patterns</name>
  <files>apps/web/components/dashboard/balance-card.tsx, apps/web/app/dashboard/page.tsx</files>
  <action>
    Clean up code with production patterns:
    - Extract magic strings to constants (e.g., POLLING_INTERVAL = 30000)
    - Add proper TypeScript types for props
    - Optimize re-renders with useMemo for balance calculations
    - Add error boundary wrapper
    - Ensure consistent Thai comments
    - Remove any TODO comments left during GREEN phase
    
    Do NOT change functionality - only improve code quality.
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx && bun run build</automated>
  </verify>
  <done>Build succeeds with 0 errors/warnings, tests pass, committed with "refactor: optimize balance card component (#issue)"</done>
</task>

</tasks>

<verification>
- [ ] All tests pass (bun test app/dashboard/page.test.tsx)
- [ ] Build succeeds with zero warnings (bun run build)
- [ ] useWalletPoll hook is called with user?.wallet address
- [ ] "Updating..." badge has animate-pulse class
- [ ] Balance card uses clay-xl variant with gradient
- [ ] Material Symbols icon displays correctly
- [ ] Error state shows retry button
</verification>

<success_criteria>

- FOUND-01: Dashboard displays user's USDT balance from PocketBase ✓
- FOUND-07: Wallet balance auto-polls every 30 seconds with "Updating..." indicator ✓
- Test coverage: 6+ test cases for balance card functionality ✓
- Component reusable and follows Jules design patterns ✓
  </success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-wallet/09-01-SUMMARY.md` documenting:
- Balance card implementation details
- Test patterns used
- Integration with useWalletPoll hook
- Any deviations from Jules design
</output>
