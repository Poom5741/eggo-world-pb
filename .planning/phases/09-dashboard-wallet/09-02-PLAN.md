---
phase: 09-dashboard-wallet
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  [
    apps/web/app/dashboard/page.test.tsx,
    apps/web/components/dashboard/buddy-chain.tsx,
    apps/web/app/dashboard/page.tsx,
  ]
autonomous: true
requirements: [DASH-02]
user_setup: []

must_haves:
  truths:
    - "Referral chain displays 4 levels (G1-G4) with correct commission percentages"
    - "Each level card shows percentage fill visualization with level colors"
  artifacts:
    - path: "apps/web/components/dashboard/buddy-chain.tsx"
      provides: "Buddy Chain component with 4 level cards"
      exports: ["BuddyChain"]
    - path: "apps/web/app/dashboard/page.tsx"
      provides: "Dashboard page integrating BuddyChain"
      contains: "BuddyChain"
  key_links:
    - from: "apps/web/components/dashboard/buddy-chain.tsx"
      to: "apps/web/lib/pocketbase/client.ts"
      via: "PocketBase client for referral data"
      pattern: "commission_records|referral"
    - from: "apps/web/app/dashboard/page.tsx"
      to: "apps/web/components/dashboard/buddy-chain.tsx"
      via: "Component import and render"
      pattern: "import.*BuddyChain"
---

<objective>
Create Buddy Chain referral visualization with 4-level display using TDD workflow

Purpose: Users can see their 4-level referral chain (G1-G4) with percentage progress visualization and correct commission percentages (20%/10%/10%/10%), matching Jules design

Output: Test specifications, BuddyChain component, integration into dashboard
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-dashboard-wallet/09-CONTEXT.md
@resources/eggo-world-uxui-jules/src/app/dashboard/page.tsx
@apps/web/components/ui/card.tsx

<interfaces>
<!-- Key types and contracts for referral data -->

Expected referral data structure:

```typescript
interface ReferralLevel {
  level: number // 1-4
  count: number // Number of buddies at this level
  percentage: number // Fill percentage (0-100)
  commissionRate: number // 0.20 for L1, 0.10 for L2-L4
}

interface BuddyChainProps {
  levels: ReferralLevel[]
  loading?: boolean
  error?: string | null
}
```

Commission percentages per requirements:

- Level 1 (G1): 20%
- Level 2 (G2): 10%
- Level 3 (G3): 10%
- Level 4 (G4): 10%
  </interfaces>
  </context>

<feature>
  <name>Buddy Chain Referral Visualization</name>
  <files>apps/web/components/dashboard/buddy-chain.tsx, apps/web/app/dashboard/page.test.tsx</files>
  <behavior>
    - Test 1: Displays 4 level cards (L1-L4) in a row
    - Test 2: Each card shows percentage fill bar with correct height
    - Test 3: L1 uses primary color, L2 secondary, L3 tertiary, L4 on-surface-variant per D-06
    - Test 4: Each card displays level count (e.g., "42 Buddies")
    - Test 5: Cards use square aspect ratio with percentage overlay
    - Test 6: Commission percentages displayed: 20%, 10%, 10%, 10%
    - Test 7: Card layout matches Jules: percentage → level label → buddy count
  </behavior>
  <implementation>
    1. Write test file (RED) - file content tests for BuddyChain structure
    2. Create BuddyChain component with:
       - Grid layout: 4 columns on desktop, responsive on mobile
       - Each level card: square aspect ratio, colored fill bar, percentage, level label, buddy count
       - Level colors: L1=primary, L2=secondary, L3=tertiary, L4=on-surface-variant
       - Clay card styling with rounded-2xl
    3. Fetch referral data from PocketBase commission_records
    4. Integrate into dashboard page
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Write Buddy Chain test specifications (RED)</name>
  <files>apps/web/app/dashboard/page.test.tsx</files>
  <action>
    Add test cases to existing dashboard test file (or create separate buddy-chain.test.tsx):
    - Verify BuddyChain component renders 4 level cards
    - Verify percentage fill bars use correct colors (primary/secondary/tertiary/on-surface-variant)
    - Verify commission percentages: 20%, 10%, 10%, 10%
    - Verify square aspect ratio cards with percentage overlay
    - Verify level labels: "Lvl 1", "Lvl 2", "Lvl 3", "Lvl 4"
    - Verify buddy count display format: "{count} Buddies"
    
    Use file content assertions with fs.readFileSync pattern.
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx --dry-run</automated>
  </verify>
  <done>Test cases added, failing as expected (RED phase), committed with "red: test spec for Buddy Chain (#issue)"</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Create BuddyChain component (GREEN)</name>
  <files>apps/web/components/dashboard/buddy-chain.tsx</files>
  <action>
    Create BuddyChain component implementing tests:
    - Accept levels array as props: [{level, count, percentage, commissionRate}]
    - Render 4 cards in responsive grid (grid-cols-4 gap-4)
    - Each card:
      - Square aspect ratio (aspect-square)
      - Rounded-2xl with clay-card-inset variant
      - Colored fill bar at bottom: h-[{percentage}%]
      - Level color: L1=bg-primary/20, L2=bg-secondary/20, L3=bg-tertiary/20, L4=bg-on-surface-variant/10
      - Percentage text: pixel-font text-2xl with matching color
      - Level label: "Lvl {N}" uppercase
      - Buddy count: "{count} Buddies"
    - Thai comments throughout
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx</automated>
  </verify>
  <done>BuddyChain component created, all tests pass, committed with "green: implement Buddy Chain component (#issue)"</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Fetch referral data and integrate into dashboard</name>
  <files>apps/web/app/dashboard/page.tsx, apps/web/components/dashboard/buddy-chain.tsx</files>
  <action>
    1. Add referral data fetching to dashboard page:
       - Query commission_records collection grouped by referrer level
       - Calculate buddy count per level (G1-G4)
       - Calculate percentage fill (buddies / target, e.g., 50 buddies target)
    
    2. Integrate BuddyChain into dashboard:
       - Import BuddyChain component
       - Pass levels array as props
       - Place in xl:col-span-8 section per Jules layout
       - Handle loading and error states
    
    3. Wire data flow:
       - Fetch in useEffect after hydration
       - Use Promise.all with other dashboard queries
       - Transform PocketBase data to ReferralLevel[] format
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx</automated>
  </verify>
  <done>BuddyChain integrated with real data, tests pass, committed with "green: integrate Buddy Chain with referral data (#issue)"</done>
</task>

<task type="auto">
  <name>Refactor: Optimize data fetching and component patterns</name>
  <files>apps/web/components/dashboard/buddy-chain.tsx, apps/web/app/dashboard/page.tsx</files>
  <action>
    Clean up code:
    - Extract level color mapping to constant object
    - Add TypeScript interfaces for ReferralLevel, BuddyChainProps
    - Optimize percentage calculations with useMemo
    - Add loading skeleton for BuddyChain
    - Ensure consistent Thai comments
    - Remove hardcoded values, use constants
    
    Do NOT change functionality - only improve code quality.
  </action>
  <verify>
    <automated>cd apps/web && bun test app/dashboard/page.test.tsx && bun run build</automated>
  </verify>
  <done>Build succeeds, tests pass, committed with "refactor: optimize Buddy Chain patterns (#issue)"</done>
</task>

</tasks>

<verification>
- [ ] All tests pass (bun test app/dashboard/page.test.tsx)
- [ ] Build succeeds with zero warnings
- [ ] BuddyChain renders 4 level cards (L1-L4)
- [ ] Percentage fill bars use correct colors per D-06
- [ ] Commission percentages: 20%, 10%, 10%, 10%
- [ ] Square aspect ratio cards with percentage overlay
- [ ] Responsive grid layout (4 cols desktop, responsive mobile)
</verification>

<success_criteria>

- DASH-02: Referral chain displays 4 levels with correct percentages ✓
- Visual design matches Jules "Buddy Chain" card layout ✓
- Level colors follow D-06 specification ✓
- Test coverage: 6+ test cases for BuddyChain functionality ✓
  </success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-wallet/09-02-SUMMARY.md` documenting:
- BuddyChain component structure
- Data fetching strategy for referral data
- Color mapping for level visualization
- Any data transformation from PocketBase format
</output>
