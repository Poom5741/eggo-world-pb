---
phase: 60-withdraw-flow-validation
plan: 01
type: verify
wave: 1
depends_on: [59-01-PLAN.md]
files_modified: []
autonomous: true
requirements: [VERIFY-02]

must_haves:
  truths:
    - "User can withdraw 1 USDT from withdraw page — fee preview shows 0.05 USDT fee, user types external wallet, submits"
    - "5 USDT and 10 USDT withdrawals also succeed with correct fee (D-01 happy paths)"
    - "0.01 USDT (min boundary) withdrawal succeeds (D-01 edge)"
    - "0 USDT and -1 USDT are rejected with 400 validation (D-01 edge)"
    - "Insufficient balance returns 400 (D-04 user error)"
    - "Invalid external wallet address returns 400 (D-04 validation)"
    - "wallet-api unreachable → graceful 500, no crash, balance unchanged (D-04 infra failure)"
    - "Blockchain tx visible on 0xl3 explorer with correct USDT transfer amount (D-02 layer 1)"
    - "Recipient wallet USDT balance increases by (amount - fee) after successful tx (D-02 layer 2)"
    - "PocketBase withdrawal record has correct tx_hash and status=completed (D-02 layer 3)"
    - "Fee config override: set WITHDRAWAL_FEE=3%, withdraw 5 USDT, verify fee=0.15 (D-03)"
    - "Duplicate rapid submit results in exactly one tx executed (D-04 idempotency)"
    - "All tests reuse Phase 59 test users with organically earned commissions (D-05)"
  artifacts:
    - path: "PocketBase withdrawals collection"
      provides: "Withdrawal records with tx_hash values and completed status"
      contains: "user_id, amount, fee, external_wallet_address, status, tx_hash, created"
    - path: "PocketBase wallet_configs collection"
      provides: "WITHDRAWAL_FEE override record"
      contains: "key=WITHDRAWAL_FEE, value=0.03"
    - path: "0xl3 blockchain explorer (tx hash)"
      provides: "On-chain proof of USDT transfer"
      contains: "from=hot_wallet, to=external_wallet, value=amount-fee"
    - path: "On-chain CommissionDistribution contract"
      provides: "Pre/post withdrawal commission balance"
      contains: "getCommissionBalance(user)(uint256)"
  key_links:
    - from: "Frontend /dashboard/withdraw page"
      to: "POST /api/v2/wallet/withdraw"
      via: "PocketBase hook 09-withdraw-usdt.pb.js"
    - from: "POST /api/v2/wallet/withdraw"
      to: "wallet-api POST /api/v1/wallet/transfer"
      via: "$http.send from pb_hook"
    - from: "wallet-api transfer endpoint"
      to: "On-chain USDT transfer via ethers.js"
      via: "hot wallet signer → USDT contract.transfer()"
    - from: "On-chain tx receipt"
      to: "PocketBase withdrawal record"
      via: "tx_hash stored in status=completed record"
---
