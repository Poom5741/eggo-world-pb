---
phase: 16-play-feature-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/auth/AuthLink.test.tsx
  - apps/web/components/referrals/CommissionBreakdown.test.tsx
  - apps/web/components/marketplace/CreateListingDialog.test.tsx
  - apps/web/components/account-modal.test.tsx
  - apps/web/hooks/use-marketplace-sync.test.ts
  - apps/web/hooks/use-auth-redirect.test.tsx
  - apps/web/components/bottom-nav-mobile.test.tsx
  - apps/web/components/food-nft/FoodCard.test.tsx
  - apps/web/components/eggs/egg-card.test.tsx
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "Test suite runs without vi.mock setup errors"
    - "All 9 previously failing tests now pass"
    - "Test output shows zero unhandled errors"
  artifacts:
    - path: "apps/web/components/auth/AuthLink.test.tsx"
      provides: "Fixed vi.mock imports for bun:test"
    - path: "apps/web/components/referrals/CommissionBreakdown.test.tsx"
      provides: "Fixed vi.mocked usage for bun:test"
    - path: "apps/web/components/marketplace/CreateListingDialog.test.tsx"
      provides: "Fixed vi.mock setup"
    - path: "apps/web/components/account-modal.test.tsx"
      provides: "Fixed vi.mock setup"
    - path: "apps/web/hooks/use-marketplace-sync.test.ts"
      provides: "Added vi import from bun:test"
    - path: "apps/web/hooks/use-auth-redirect.test.tsx"
      provides: "Fixed vi.mock setup"
  key_links:
    - from: "apps/web/**/*.test.tsx"
      to: "bun:test"
      via: "vi.mock, vi.fn, vi.mocked imports"
      pattern: "from 'bun:test'"
---

<objective>
Fix all 9 vi.mock test setup failures so the test suite runs clean before adding new Phase 16 feature tests.

Purpose: Establish reliable test infrastructure as foundation for Phase 16 test coverage increase (QUAL-01 per D-05).

Output: All existing tests pass without setup errors, vi.mock syntax matches bun:test requirements.
</objective>

<execution_context>
@/Users/poom-work/tokenine/eggo-pocketbase/.qwen/get-shit-done/workflows/execute-plan.md
@/Users/poom-work/tokenine/eggo-pocketbase/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-play-feature-test-infrastructure/16-CONTEXT.md

<interfaces>
<!-- Key test patterns extracted from codebase -->

From apps/web/components/auth/AuthLink.test.tsx (working pattern):

```typescript
import { describe, it, expect, beforeEach, vi } from "bun:test"
vi.mock("@/lib/pocketbase/client", () => ({
  isAuthenticated: () => mockIsAuthenticated(),
}))
```

From apps/web/hooks/use-marketplace-sync.test.ts (BROKEN - missing vi import):

```typescript
import { describe, it, expect } from "bun:test" // Missing vi!
const mockGetMarketplaceListings = vi.hoisted(() => vi.fn()) // Error: vi is not defined
```

From apps/web/components/referrals/CommissionBreakdown.test.tsx (BROKEN - vi.mocked undefined):

```typescript
TypeError: vi.mocked is not a function
// bun:test does NOT have vi.mocked() - use manual type assertion instead
```

Test runner: bun:test (NOT vitest)

- bun:test provides: vi.mock, vi.fn, vi.hoisted
- bun:test does NOT provide: vi.mocked (use manual casting)
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix vi import failures (3 files)</name>
  <files>
    apps/web/hooks/use-marketplace-sync.test.ts
    apps/web/hooks/use-auth-redirect.test.tsx
    apps/web/components/bottom-nav-mobile.test.tsx
  </files>
  <action>
Fix missing `vi` imports from bun:test in test files that use vi.mock, vi.fn, or vi.hoisted.

**Pattern to apply:**
Change:

```typescript
import { describe, it, expect } from "bun:test"
```

To:

```typescript
import { describe, it, expect, vi, beforeEach } from "bun:test"
```

**Files to fix:**

1. **apps/web/hooks/use-marketplace-sync.test.ts** (line 1):
   - Add `vi, beforeEach` to bun:test import
   - Error was: "ReferenceError: vi is not defined" at line 6

2. **apps/web/hooks/use-auth-redirect.test.tsx** (line 1):
   - Add `vi, beforeEach` to bun:test import
   - Verify all vi.mock calls work after import fix

3. **apps/web/components/bottom-nav-mobile.test.tsx** (line 1):
   - Add `vi` to bun:test import if using any vi.\* functions
   - Check for vi.mock, vi.fn usage

**Critical rules:**

- Do NOT change test logic or assertions — only fix imports
- Keep existing mock implementations intact
- Run `bun test <file>` after each fix to verify
  </action>
  <verify>
  <automated>cd apps/web && bun test hooks/use-marketplace-sync.test.ts hooks/use-auth-redirect.test.tsx components/bottom-nav-mobile.test.tsx 2>&1 | grep -E "(pass|fail|error)" | head -20</automated>
  </verify>
  <done>
  All 3 files have vi imported from bun:test, tests run without "vi is not defined" errors.
  </done>
  </task>

<task type="auto" tdd="false">
  <name>Task 2: Fix vi.mocked usage and mock factory patterns (6 files)</name>
  <files>
    apps/web/components/referrals/CommissionBreakdown.test.tsx
    apps/web/components/marketplace/CreateListingDialog.test.tsx
    apps/web/components/account-modal.test.tsx
    apps/web/components/auth/AuthLink.test.tsx
    apps/web/components/food-nft/FoodCard.test.tsx
    apps/web/components/eggs/egg-card.test.tsx
  </files>
  <action>
Fix vi.mocked() calls (not available in bun:test) and correct vi.mock factory return patterns.

**Issue 1: vi.mocked() not available in bun:test**

Replace:

```typescript
const mockGetList = vi.mocked(createClient().collection("").getList)
```

With:

```typescript
const mockGetList = createClient().collection("").getList as ReturnType<typeof vi.fn>
// Or refactor to use vi.fn() directly in mock factory
```

**Issue 2: vi.mock factory must return correct shape**

Ensure vi.mock factories return objects matching the imported module's exports:

```typescript
vi.mock("@/lib/pocketbase/client", () => ({
  createClient: vi.fn(() => ({
    collection: vi.fn(() => ({
      getList: vi.fn().mockResolvedValue({ items: [], totalItems: 0 }),
    })),
  })),
}))
```

**Files to fix:**

1. **apps/web/components/referrals/CommissionBreakdown.test.tsx**:
   - Replace vi.mocked() calls with manual type assertions
   - Verify mock factory returns correct PocketBase response shape

2. **apps/web/components/marketplace/CreateListingDialog.test.tsx**:
   - Fix vi.mock setup for Dialog component
   - Ensure mock returns proper component shape

3. **apps/web/components/account-modal.test.tsx**:
   - Fix vi.mock factory return types
   - Verify all mocked functions match original signatures

4. **apps/web/components/auth/AuthLink.test.tsx**:
   - Already working pattern — verify no regressions

5. **apps/web/components/food-nft/FoodCard.test.tsx** (if exists):
   - Fix any vi.mocked or incorrect factory patterns

6. **apps/web/components/eggs/egg-card.test.tsx** (if exists):
   - Fix any vi.mocked or incorrect factory patterns

**Critical rules:**

- Do NOT change test assertions — only fix mock setup
- Use vi.fn().mockResolvedValue() or vi.fn().mockReturnValue() for async/sync mocks
- Do NOT use vi.mocked() — bun:test doesn't have it (D-05)
- Run tests after each file fix to catch errors early
  </action>
  <verify>
  <automated>cd apps/web && bun test 2>&1 | grep -c "(fail)"</automated>
  </verify>
  <done>
  All 6 files have vi.mocked() replaced with bun:test-compatible patterns, mock factories return correct shapes, zero test setup errors.
  </done>
  </task>

<task type="auto" tdd="false">
  <name>Task 3: Verify full test suite runs clean</name>
  <files>[]</files>
  <action>
Run complete test suite and verify zero setup failures.

**Steps:**

1. Run `cd apps/web && bun test 2>&1 | tail -50` to see full test output
2. Count failures: `bun test 2>&1 | grep -c "(fail)"`
3. Check for unhandled errors: `bun test 2>&1 | grep -c "Unhandled error"`
4. Verify test count: `bun test 2>&1 | grep "pass"` shows expected number

**Expected outcome:**

- Zero "(fail)" lines
- Zero "Unhandled error" sections
- Zero "ReferenceError" or "TypeError" from test setup
- All tests show "(pass)" status

**If failures remain:**

- Document exact error message
- Check if file uses vitest syntax (should be bun:test)
- Verify mock factories match actual module exports
  </action>
  <verify>
  <automated>cd apps/web && bun test 2>&1 | grep -E "(\(fail\)|Unhandled error|ReferenceError|TypeError)" | wc -l</automated>
  </verify>
  <done>
  Full test suite runs with zero setup errors, all previously failing 9 tests now pass (QUAL-01 complete).
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                 |
| ------------------------ | ----------------------------------------------------------- |
| Test files → Source code | Tests import and mock source modules (no production impact) |

## STRIDE Threat Register

| Threat ID | Category    | Component             | Disposition | Mitigation Plan                                                                  |
| --------- | ----------- | --------------------- | ----------- | -------------------------------------------------------------------------------- |
| T-16-01   | Tampering   | Test mock factories   | accept      | Tests don't affect production code; mock factories only isolate test environment |
| T-16-02   | Repudiation | Test coverage metrics | accept      | Coverage reports are informational, not security-critical for this phase         |

</threat_model>

<verification>
## Overall Phase Checks

1. **Zero setup errors:** `bun test` runs without "ReferenceError", "TypeError", or "Unhandled error" sections
2. **Previously failing tests now pass:** All 9 files that had vi.mock failures show "(pass)" status
3. **No regression in passing tests:** Tests that previously passed still pass
4. **Test count stable or improved:** Same or more tests pass compared to baseline
5. **Build still succeeds:** `bun run build` exits with code 0 (test changes don't break compilation)
   </verification>

<success_criteria>

## Measurable Completion

- [ ] `bun test` shows zero "(fail)" results from vi.mock setup errors
- [ ] `bun test` shows zero "Unhandled error between tests" sections
- [ ] All 9 previously failing test files now pass:
  - AuthLink.test.tsx
  - CommissionBreakdown.test.tsx
  - CreateListingDialog.test.tsx
  - account-modal.test.tsx
  - use-marketplace-sync.test.ts
  - use-auth-redirect.test.tsx
  - bottom-nav-mobile.test.tsx
  - FoodCard.test.tsx (if exists)
  - egg-card.test.tsx (if exists)
- [ ] No vi.mocked() usage remains (bun:test incompatible)
- [ ] All vi.mock factories return correct module shapes
- [ ] `bun run build` exits with code 0
      </success_criteria>

<output>
After completion, create `.planning/phases/16-play-feature-test-infrastructure/16-01-SUMMARY.md`
</output>
