---
phase: 10-egg-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/eggs/page.tsx
  - apps/web/components/eggs/egg-card.tsx
  - apps/web/components/eggs/featured-egg-hero.tsx
  - apps/web/hooks/use-egg-poll.ts
autonomous: true
requirements:
  - EGG-01
  - EGG-02
must_haves:
  truths:
    - "User can see their Egg NFTs listed on My Eggs page"
    - "Egg cards display feeding progress bar showing X/10 food items"
    - "Featured egg hero highlights egg closest to hatching"
  artifacts:
    - path: "apps/web/app/eggs/page.tsx"
      provides: "Main eggs page with grid layout"
      exports: ["default function Eggs"]
    - path: "apps/web/components/eggs/egg-card.tsx"
      provides: "Reusable egg card component"
      exports: ["EggCard"]
    - path: "apps/web/components/eggs/featured-egg-hero.tsx"
      provides: "Featured egg hero section"
      exports: ["FeaturedEggHero"]
    - path: "apps/web/hooks/use-egg-poll.ts"
      provides: "Auto-polling hook for egg status"
      exports: ["useEggPoll"]
  key_links:
    - from: "apps/web/app/eggs/page.tsx"
      to: "apps/web/hooks/use-egg-poll.ts"
      via: "hook import and usage"
      pattern: "import.*useEggPoll"
    - from: "apps/web/components/eggs/egg-card.tsx"
      to: "apps/web/components/ui/progress.tsx"
      via: "progress bar component"
      pattern: "import.*Progress"
---

<objective>
Build egg display foundation with polling, featured egg hero, and egg card grid
Purpose: Users can view their Egg NFTs with feeding progress (EGG-01, EGG-02)
Output: Working eggs page with auto-polling status updates
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-egg-management/10-CONTEXT.md
@.planning/phases/10-egg-management/10-RESEARCH.md
@.planning/REQUIREMENTS.md
@apps/web/hooks/use-wallet-poll.ts
@resources/eggo-world-uxui-jules/src/app/eggs/page.tsx
@apps/web/components/ui/card.tsx
@apps/web/components/ui/progress.tsx
@apps/web/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: Create useEggPoll hook for auto-polling egg status</name>
  <files>apps/web/hooks/use-egg-poll.ts</files>
  <read_first>
    - apps/web/hooks/use-wallet-poll.ts (polling pattern to adapt)
    - apps/web/lib/pocketbase/client.ts (PocketBase client)
  </read_first>
  <action>
    Create new hook `useEggPoll` that adapts useWalletPoll pattern for egg NFTs:
    
    1. Import `useState`, `useEffect`, `useCallback` from 'react'
    2. Define interface `EggData` with fields: id, egg_id, food_count, is_hatched, token_id, minted_at
    3. Define interface `UseEggPollReturn` with: eggs: EggData[], loading: boolean, error: string | null, refresh: () => Promise<void>
    4. Export function `useEggPoll(walletAddress: string | undefined, intervalMs: number = 30000): UseEggPollReturn`
    5. Inside function:
       - State: eggs (EggData[]), loading (boolean), error (string | null)
       - fetchEggs callback: if !walletAddress return; setLoading(true); try: fetch from PocketBase `egg_nfts` collection with filter `owner = "${walletAddress}" && is_hatched = false`; sort by `-food_count` to get eggs closest to hatching first; setEggs(records.items); setError(null); catch: setError(err.message); finally: setLoading(false)
       - useEffect: call fetchEggs() on mount; set interval to call fetchEggs() every intervalMs; cleanup on unmount
    6. Add Thai comments explaining polling logic
    7. Export at bottom
  </action>
  <acceptance_criteria>
    - File exists at apps/web/hooks/use-egg-poll.ts
    - Exports `useEggPoll` function
    - Fetches from PocketBase `egg_nfts` collection
    - Filters by owner wallet address
    - Polls every 30 seconds by default
    - Contains Thai comments
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/web/hooks/use-egg-poll.ts && grep -q "export function useEggPoll" apps/web/hooks/use-egg-poll.ts && echo "✓ Hook created"</automated>
  </verify>
  <done>
    Hook created with auto-polling every 30 seconds, fetches user's eggs from PocketBase, returns eggs array with loading/error states
  </done>
</task>

<task type="auto">
  <name>task 2: Create EggCard component with progress bar and rarity badge</name>
  <files>apps/web/components/eggs/egg-card.tsx</files>
  <read_first>
    - resources/eggo-world-uxui-jules/src/app/eggs/page.tsx (lines 78-159 for card design)
    - apps/web/components/ui/card.tsx (claymorphism variants)
    - apps/web/components/ui/progress.tsx (progress bar)
    - apps/web/components/ui/badge.tsx (rarity badges)
  </read_first>
  <action>
    Create EggCard component following Jules design exactly (per D-01, D-03, D-04, D-05):
    
    1. Import React, EggData type from use-egg-poll, Progress from ui/progress, Badge from ui/badge, cn utility
    2. Define interface `EggCardProps` with: egg: EggData, onManage: (eggId: number) => void
    3. Export function `EggCard({ egg, onManage }: EggCardProps)`
    4. Calculate progressPercent = (egg.food_count / 10) * 100
    5. Determine status: 'Ready' if food_count < 10 && !is_hatched, 'Feeding' if food_count > 0 && food_count < 10, 'Hatched' if is_hatched (per D-06)
    6. Determine rarity from rarity_seed (Common/Rare/Epic/Legendary) - use placeholder logic for now
    7. Return JSX (claymorphism card per D-05):
       - Root div: className="bg-surface-container-lowest p-6 rounded-xl clay-card hover:-translate-y-2 transition-transform duration-300"
       - Image section: div with egg image (128px height, centered)
       - Info section: egg name (#ID), rarity badge (Badge component), element type
       - Progress section: Progress component showing progressPercent, text showing "{egg.food_count}/10 food items"
       - Button: "Manage Egg" button that calls onManage(egg.egg_id)
    8. Add Thai comments for each section
    9. Export default
  </action>
  <acceptance_criteria>
    - File exists at apps/web/components/eggs/egg-card.tsx
    - Exports `EggCard` component
    - Uses clay-card className
    - Contains Progress component showing X/10
    - Contains Badge component for rarity
    - Has "Manage Egg" button
    - Contains hover:-translate-y-2 animation
    - Contains Thai comments
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/web/components/eggs/egg-card.tsx && grep -q "export.*EggCard" apps/web/components/eggs/egg-card.tsx && grep -q "clay-card" apps/web/components/eggs/egg-card.tsx && echo "✓ EggCard created"</automated>
  </verify>
  <done>
    EggCard component displays egg image, name, rarity badge, element type, feeding progress bar (X/10), and "Manage Egg" button with hover animation
  </done>
</task>

<task type="auto">
  <name>task 3: Create FeaturedEggHero component and eggs page layout</name>
  <files>apps/web/components/eggs/featured-egg-hero.tsx, apps/web/app/eggs/page.tsx</files>
  <read_first>
    - resources/eggo-world-uxui-jules/src/app/eggs/page.tsx (full page layout)
    - apps/web/hooks/use-egg-poll.ts (hook we just created)
    - apps/web/components/eggs/egg-card.tsx (card component we just created)
    - apps/web/components/LayoutWrapper.tsx (navigation wrapper)
  </read_first>
  <action>
    Part A - Create FeaturedEggHero component (per D-02, D-03, D-04):
    
    1. Create `apps/web/components/eggs/featured-egg-hero.tsx`
    2. Define interface `FeaturedEggHeroProps` with: egg: EggData
    3. Export function `FeaturedEggHero({ egg }: FeaturedEggHeroProps)`
    4. Return JSX matching Jules design (lines 24-77 from reference):
       - Section container: bg-surface-container-low rounded-xl p-8 clay-card relative overflow-hidden
       - Grid layout: lg:grid-cols-2 gap-12 items-center
       - Left column: Egg image in rounded frame with "LEGENDARY" badge (rotated 12deg)
       - Right column: Egg name (text-4xl font-pixel-style), element badge, progress bar (large), "FEED ME" and "PLAY" buttons, Eggo's tip box
    5. Add Thai comments
    
    Part B - Create eggs page:
    
    1. Create `apps/web/app/eggs/page.tsx`
    2. Import LayoutWrapper, useEggPoll, useIsHydrated, FeaturedEggHero, EggCard, router
    3. Export default function Eggs()
    4. Get isHydrated = useIsHydrated()
    5. Get user from pb.authStore.record (after hydration check)
    6. Call useEggPoll(user?.wallet) to get eggs, loading, error
    7. Auth guard: useEffect - if isHydrated && !user, router.push('/auth/login')
    8. Loading state: if !isHydrated || loading, return loading skeleton
    9. Empty state: if eggs.length === 0, return "No eggs yet" message
    10. Main layout:
        - LayoutWrapper
        - Page header: "My Egg Inventory" title, subtitle
        - FeaturedEggHero with eggs[0] (highest food_count from sorted query)
        - Grid: md:grid-cols-2 lg:grid-cols-3 gap-8
        - Map remaining eggs to EggCard components
    11. Add Thai comments
  </action>
  <acceptance_criteria>
    - File exists at apps/web/components/eggs/featured-egg-hero.tsx
    - File exists at apps/web/app/eggs/page.tsx
    - FeaturedEggHero has grid layout with egg image and details
    - Eggs page imports and uses useEggPoll hook
    - Eggs page has auth guard redirecting to /auth/login
    - Eggs page displays FeaturedEggHero at top
    - Eggs page has 3-column grid (lg), 2-column (md), 1-column (mobile)
    - Contains Thai comments
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/web/components/eggs/featured-egg-hero.tsx && test -f apps/web/app/eggs/page.tsx && grep -q "export default function Eggs" apps/web/app/eggs/page.tsx && echo "✓ Eggs page created"</automated>
  </verify>
  <done>
    Featured egg hero displays at top with egg closest to hatching, below is responsive grid (3 cols lg, 2 md, 1 mobile) showing all user's eggs with progress bars
  </done>
</task>

</tasks>

<verification>
**Overall Phase Checks:**
- [ ] `app/eggs/page.tsx` loads and displays user's eggs
- [ ] Featured egg hero shows at top (egg with highest food_count)
- [ ] Egg cards display in responsive grid (3 cols desktop, 1 mobile)
- [ ] Each card shows feeding progress bar (X/10)
- [ ] Status updates every 30 seconds via polling
- [ ] "Updating..." badge appears during polling (reuse Phase 9 pattern)
</verification>

<success_criteria>

- My Eggs page lists all user's Egg NFTs with status badges (EGG-01) ✓
- Egg card displays feeding progress bar showing X/10 food items collected (EGG-02) ✓
- Page auto-refreshes every 30 seconds via useEggPoll hook
- Auth guard redirects unauthenticated users to /auth/login
- All components use claymorphism styling per Jules design
  </success_criteria>

<output>
After completion, create `.planning/phases/10-egg-management/10-01-SUMMARY.md` with:
- What was built (egg display, polling, cards)
- Files created/modified
- Links to components
- Testing notes
</output>
