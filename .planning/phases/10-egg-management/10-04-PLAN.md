---
phase: 10-egg-management
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified:
  - apps/web/app/eggs/loading.tsx
  - apps/web/app/eggs/error.tsx
  - apps/web/components/eggs/egg-card.tsx
  - apps/web/hooks/use-egg-poll.ts
autonomous: true
requirements:
  - EGG-07
must_haves:
  truths:
    - "Loading skeleton displays while eggs are fetching"
    - "Error boundary shows on fetch failures with retry button"
    - "'Updating...' badge appears during polling (per D-18)"
    - "Exponential backoff on errors (per D-20)"
    - "All edge cases handled (empty state, network errors, no eggs)"
  artifacts:
    - path: "apps/web/app/eggs/loading.tsx"
      provides: "Loading skeleton UI"
      exports: ["default function Loading"]
    - path: "apps/web/app/eggs/error.tsx"
      provides: "Error boundary with retry"
      exports: ["default function Error"]
  key_links:
    - from: "apps/web/hooks/use-egg-poll.ts"
      to: "apps/web/app/eggs/page.tsx"
      via: "error and loading states"
      pattern: "loading.*error"
    - from: "apps/web/components/eggs/egg-card.tsx"
      to: "apps/web/components/ui/badge.tsx"
      via: "'Updating...' badge"
      pattern: "Badge.*Updating"
---

<objective>
Add polish with loading states, error boundaries, polling indicators, and edge case handling
Purpose: Production-ready UX with smooth loading, clear errors, and robust polling (EGG-07)
Output: Polished eggs page with professional UX patterns
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-egg-management/10-CONTEXT.md
@.planning/phases/10-egg-management/10-RESEARCH.md
@.planning/REQUIREMENTS.md
@apps/web/app/auth/loading.tsx (existing loading pattern)
@apps/web/app/auth/error.tsx (existing error pattern)
@apps/web/components/ui/badge.tsx (badge for "Updating..." indicator)
@apps/web/components/ui/skeleton.tsx (loading skeletons)
</context>

<tasks>

<task type="auto">
  <name>task 1: Create loading skeleton and error boundary for eggs page</name>
  <files>apps/web/app/eggs/loading.tsx, apps/web/app/eggs/error.tsx</files>
  <read_first>
    - apps/web/app/auth/loading.tsx (existing loading pattern)
    - apps/web/app/auth/error.tsx (existing error pattern)
    - apps/web/components/ui/skeleton.tsx (skeleton primitives)
  </read_first>
  <action>
    Part A - Create loading skeleton:
    
    1. Create `apps/web/app/eggs/loading.tsx`
    2. Import Skeleton from ui/skeleton, LayoutWrapper
    3. Export default function Loading()
    4. Return LayoutWrapper with:
       - Page header skeleton:
         - Skeleton width="w-64" height="h-12" (title)
         - Skeleton width="w-96" height="h-6" (subtitle)
       - Featured egg hero skeleton:
         - Large skeleton box: w-full h-64 rounded-xl
         - Grid skeleton for details: 3 Skeleton boxes stacked
       - Egg grid skeleton:
         - Grid layout matching page: md:grid-cols-2 lg:grid-cols-3 gap-8
         - 6 EggCard skeletons (2 rows × 3 cols):
           - Each: rounded-xl clay-card p-6
           - Skeleton for image: w-full h-48 rounded-lg
           - Skeleton for title: w-32 h-6 mt-4
           - Skeleton for progress: w-full h-4 mt-2
           - Skeleton for button: w-full h-10 mt-4
    5. Add animation: all skeletons use animate-pulse
    6. Add Thai comments
    
    Part B - Create error boundary:
    
    1. Create `apps/web/app/eggs/error.tsx`
    2. Import Button from ui/button, LayoutWrapper
    3. Define interface `ErrorProps` with: error: Error & { digest?: string }, reset: () => void
    4. Export default function Error({ error, reset }: ErrorProps)
    5. Return LayoutWrapper with centered error container:
       - Icon: span.material-symbols-outlined text-6xl text-primary "error"
       - Title: "Failed to load eggs"
       - Error message: error.message (truncate if long)
       - Help text: "Please check your connection and try again"
       - Retry button: Button onClick={reset} with icon "refresh"
       - Alternative: "Go to Dashboard" link to /dashboard
    6. Style error container: clay-card bg-surface-container-low rounded-xl p-8 max-w-md mx-auto mt-20
    7. Add Thai comments
  </action>
  <acceptance_criteria>
    - File exists at apps/web/app/eggs/loading.tsx
    - File exists at apps/web/app/eggs/error.tsx
    - Loading skeleton matches page layout structure
    - Skeleton has animate-pulse animation
    - Error boundary has retry button
    - Error boundary has link back to dashboard
    - Both contain Thai comments
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/web/app/eggs/loading.tsx && test -f apps/web/app/eggs/error.tsx && grep -q "export default function Loading" apps/web/app/eggs/loading.tsx && grep -q "export default function Error" apps/web/app/eggs/error.tsx && echo "✓ Loading and error created"</automated>
  </verify>
  <done>
    Loading skeleton displays during initial fetch, error boundary shows on failures with retry button and dashboard link
  </done>
</task>

<task type="auto">
  <name>task 2: Add "Updating..." badge during polling with exponential backoff</name>
  <files>apps/web/hooks/use-egg-poll.ts, apps/web/components/eggs/egg-card.tsx</files>
  <read_first>
    - apps/web/hooks/use-egg-poll.ts (polling hook from Plan 01)
    - apps/web/components/ui/badge.tsx (badge component)
  </read_first>
  <action>
    Part A - Update useEggPoll hook with exponential backoff (per D-20):
    
    1. Edit `apps/web/hooks/use-egg-poll.ts`
    2. Add state: pollInterval (number), errorCount (number)
    3. Update fetchEggs callback:
       - On success: setErrorCount(0), reset pollInterval to 30000
       - On error: setErrorCount(prev => prev + 1), calculate new interval:
         - Backoff: 30s → 60s → 120s → 5min (per D-20)
         - Formula: min(30000 * Math.pow(2, errorCount), 300000)
    4. Add to return object: polling (boolean), lastUpdated (Date | null)
    5. Update useEffect to use dynamic pollInterval
    6. Add Thai comments explaining backoff logic
    
    Part B - Add "Updating..." badge to egg cards (per D-18):
    
    1. Edit `apps/web/components/eggs/egg-card.tsx`
    2. Import Badge from ui/badge
    3. Add prop: polling?: boolean to EggCardProps
    4. In card JSX, top-right corner:
       - If polling:
         - Badge with className="animate-pulse"
         - Text: "Updating..."
         - Icon: material-symbols-outlined "sync" (spinning)
    5. Wire polling prop from page: pass from useEggPoll return value
    6. Add Thai comments for badge
  </action>
  <acceptance_criteria>
    - useEggPoll.ts has exponential backoff logic
    - Backoff formula: min(30000 * 2^errorCount, 300000)
    - Backoff resets on success
    - EggCard shows "Updating..." badge when polling=true
    - Badge has animate-pulse class
    - Badge shows sync icon
    - Contains Thai comments
  </acceptance_criteria>
  <verify>
    <automated>grep -q "exponential.*backoff\|Math.pow(2" apps/web/hooks/use-egg-poll.ts && echo "✓ Backoff added"</automated>
    <automated>grep -q "Updating" apps/web/components/eggs/egg-card.tsx && grep -q "animate-pulse" apps/web/components/eggs/egg-card.tsx && echo "✓ Badge added"</automated>
  </verify>
  <done>
    Polling uses exponential backoff (30s→60s→120s→5min) on errors, resets on success; "Updating..." badge with pulse animation appears during polling
  </done>
</task>

<task type="auto">
  <name>task 3: Handle edge cases (empty state, no wallet, network errors)</name>
  <files>apps/web/app/eggs/page.tsx</files>
  <read_first>
    - apps/web/app/eggs/page.tsx (page from Plan 01)
  </read_first>
  <action>
    Enhance eggs page with comprehensive edge case handling:
    
    1. Edit `apps/web/app/eggs/page.tsx`
    2. Empty state (no eggs yet):
       - Check: if eggs.length === 0
       - Show: clay-card p-12 text-center
       - Icon: material-symbols-outlined "egg" text-6xl
       - Title: "No eggs yet"
       - Text: "Purchase your first Egg NFT from the marketplace"
       - Button: "Go to Marketplace" → router.push('/marketplace')
    3. No wallet connected:
       - Check: if !user?.wallet (after hydration)
       - Show: clay-card p-12 text-center
       - Icon: material-symbols-outlined "account_balance_wallet" text-6xl
       - Title: "Wallet not connected"
       - Text: "Please connect your wallet to view your eggs"
       - Button: "Connect Wallet" → links to wallet connection flow
    4. Network error handling:
       - Check: if error from useEggPoll
       - Show error from error boundary (Next.js error.tsx handles this)
       - Add manual retry button in page as alternative
    5. Partial data handling:
       - If egg.food_count is undefined, default to 0
       - If egg.rarity_seed is missing, show "Unknown" rarity
       - If egg image fails to load, show fallback egg SVG
    6. Add helpful UI hints:
       - Tooltip on progress bar: "Feed 10 food items to hatch your egg"
       - Hint for featured egg: "This egg is closest to hatching!"
    7. Ensure all states have Thai comments
  </action>
  <acceptance_criteria>
    - Page handles empty state (eggs.length === 0)
    - Empty state has "Go to Marketplace" button
    - Page handles missing wallet
    - Wallet state has "Connect Wallet" button
    - Network errors show retry option
    - Missing egg data has fallbacks (food_count=0, rarity="Unknown")
    - Fallback egg image for broken URLs
    - Contains Thai comments
  </acceptance_criteria>
  <verify>
    <automated>grep -q "eggs.length === 0\|eggs?.length === 0" apps/web/app/eggs/page.tsx && grep -q "Marketplace" apps/web/app/eggs/page.tsx && echo "✓ Edge cases handled"</automated>
  </verify>
  <done>
    All edge cases handled: empty state (no eggs), no wallet, network errors, missing data with fallbacks, helpful UI hints and tooltips
  </done>
</task>

</tasks>

<verification>
**Overall Phase Checks:**
- [ ] Loading skeleton displays during initial fetch
- [ ] Error boundary appears on failures with retry button
- [ ] "Updating..." badge pulses during polling
- [ ] Polling interval backs off on errors (30s → 60s → 120s → 5min)
- [ ] Empty state shows "No eggs yet" with marketplace link
- [ ] Missing wallet shows "Connect Wallet" prompt
- [ ] Broken egg images show fallback SVG
- [ ] All toast notifications show success/error messages
</verification>

<success_criteria>

- Egg status updates automatically after blockchain confirmation (EGG-07) ✓
- "Updating..." badge displays with pulse animation during polling (per D-18)
- Exponential backoff on errors: 30s → 60s → 120s → 5min, reset on success (per D-20)
- Loading skeleton matches page layout
- Error boundary provides retry and navigation options
- Empty state is friendly with clear call-to-action
- All edge cases handled gracefully
  </success_criteria>

<output>
After completion, create `.planning/phases/10-egg-management/10-04-SUMMARY.md` with:
- What was built (loading, error handling, polling polish)
- Files created/modified
- Backoff interval formula
- Edge cases handled
- Testing notes (verify retry works, polling resumes)
</output>
