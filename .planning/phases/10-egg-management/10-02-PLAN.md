---
phase: 10-egg-management
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - apps/web/hooks/use-egg-feed.ts
  - apps/web/components/eggs/feed-dialog.tsx
  - apps/web/lib/contracts/egg-nft.ts
  - apps/web/app/eggs/page.tsx
autonomous: true
requirements:
  - EGG-03
  - EGG-04
must_haves:
  truths:
    - "User can click 'Manage Egg' button to open feed dialog"
    - "Feed dialog auto-selects exactly 10 food items from inventory"
    - "User sees confirmation dialog before transaction"
    - "Feed transaction calls smart contract with correct parameters"
    - "Success toast appears after transaction submitted"
  artifacts:
    - path: "apps/web/hooks/use-egg-feed.ts"
      provides: "Feed transaction logic"
      exports: ["useEggFeed"]
    - path: "apps/web/components/eggs/feed-dialog.tsx"
      provides: "Feed flow dialog/modal"
      exports: ["FeedDialog"]
    - path: "apps/web/lib/contracts/egg-nft.ts"
      provides: "Contract ABI and function wrappers"
      exports: ["upgradeEggRarity", "getFoodCount"]
  key_links:
    - from: "apps/web/components/eggs/feed-dialog.tsx"
      to: "apps/web/lib/contracts/egg-nft.ts"
      via: "contract function calls"
      pattern: "upgradeEggRarity|getFoodCount"
    - from: "apps/web/hooks/use-egg-feed.ts"
      to: "/api/wallet"
      via: "wallet API call"
      pattern: "fetch.*/api/wallet"
---

<objective>
Implement feed flow with quick-fill auto-select and smart contract integration
Purpose: Users can feed their eggs with exactly 10 food items (EGG-03, EGG-04)
Output: Working feed dialog with transaction confirmation and success feedback
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-egg-management/10-CONTEXT.md
@.planning/phases/10-egg-management/10-RESEARCH.md
@.planning/REQUIREMENTS.md
@contracts/src/EggNFT.sol (lines 228-283 for upgradeEggRarity)
@apps/web/components/ui/dialog.tsx
@apps/web/components/ui/button.tsx
@apps/web/hooks/use-toast.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create contract integration library for EggNFT functions</name>
  <files>apps/web/lib/contracts/egg-nft.ts</files>
  <read_first>
    - contracts/src/EggNFT.sol (lines 194-227 for hatchEgg, lines 228-283 for upgradeEggRarity)
    - apps/web/lib/pocketbase/client.ts (pattern for client creation)
  </read_first>
  <action>
    Create contract integration library (no wallet-api needed yet — direct contract calls):
    
    1. Create `apps/web/lib/contracts/egg-nft.ts`
    2. Define constants:
       - `EGG_NFT_ADDRESS = process.env.NEXT_PUBLIC_EGG_NFT_ADDRESS || ''`
       - `EGG_NFT_ABI` array with function signatures from EggNFT.sol:
         - `function upgradeEggRarity(uint256 eggTokenId, uint256[] calldata foodIds) external`
         - `function hatchEgg(uint256 tokenId) external nonReentrant returns (uint256)`
         - `function getFoodCount(uint256 tokenId) external view returns (uint256)`
         - `function isEggHatched(uint256 tokenId) external view returns (bool)`
         - `function getEggProperties(uint256 tokenId) external view returns (...)`
    3. Import ethers v6 from 'ethers'
    4. Export function `getEggNftContract(signer: ethers.Signer)`: returns new ethers.Contract(EGG_NFT_ADDRESS, EGG_NFT_ABI, signer)
    5. Export function `upgradeEggRarity(signer: ethers.Signer, eggTokenId: number, foodIds: number[]): Promise<string>`:
       - Get contract via getEggNftContract(signer)
       - Call contract.upgradeEggRarity(eggTokenId, foodIds)
       - Return transaction hash
    6. Export function `hatchEgg(signer: ethers.Signer, eggTokenId: number): Promise<number>`:
       - Get contract
       - Call contract.hatchEgg(eggTokenId)
       - Wait for transaction receipt
       - Parse logs to get animal token ID from EggHatched event
       - Return animal token ID
    7. Export function `getFoodCount(signer: ethers.Signer, eggTokenId: number): Promise<number>`:
       - Get contract
       - Call contract.getFoodCount(eggTokenId)
       - Return food count as number
    8. Add Thai comments explaining each function
  </action>
  <acceptance_criteria>
    - File exists at apps/web/lib/contracts/egg-nft.ts
    - Exports `getEggNftContract`, `upgradeEggRarity`, `hatchEgg`, `getFoodCount`
    - Contains EGG_NFT_ADDRESS from env
    - Contains EGG_NFT_ABI with function signatures
    - Uses ethers v6 for contract calls
    - Contains Thai comments
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/web/lib/contracts/egg-nft.ts && grep -q "export.*upgradeEggRarity" apps/web/lib/contracts/egg-nft.ts && grep -q "ethers" apps/web/lib/contracts/egg-nft.ts && echo "✓ Contract library created"</automated>
  </verify>
  <done>
    Contract library provides type-safe wrappers for upgradeEggRarity, hatchEgg, getFoodCount, isEggHatched with proper ABI definitions
  </done>
</task>

<task type="auto">
  <name>task 2: Create useEggFeed hook for feed transaction logic</name>
  <files>apps/web/hooks/use-egg-feed.ts</files>
  <read_first>
    - apps/web/lib/contracts/egg-nft.ts (contract functions we just created)
    - apps/web/hooks/use-toast.ts (toast notifications)
  </read_first>
  <action>
    Create hook to manage feed flow:
    
    1. Create `apps/web/hooks/use-egg-feed.ts`
    2. Import `useState`, `useCallback` from 'react', `upgradeEggRarity` from lib/contracts/egg-nft, `useToast` from use-toast
    3. Define interface `UseEggFeedReturn` with:
       - feedEgg: (eggId: number, foodIds: number[]) => Promise<boolean>
       - loading: boolean
       - error: string | null
    4. Export function `useEggFeed(): UseEggFeedReturn`
    5. Inside:
       - State: loading (false), error (null)
       - Get toast from useToast()
       - feedEgg callback with params (eggId: number, foodIds: number[]):
         - Validate: if foodIds.length !== 10, throw error "Must feed exactly 10 food items" (per EGG-03)
         - setLoading(true), setError(null)
         - Try:
           - Get signer (from window.ethereum or dacc-js)
           - Call upgradeEggRarity(signer, eggId, foodIds)
           - Get txHash
           - Show toast: "Feeding submitted! Waiting for confirmation..."
           - Wait for transaction receipt (poll every 2s)
           - On success: toast.success("Egg fed successfully! 10 food items added")
           - Return true
         - Catch (err):
           - setError(err.message)
           - toast.error(`Feed failed: ${err.message}`)
           - Return false
         - Finally: setLoading(false)
    6. Add Thai comments explaining feed logic and validation
    7. Export return object
  </action>
  <acceptance_criteria>
    - File exists at apps/web/hooks/use-egg-feed.ts
    - Exports `useEggFeed` function
    - feedEgg validates foodIds.length === 10
    - Shows toast notifications on success/error
    - Waits for transaction confirmation
    - Contains Thai comments
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/web/hooks/use-egg-feed.ts && grep -q "export.*useEggFeed" apps/web/hooks/use-egg-feed.ts && grep -q "foodIds.length !== 10" apps/web/hooks/use-egg-feed.ts && echo "✓ Feed hook created"</automated>
  </verify>
  <done>
    Hook validates exactly 10 food items, calls upgradeEggRarity contract function, shows toast notifications, waits for blockchain confirmation
  </done>
</task>

<task type="auto">
  <name>task 3: Create FeedDialog with quick-fill auto-select</name>
  <files>apps/web/components/eggs/feed-dialog.tsx</files>
  <read_first>
    - apps/web/components/ui/dialog.tsx (Dialog primitives)
    - apps/web/components/ui/button.tsx (Button variants)
    - apps/web/hooks/use-egg-feed.ts (hook we just created)
  </read_first>
  <action>
    Create feed dialog component with quick-fill (per D-07, D-08, D-09, D-10):
    
    1. Create `apps/web/components/eggs/feed-dialog.tsx`
    2. Import Dialog primitives from ui/dialog, Button from ui/button, useEggFeed from hooks, EggData type
    3. Define interface `FeedDialogProps` with:
       - egg: EggData
       - open: boolean
       - onOpenChange: (open: boolean) => void
       - onSuccess: () => void
    4. Export function `FeedDialog({ egg, open, onOpenChange, onSuccess }: FeedDialogProps)`
    5. Get feedEgg, loading, error from useEggFeed()
    6. State: confirmed (false) - for two-step confirmation
    7. Quick-fill function:
       - `handleQuickFill()`:
       - Fetch user's food NFTs from PocketBase `food_nfts` collection
       - Select first 10 food items (per D-08: auto-select, no manual UI)
       - Set confirmed = true to show confirmation dialog
    8. Submit function:
       - `handleSubmit()`:
       - Call feedEgg(egg.egg_id, selectedFoodIds)
       - If success: onSuccess(), onOpenChange(false), reset confirmed = false
    9. Render Dialog:
       - Dialog open={open} onOpenChange={onOpenChange}
       - DialogContent with title "Feed [Egg Name]"
       - If !confirmed:
         - Show info: "This will feed your egg with 10 food items from your inventory"
         - Show "FEED ME" button (auto-select 10) → calls handleQuickFill
       - If confirmed:
         - Show confirmation: "Feed [Egg Name] with 10 food items?"
         - Show "Confirm" button → calls handleSubmit
         - Show "Cancel" button → resets confirmed = false
       - Show loading state during transaction
    10. Wire into eggs page: EggCard's onManage opens FeedDialog
    11. Add Thai comments
  </action>
  <acceptance_criteria>
    - File exists at apps/web/components/eggs/feed-dialog.tsx
    - Exports `FeedDialog` component
    - Uses Dialog primitives from ui/dialog
    - Has quick-fill auto-select (first 10 food items)
    - Has two-step confirmation (select → confirm)
    - Calls useEggFeed hook
    - Shows loading state during transaction
    - Contains Thai comments
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/web/components/eggs/feed-dialog.tsx && grep -q "export.*FeedDialog" apps/web/components/eggs/feed-dialog.tsx && grep -q "handleQuickFill" apps/web/components/eggs/feed-dialog.tsx && echo "✓ FeedDialog created"</automated>
  </verify>
  <done>
    FeedDialog opens from egg card, auto-selects first 10 food items, shows confirmation dialog, submits feed transaction, displays success toast
  </done>
</task>

</tasks>

<verification>
**Overall Phase Checks:**
- [ ] Feed dialog opens when clicking "Manage Egg" button
- [ ] Quick-fill auto-selects exactly 10 food items (no manual selection)
- [ ] Confirmation dialog appears before transaction
- [ ] Transaction calls upgradeEggRarity with correct parameters
- [ ] Success toast shows after transaction submitted
- [ ] Egg card progress bar updates after confirmation
- [ ] Error handling shows error message with retry option
</verification>

<success_criteria>

- Feed flow allows selecting one egg and exactly 10 food items from inventory (EGG-03) ✓
- Feed transaction calls smart contract upgradeEggRarity() with correct parameters (eggId, foodIds[]) (EGG-04) ✓
- Quick-fill auto-selects first 10 food items (per D-07, D-08)
- Confirmation dialog before transaction (per D-09)
- Success toast after transaction submitted (per D-10)
  </success_criteria>

<output>
After completion, create `.planning/phases/10-egg-management/10-02-SUMMARY.md` with:
- What was built (feed flow, contract integration, dialog)
- Files created/modified
- Contract function signatures used
- Testing notes (verify 10 food items validation)
</output>
