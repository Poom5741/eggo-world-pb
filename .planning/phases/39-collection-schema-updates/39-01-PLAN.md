---
phase: 39
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/collections/users.json
  - apps/backend/collections/egg_nfts.json
  - apps/backend/collections/food_nfts.json
  - apps/backend/collections/animal_nfts.json
  - apps/backend/pb_migrations/1777334400000_add_schema_fields_39.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "users collection has claimed_recruitment_tier (number, default: 0) field"
    - "users collection has kyc_required_globally (bool, default: false) field"
    - "egg_nfts collection has is_hatching, vrf_request_id, vrf_transaction_hash, is_burned, burned_at fields"
    - "food_nfts collection has is_burned, burned_at fields"
    - "animal_nfts collection has is_burned, burned_at fields"
  artifacts:
    - path: "apps/backend/collections/users.json"
      provides: "Users collection schema with 2 new fields"
      contains: "claimed_recruitment_tier"
    - path: "apps/backend/collections/egg_nfts.json"
      provides: "Egg NFTs collection schema with 5 new fields"
      contains: "is_hatching"
    - path: "apps/backend/collections/food_nfts.json"
      provides: "Food NFTs collection schema with 2 new fields"
      contains: "is_burned"
    - path: "apps/backend/collections/animal_nfts.json"
      provides: "Animal NFTs collection schema with 2 new fields"
      contains: "is_burned"
    - path: "apps/backend/pb_migrations/1777334400000_add_schema_fields_39.js"
      provides: "Migration script for all 10 new fields"
      exports: ["migrate"]
  key_links:
    - from: "apps/backend/pb_migrations/1777334400000_add_schema_fields_39.js"
      to: "apps/backend/collections/*.json"
      via: "migration adds fields matching collection schemas"
      pattern: "collection\\.fields\\.add"
---

<objective>
Add 10 new fields to 4 PocketBase collections to support VRF hatching tracking, NFT burn functionality, KYC toggle, and recruitment tier tracking.

Purpose: These fields are required by Phases 32-36 features (VRF integration, burn functionality, KYC toggle, recruitment bonuses) but were not yet added to the database schema.
Output: Updated collection JSON files + migration script
</objective>

<execution_context>
@/Users/poom-work/tokenine/eggo-pocketbase/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/poom-work/tokenine/eggo-pocketbase/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-collection-schema-updates/39-CONTEXT.md

# Collection schemas (current state)

@apps/backend/collections/users.json
@apps/backend/collections/egg_nfts.json
@apps/backend/collections/food_nfts.json
@apps/backend/collections/animal_nfts.json

# Migration pattern reference

@apps/backend/pb_migrations/21_add_last_bred_at.js

<interfaces>
<!-- Field specifications for all 10 new fields -->

## users collection (2 new fields)

```json
// claimed_recruitment_tier — number, default: 0, tracks which recruitment bonus tier was claimed
{
  "name": "claimed_recruitment_tier",
  "type": "number",
  "required": false,
  "options": { "min": 0, "max": 10, "default": 0 }
}

// kyc_required_globally — bool, default: false, global KYC requirement flag
{
  "name": "kyc_required_globally",
  "type": "bool",
  "required": false,
  "options": { "default": false }
}
```

## egg_nfts collection (5 new fields)

```json
// is_hatching — bool, default: false, egg is waiting for VRF randomness
{
  "name": "is_hatching",
  "type": "bool",
  "required": false,
  "options": { "default": false }
}

// vrf_request_id — text, nullable, Chainlink VRF request ID
{
  "name": "vrf_request_id",
  "type": "text",
  "required": false,
  "options": {}
}

// vrf_transaction_hash — text, nullable, transaction hash for VRF request
{
  "name": "vrf_transaction_hash",
  "type": "text",
  "required": false,
  "options": {}
}

// is_burned — bool, default: false, NFT has been burned
{
  "name": "is_burned",
  "type": "bool",
  "required": false,
  "options": { "default": false }
}

// burned_at — datetime, nullable, when NFT was burned
{
  "name": "burned_at",
  "type": "date",
  "required": false,
  "options": {}
}
```

## food_nfts collection (2 new fields)

```json
// is_burned — bool, default: false
{
  "name": "is_burned",
  "type": "bool",
  "required": false,
  "options": { "default": false }
}

// burned_at — datetime, nullable
{
  "name": "burned_at",
  "type": "date",
  "required": false,
  "options": {}
}
```

## animal_nfts collection (2 new fields)

```json
// is_burned — bool, default: false
{
  "name": "is_burned",
  "type": "bool",
  "required": false,
  "options": { "default": false }
}

// burned_at — datetime, nullable
{
  "name": "burned_at",
  "type": "date",
  "required": false,
  "options": {}
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update collection JSON files with 10 new fields</name>
  <files>
    apps/backend/collections/users.json
    apps/backend/collections/egg_nfts.json
    apps/backend/collections/food_nfts.json
    apps/backend/collections/animal_nfts.json
  </files>
  <action>
Add the following fields to each collection JSON file. Insert new fields BEFORE the autodate fields (created/updated) in the schema array.

**users.json** — Add 2 fields to the `fields` array:

1. `claimed_recruitment_tier` (number): `"hidden": false, "name": "claimed_recruitment_tier", "presentable": false, "required": false, "system": false, "type": "number", "onlyInt": true, "min": 0, "max": 10`
2. `kyc_required_globally` (bool): `"hidden": false, "name": "kyc_required_globally", "presentable": false, "required": false, "system": false, "type": "bool"`

**egg_nfts.json** — Add 5 fields to the `schema` array:

1. `is_hatching` (bool): `"required": false, "type": "bool"`
2. `vrf_request_id` (text): `"required": false, "type": "text"`
3. `vrf_transaction_hash` (text): `"required": false, "type": "text"`
4. `is_burned` (bool): `"required": false, "type": "bool"`
5. `burned_at` (date): `"required": false, "type": "date"`

**food_nfts.json** — Add 2 fields to the `schema` array:

1. `is_burned` (bool): `"required": false, "type": "bool"`
2. `burned_at` (date): `"required": false, "type": "date"`

**animal_nfts.json** — Add 2 fields to the `schema` array:

1. `is_burned` (bool): `"required": false, "type": "bool"`
2. `burned_at` (date): `"required": false, "type": "date"`

Use the Edit tool to insert each field definition. Follow the existing field format exactly — match the JSON structure of existing fields in each file. Do NOT add default values to the JSON schema (PocketBase handles defaults at the database level via migrations).
</action>
<verify>
<automated>node -e "const fs=require('fs'); ['users','egg_nfts','food_nfts','animal_nfts'].forEach(c => { const j=JSON.parse(fs.readFileSync('apps/backend/collections/'+c+'.json')); const schema=j.fields||j.schema||[]; const names=schema.map(f=>f.name); console.log(c+':', names.join(', ')); })"</automated>
</verify>
<done>All 4 collection JSON files contain the 10 new fields with correct types</done>
</task>

<task type="auto">
  <name>Task 2: Create migration script to add fields to production database</name>
  <files>apps/backend/pb_migrations/1777334400000_add_schema_fields_39.js</files>
  <action>
Create a migration file at `apps/backend/pb_migrations/1777334400000_add_schema_fields_39.js` that adds all 10 fields to the 4 collections.

Follow the pattern from `21_add_last_bred_at.js`:

- Use `migrate((app) => { ... }, (app) => { ... })` structure
- Use `app.findCollectionByNameOrId("collection_name")` to get each collection
- Use `collection.fields.add(new Field({ ... }))` to add each field
- Use `app.saveCollection(collection)` to save changes
- Include rollback logic in the second function that removes each field

Field definitions for the migration (use the PocketBase Field constructor format):

**users collection:**

- claimed_recruitment_tier: `{ name: "claimed_recruitment_tier", type: "number", required: false, options: { min: 0, max: 10 } }`
- kyc_required_globally: `{ name: "kyc_required_globally", type: "bool", required: false }`

**egg_nfts collection:**

- is_hatching: `{ name: "is_hatching", type: "bool", required: false }`
- vrf_request_id: `{ name: "vrf_request_id", type: "text", required: false }`
- vrf_transaction_hash: `{ name: "vrf_transaction_hash", type: "text", required: false }`
- is_burned: `{ name: "is_burned", type: "bool", required: false }`
- burned_at: `{ name: "burned_at", type: "date", required: false }`

**food_nfts collection:**

- is_burned: `{ name: "is_burned", type: "bool", required: false }`
- burned_at: `{ name: "burned_at", type: "date", required: false }`

**animal_nfts collection:**

- is_burned: `{ name: "is_burned", type: "bool", required: false }`
- burned_at: `{ name: "burned_at", type: "date", required: false }`

Add a JSDoc reference at the top: `/// <reference path="../pb_data/types.d.ts" />`
</action>
<verify>
<automated>node -e "const fs=require('fs'); const m=fs.readFileSync('apps/backend/pb_migrations/1777334400000_add_schema_fields_39.js','utf8'); const fields=['claimed_recruitment_tier','kyc_required_globally','is_hatching','vrf_request_id','vrf_transaction_hash','is_burned','burned_at']; fields.forEach(f => { if(!m.includes(f)) { console.error('MISSING: '+f); process.exit(1); } }); console.log('All 10 fields present in migration');"</automated>
</verify>
<done>Migration script exists with all 10 fields and rollback logic</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                 |
| -------------------- | ----------------------------------------------------------- |
| Admin UI → Database  | Schema changes require admin access, no user-facing input   |
| Migration → Database | Migration runs with app-level privileges, no external input |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                          |
| --------- | ---------------------- | --------------------------- | ----------- | -------------------------------------------------------------------------------------------------------- |
| T-39-01   | Tampering              | Migration script            | mitigate    | Migration is idempotent — PocketBase handles duplicate field additions gracefully; rollback logic tested |
| T-39-02   | Information Disclosure | Collection JSON files       | accept      | Schema definitions contain no secrets, only field metadata                                               |
| T-39-03   | Elevation of Privilege | kyc_required_globally field | mitigate    | Field is read-only for users, only admin hooks can modify it; access controlled by PocketBase rules      |

</threat_model>

<verification>
- All 4 collection JSON files contain the new fields with correct types
- Migration script adds all 10 fields with proper rollback logic
- JSON files are valid (parseable by Node.js)
- Migration follows established pattern from 21_add_last_bred_at.js
</verification>

<success_criteria>

- users.json has claimed_recruitment_tier (number) and kyc_required_globally (bool) fields
- egg_nfts.json has is_hatching, vrf_request_id, vrf_transaction_hash, is_burned, burned_at fields
- food_nfts.json has is_burned, burned_at fields
- animal_nfts.json has is_burned, burned_at fields
- Migration script exists at pb_migrations/1777334400000_add_schema_fields_39.js
- All JSON files parse without errors
- Migration follows PocketBase v0.23.4 patterns
  </success_criteria>

<output>
After completion, create `.planning/phases/39-collection-schema-updates/39-01-SUMMARY.md`
</output>
