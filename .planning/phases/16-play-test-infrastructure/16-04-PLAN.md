---
phase: 16-play-test-infrastructure
plan: 04
type: execute
wave: 4
depends_on: [16-01-PLAN]
files_created:
  - apps/web/components/dashboard/balance-detail.tsx
files_modified:
  - apps/web/hooks/use-wallet-poll.ts
  - apps/web/components/dashboard/balance-card.tsx
autonomous: true
requirements:
  - FEAT-08
  - FEAT-09
must_haves:
  truths:
    - "use-wallet-poll.ts already has exponential backoff (line 116-119) — verify correctness"
    - "Hot wallet balance endpoint returns total_earned, total_spent, total_withdrawn"
    - "Transactions collection exists for activity feed"
    - "Balance breakdown is collapsible below main balance"
  key_links:
    - from: "apps/backend/pb_hooks/12-hot-wallet-balance.pb.js"
      via: "Response schema: withdrawable, usdt_balance, total_earned, total_spent, total_withdrawn"
    - from: "apps/web/components/dashboard/activity-feed.tsx"
      via: "Reference pattern for transaction list"
---

<objective>
Add balance detail breakdown with transaction history
Purpose: Users can see account breakdown (earned, spent, withdrawn) and last 10 transactions
Output: Balance card shows collapsible detail section with stats and transaction list
</objective>

<execution_context>
@.planning/phases/16-play-test-infrastructure/16-CONTEXT.md
@.planning/phases/16-play-test-infrastructure/tasks.md
</execution_context>

<steps>
1. T16.11: Extend use-wallet-poll.ts
   - Add to WalletBalance interface: `totalEarned`, `totalSpent`, `totalWithdrawn` (all string)
   - Parse from response: `data.total_earned`, `data.total_spent`, `data.total_withdrawn`
   - Verify exponential backoff implementation:
     - Initial: intervalMs (30s)
     - On error: `Math.min(30000 * Math.pow(2, errorCount), 300000)`
     - Reset on success
     - ✅ Already correct — no changes needed for backoff logic itself

2. T16.12: Create BalanceDetail component
   - 4 stat boxes in a 2x2 grid:
     - Withdrawable: balance.usdt
     - Total Earned: balance.totalEarned
     - Total Spent: balance.totalSpent
     - Total Withdrawn: balance.totalWithdrawn
   - Transaction history: fetch last 10 from transactions collection
     - Reuse pattern from ActivityFeed but inline
     - Show: icon (by type), title, relative time, amount
     - Empty state: "No transactions yet"
   - Loading/error states for each section

3. T16.13: Integrate BalanceDetail into BalanceCard
   - Add collapsible section below main balance
   - Use `<details>` HTML element or shadcn Collapsible
   - Default: collapsed, summary says "View details ▼"
     </steps>

<verification>
```bash
# Start dev server
bun run dev

# Navigate to /dashboard

# Expected: Balance card shows "View details" expandable section

# Click: shows 4 stat boxes + transaction list

```
</verification>
```
