---
phase: 16-play-test-infrastructure
plan: 05
type: execute
wave: 5
depends_on: [16-03-PLAN, 16-04-PLAN]
files_created:
  - apps/web/hooks/use-daily-checkin.test.ts
  - apps/web/components/eggs/play-dialog.test.tsx
  - apps/web/components/dashboard/balance-detail.test.tsx
  - apps/web/components/eggs/featured-egg-hero.test.tsx
  - apps/web/components/eggs/feed-dialog.test.tsx
autonomous: true
requirements:
  - QUAL-02
must_haves:
  truths:
    - "Bun test uses bun:test, not vitest"
    - "Use mock.module() or vi.mock() — both work, but avoid vi.mocked()"
    - "Target: 80%+ line coverage across all files"
    - "Colocate tests with source files per project convention"
  key_links:
    - from: "apps/web/components/marketplace/BuyFlow.test.tsx"
      via: "Reference: working mock.module() pattern"
    - from: "apps/web/hooks/use-marketplace-sync.test.ts"
      via: "Reference: working vi.mock() + vi.hoisted() pattern"
---

<objective>
Add test coverage for new and existing untested components
Purpose: Reach 80%+ line coverage, ensure new Play/Balance features are tested
Output: 80%+ coverage, 0 failures, all new components have tests
</objective>

<execution_context>
@.planning/phases/16-play-test-infrastructure/tasks.md
@.planning/phases/16-play-test-infrastructure/16-CONTEXT.md
</execution_context>

<steps>
1. T16.14: Write tests for PlayDialog + use-daily-checkin
   - use-daily-checkin.test.ts (min 4 tests):
     a. getStatus returns can_claim=true when no check-in today
     b. getStatus returns can_claim=false + cooldown when checked in
     c. checkIn() calls POST and returns reward data
     d. checkIn() handles 400 error gracefully
   - play-dialog.test.tsx (min 3 tests):
     a. Renders claim button when eligible
     b. Renders countdown when in cooldown
     c. Claim success calls onSuccess callback

2. T16.15: Write tests for BalanceDetail
   - balance-detail.test.tsx (min 3 tests):
     a. Renders 4 stat boxes with correct values
     b. Renders transaction list
     c. Shows empty state when no transactions

3. T16.16: Write tests for existing untested components
   - featured-egg-hero.test.tsx (min 2 tests):
     a. Renders egg info, progress bar, action buttons
     b. Shows HATCH button when food_count >= 10
   - feed-dialog.test.tsx (min 2 tests):
     a. Shows FEED ME button
     b. Shows confirmation after quick-fill

4. T16.17: Verify coverage
   - Run `bun test` — expect 0 failures
   - Run `bun test --coverage` — expect >= 80% line coverage
   - If below 80%, identify gaps and add tests
     </steps>

<verification>
```bash
cd apps/web && bun test
# Expected: 0 failures, 290+ passed

cd apps/web && bun test --coverage

# Expected: >= 80% line coverage

```
</verification>
```
