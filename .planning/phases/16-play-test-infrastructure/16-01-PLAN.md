---
phase: 16-play-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/referrals/CommissionBreakdown.test.tsx
  - apps/web/components/marketplace/CreateListingDialog.test.tsx
  - apps/web/components/marketplace/BuyFlow.test.tsx
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - "Bun test runner's `vi` does not have `mocked()` — must use direct mock function assignment"
    - "BuyFlow was rewritten to use PocketBase API — no MetaMask approval dialog"
    - "CreateListingDialog has multiple elements matching 'Create Listing' text"
  key_links:
    - from: "apps/web/components/referrals/CommissionBreakdown.test.tsx"
      to: "bun:test vi"
      via: "vi.mocked not supported"
    - from: "apps/web/components/marketplace/BuyFlow.test.tsx"
      to: "BuyFlow.tsx"
      via: "component uses PocketBase API not MetaMask"
---

<objective>
Fix all 14 test failures by addressing vi.mocked incompatibility and dialog text mismatches
Purpose: Zero failing tests before proceeding with new feature work
Output: bun test passes with 0 failures
</objective>

<execution_context>
@.planning/phases/16-play-test-infrastructure/16-CONTEXT.md
@.planning/phases/16-play-test-infrastructure/16-DISCUSSION-LOG.md
@.planning/phases/16-play-test-infrastructure/tasks.md
</execution_context>

<steps>
1. T16.01: Fix CommissionBreakdown.test.tsx — replace all `vi.mocked(...)` with direct mock assignments
   - Replace `vi.mocked(createClient().collection('').getList).mockImplementation(mockGetList)`
   - With: assign `mockGetList` directly in the mock factory
   - The mock factory already returns `getList: vi.fn()` — use that reference directly

2. T16.02: Fix CreateListingDialog.test.tsx — replace vi.mocked and fix ambiguous selector
   - Replace `vi.mocked(eggNft.getSigner)` and `vi.mocked(marketplace.*)` with direct mock assertions
   - Use `import * as marketplace from '@/lib/contracts/marketplace'` — the mock replaces module exports
   - Fix button selector: use `screen.getAllByRole('button', { name: /Create Listing/i })[0]`

3. T16.03: Fix BuyFlow.test.tsx — update dialog text assertions
   - Remove: Thai MetaMask approval text checks (/อนุญาตให้ marketplace/, /กรุณายืนยันธุรกรรม/)
   - Replace with: "Confirm Purchase" heading, item name ("Golden Chicken #42"), price ("100.00 USDT"), Cancel/Confirm buttons

4. T16.04: Verify all tests pass
   - Run `bun test` → expect 0 failures
     </steps>

<verification>
```bash
cd apps/web && bun test
# Expected: 278 tests, 0 fail
cd apps/web && bun test --coverage
# Expected: coverage >= 70% (baseline)
```
</verification>
