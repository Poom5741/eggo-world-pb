---
phase: 16-play-test-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [16-02-PLAN]
files_created:
  - apps/web/hooks/use-daily-checkin.ts
  - apps/web/components/eggs/play-dialog.tsx
files_modified:
  - apps/web/app/eggs/page.tsx
autonomous: true
requirements:
  - FEAT-05
  - FEAT-06
  - FEAT-07
must_haves:
  truths:
    - "Play = daily check-in, not mini-game"
    - "PlayDialog opens on PLAY button click in FeaturedEggHero or EggCard"
    - "24h cooldown calculated by comparing last_check_in with current time"
    - "Countdown timer must tick every second"
    - "Food NFT reward is off-chain (PocketBase record only)"
  key_links:
    - from: "apps/web/components/eggs/feed-dialog.tsx"
      via: "Reference pattern for dialog component"
    - from: "apps/web/app/eggs/page.tsx"
      via: "handlePlayEgg needs wiring"
---

<objective>
Create daily check-in frontend (PlayDialog + hook)
Purpose: Users can click PLAY, see check-in dialog, claim daily Food NFT reward
Output: Working play flow from button click to reward claimed
</objective>

<execution_context>
@.planning/phases/16-play-test-infrastructure/16-CONTEXT.md
@.planning/phases/16-play-test-infrastructure/tasks.md
</execution_context>

<steps>
1. T16.08: Create use-daily-checkin.ts hook
   - Functions: checkIn(), getStatus()
   - State: canClaim, cooldownMs, streakCount, lastCheckIn, reward, bonus
   - Use fetch to POST/GET /api/v2/play/check-in via PocketBase
   - Countdown timer: useEffect with setInterval(1000), decrement cooldownMs
   - Auto-fetch status on mount
   - Handle hydration (useIsHydrated)

2. T16.09: Create PlayDialog component
   - Two display states based on `canClaim`:
     a. Eligible: "Day N Streak!" banner, egg icon, "1 Food NFT" reward preview,
     streak bonus preview at 7/30 days, "Claim Reward" button
     b. Cooldown: "Come back in HH:MM:SS" countdown, current streak info,
     next bonus preview
   - Loading state during claim
   - Error state with retry
   - Toast on success ("Daily reward claimed! +1 Food NFT")

3. T16.10: Wire play button in eggs page
   - Update handlePlayEgg in page.tsx to open PlayDialog
   - Add PlayDialog import and state management (similar to FeedDialog pattern)
   - onSuccess: refresh egg data (for food count update if feeding was part of flow)
   - No changes needed to EggCard/FeaturedEggHero — they already pass onPlay
     </steps>

<verification>
```bash
# Start dev server
bun run dev

# Manual test:

# 1. Navigate to /eggs

# 2. Click PLAY on featured egg or any egg card

# 3. Verify dialog opens with "Claim Reward" or countdown

# 4. Click "Claim Reward" — verify toast appears

# 5. Click PLAY again — verify cooldown timer shows

```
</verification>
```
