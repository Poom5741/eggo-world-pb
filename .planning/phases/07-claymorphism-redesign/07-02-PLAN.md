---
phase: 07-claymorphism-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/app/globals.css]
autonomous: true
requirements: [CLAY-01, CLAY-02]
user_setup: []

must_haves:
  truths:
    - "Claymorphism shadow tokens are available as CSS variables"
    - "Claymorphism border radius tokens are available as CSS variables"
    - "Claymorphism color extensions are available for highlights/shadows"
    - "Dark mode variants exist for all claymorphism tokens"
  artifacts:
    - path: "apps/web/app/globals.css"
      provides: "Complete claymorphism design token system"
      contains: ["--clay-sm-outer", "--clay-md-outer", "--radius-clay-sm", "@theme inline"]
  key_links:
    - from: "apps/web/app/globals.css"
      to: "apps/web/components/ui/*"
      via: "CSS variable references"
      pattern: "var\\(--clay-"
---

<objective>
Establish the claymorphism design token foundation by updating globals.css with complete shadow system, border radius scale, color extensions, and Tailwind integration.

Purpose: Provides the CSS variable infrastructure that all subsequent claymorphism components will depend on.
Output: Production-ready globals.css with claymorphism tokens fully integrated alongside existing design system.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-claymorphism-redesign/DESIGN-TOKENS.md
@.planning/phases/07-claymorphism-redesign/07-01-SUMMARY.md
@apps/web/app/globals.css
</context>

<interfaces>
<!-- Current CSS variable structure in globals.css -->
<!-- Executor must preserve existing variables while adding claymorphism extensions -->

Current :root variables:

```css
--background: #1a1a2e;
--foreground: #fef9c3;
--card: #16213e;
--primary: #facc15;
--secondary: #0f3460;
--accent: #e94560;
--radius: 0px;
```

Current @theme inline exports:

```css
--color-background: var(--background);
--color-primary: var(--primary);
--radius-sm: calc(var(--radius) - 4px);
--radius-md: calc(var(--radius) - 2px);
--radius-lg: var(--radius);
--radius-xl: calc(var(--radius) + 4px);
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add claymorphism shadow CSS variables to :root and .dark</name>
  <files>apps/web/app/globals.css</files>
  <action>
Add claymorphism shadow variables to both :root and .dark selectors in globals.css.

In :root selector (after existing variables, before @theme inline):

```css
/* CLAYMORPHISM SHADOWS - Light Mode */
--clay-sm-outer: 2px 2px 4px rgba(0, 0, 0, 0.15), -2px -2px 4px rgba(255, 255, 255, 0.05);
--clay-sm-inner:
  inset 2px 2px 4px rgba(0, 0, 0, 0.1), inset -2px -2px 4px rgba(255, 255, 255, 0.05);
--clay-sm: var(--clay-sm-outer), var(--clay-sm-inner);

--clay-md-outer: 4px 4px 8px rgba(0, 0, 0, 0.2), -4px -4px 8px rgba(255, 255, 255, 0.08);
--clay-md-inner:
  inset 2px 2px 4px rgba(0, 0, 0, 0.12), inset -2px -2px 4px rgba(255, 255, 255, 0.08);
--clay-md: var(--clay-md-outer), var(--clay-md-inner);

--clay-lg-outer: 6px 6px 12px rgba(0, 0, 0, 0.25), -6px -6px 12px rgba(255, 255, 255, 0.1);
--clay-lg-inner:
  inset 3px 3px 6px rgba(0, 0, 0, 0.15), inset -3px -3px 6px rgba(255, 255, 255, 0.1);
--clay-lg: var(--clay-lg-outer), var(--clay-lg-inner);

--clay-xl-outer: 8px 8px 16px rgba(0, 0, 0, 0.3), -8px -8px 16px rgba(255, 255, 255, 0.12);
--clay-xl-inner:
  inset 4px 4px 8px rgba(0, 0, 0, 0.18), inset -4px -4px 8px rgba(255, 255, 255, 0.12);
--clay-xl: var(--clay-xl-outer), var(--clay-xl-inner);

--clay-2xl-outer: 12px 12px 24px rgba(0, 0, 0, 0.35), -12px -12px 24px rgba(255, 255, 255, 0.15);
--clay-2xl-inner:
  inset 6px 6px 12px rgba(0, 0, 0, 0.2), inset -6px -6px 12px rgba(255, 255, 255, 0.15);
--clay-2xl: var(--clay-2xl-outer), var(--clay-2xl-inner);
```

In .dark selector (replace with adjusted opacity for dark backgrounds):

```css
/* CLAYMORPHISM SHADOWS - Dark Mode */
--clay-sm-outer: 3px 3px 6px rgba(0, 0, 0, 0.4), -3px -3px 6px rgba(255, 255, 255, 0.03);
--clay-sm-inner:
  inset 2px 2px 4px rgba(0, 0, 0, 0.3), inset -2px -2px 4px rgba(255, 255, 255, 0.03);
--clay-sm: var(--clay-sm-outer), var(--clay-sm-inner);

--clay-md-outer: 5px 5px 10px rgba(0, 0, 0, 0.5), -5px -5px 10px rgba(255, 255, 255, 0.05);
--clay-md-inner:
  inset 3px 3px 6px rgba(0, 0, 0, 0.4), inset -3px -3px 6px rgba(255, 255, 255, 0.05);
--clay-md: var(--clay-md-outer), var(--clay-md-inner);

--clay-lg-outer: 8px 8px 16px rgba(0, 0, 0, 0.6), -8px -8px 16px rgba(255, 255, 255, 0.08);
--clay-lg-inner:
  inset 4px 4px 8px rgba(0, 0, 0, 0.5), inset -4px -4px 8px rgba(255, 255, 255, 0.08);
--clay-lg: var(--clay-lg-outer), var(--clay-lg-inner);

--clay-xl-outer: 10px 10px 20px rgba(0, 0, 0, 0.7), -10px -10px 20px rgba(255, 255, 255, 0.1);
--clay-xl-inner:
  inset 5px 5px 10px rgba(0, 0, 0, 0.6), inset -5px -5px 10px rgba(255, 255, 255, 0.1);
--clay-xl: var(--clay-xl-outer), var(--clay-xl-inner);

--clay-2xl-outer: 14px 14px 28px rgba(0, 0, 0, 0.8), -14px -14px 28px rgba(255, 255, 255, 0.12);
--clay-2xl-inner:
  inset 7px 7px 14px rgba(0, 0, 0, 0.7), inset -7px -7px 14px rgba(255, 255, 255, 0.12);
--clay-2xl: var(--clay-2xl-outer), var(--clay-2xl-inner);
```

Do NOT remove existing variables. Add claymorphism variables after the existing color variables in each selector.
</action>
<verify>
<automated>grep -c "--clay-md-outer" apps/web/app/globals.css</automated>
<expected>2 (one in :root, one in .dark)</expected>
</verify>
<done>Both :root and .dark selectors contain all 5 claymorphism shadow presets (sm, md, lg, xl, 2xl) with outer/inner variants</done>
</task>

<task type="auto">
  <name>Task 2: Add claymorphism border radius and spacing tokens</name>
  <files>apps/web/app/globals.css</files>
  <action>
Add border radius and spacing tokens to both :root and .dark selectors.

In :root selector (after shadow variables):

```css
/* CLAYMORPHISM BORDER RADIUS */
--clay-rounded-sm: 16px;
--clay-rounded: 20px;
--clay-rounded-md: 24px;
--clay-rounded-lg: 32px;
--clay-rounded-xl: 40px;
--clay-rounded-full: 9999px;

/* CLAYMORPHISM SPACING */
--clay-padding-sm: 0.75rem;
--clay-padding-md: 1rem;
--clay-padding-lg: 1.5rem;
--clay-padding-xl: 2rem;
--clay-padding-2xl: 3rem;

--clay-gap-sm: 0.5rem;
--clay-gap-md: 1rem;
--clay-gap-lg: 1.5rem;
--clay-gap-xl: 2rem;

--clay-margin-sm: 0.5rem;
--clay-margin-md: 1rem;
--clay-margin-lg: 1.5rem;
--clay-margin-xl: 2rem;
```

Update existing radius variables to use claymorphism values (replaces current 0px system):

```css
/* Replace current --radius: 0px system */
--radius: var(--clay-rounded); /* Was: 0px, now: 20px */
--radius-sm: var(--clay-rounded-sm); /* Was: calc(var(--radius) - 4px), now: 16px */
--radius-md: var(--clay-rounded); /* Was: calc(var(--radius) - 2px), now: 20px */
--radius-lg: var(--clay-rounded-md); /* Was: var(--radius), now: 24px */
--radius-xl: var(--clay-rounded-lg); /* Was: calc(var(--radius) + 4px), now: 32px */
```

Same variables in .dark selector (identical values - radius doesn't change with theme).
</action>
<verify>
<automated>grep -c "--clay-rounded:" apps/web/app/globals.css</automated>
<expected>2 (one in :root, one in .dark)</expected>
</verify>
<done>Border radius scale defined with 6 values (sm, base, md, lg, xl, full) and standard radius variables updated to use claymorphism values</done>
</task>

<task type="auto">
  <name>Task 3: Add claymorphism color extensions and gradients</name>
  <files>apps/web/app/globals.css</files>
  <action>
Add extended color palettes for claymorphism volume effects.

In :root selector (after spacing tokens):

```css
/* CLAYMORPHISM COLOR EXTENSIONS */
/* Primary Yellow (#facc15) Extensions */
--clay-primary-highlight: rgba(254, 249, 195, 0.3);
--clay-primary-shadow: rgba(234, 179, 8, 0.4);

/* Secondary Blue (#0f3460) Extensions */
--clay-secondary-highlight: rgba(199, 210, 254, 0.2);
--clay-secondary-shadow: rgba(30, 41, 59, 0.5);

/* Card Blue (#16213e) Extensions */
--clay-card-highlight: rgba(255, 255, 255, 0.08);
--clay-card-shadow: rgba(0, 0, 0, 0.3);

/* Background Navy (#1a1a2e) Extensions */
--clay-bg-highlight: rgba(255, 255, 255, 0.05);
--clay-bg-shadow: rgba(0, 0, 0, 0.4);

/* Accent Red (#e94560) Extensions */
--clay-accent-highlight: rgba(254, 205, 211, 0.25);
--clay-accent-shadow: rgba(190, 18, 60, 0.4);

/* CLAYMORPHISM GRADIENTS */
--gradient-clay-light: linear-gradient(
  135deg,
  rgba(255, 255, 255, 0.15) 0%,
  transparent 50%,
  rgba(0, 0, 0, 0.1) 100%
);
--gradient-clay-highlight: linear-gradient(
  180deg,
  rgba(255, 255, 255, 0.2) 0%,
  transparent 40%,
  transparent 60%,
  rgba(0, 0, 0, 0.15) 100%
);
--gradient-clay-sheen: linear-gradient(
  45deg,
  transparent 40%,
  rgba(255, 255, 255, 0.05) 50%,
  transparent 60%
);
--gradient-clay-surface: linear-gradient(
  145deg,
  rgba(255, 255, 255, 0.08) 0%,
  transparent 50%,
  rgba(0, 0, 0, 0.12) 100%
);
```

In .dark selector (adjust opacity for visibility):

```css
/* CLAYMORPHISM COLOR EXTENSIONS - Dark Mode */
--clay-primary-highlight: rgba(254, 249, 195, 0.2);
--clay-primary-shadow: rgba(234, 179, 8, 0.5);
--clay-secondary-highlight: rgba(199, 210, 254, 0.15);
--clay-secondary-shadow: rgba(30, 41, 59, 0.6);
--clay-card-highlight: rgba(255, 255, 255, 0.06);
--clay-card-shadow: rgba(0, 0, 0, 0.4);
--clay-bg-highlight: rgba(255, 255, 255, 0.03);
--clay-bg-shadow: rgba(0, 0, 0, 0.5);
--clay-accent-highlight: rgba(254, 205, 211, 0.2);
--clay-accent-shadow: rgba(190, 18, 60, 0.5);

/* Gradients (same as light mode, work with dark backgrounds) */
--gradient-clay-light: linear-gradient(
  135deg,
  rgba(255, 255, 255, 0.15) 0%,
  transparent 50%,
  rgba(0, 0, 0, 0.1) 100%
);
--gradient-clay-highlight: linear-gradient(
  180deg,
  rgba(255, 255, 255, 0.2) 0%,
  transparent 40%,
  transparent 60%,
  rgba(0, 0, 0, 0.15) 100%
);
--gradient-clay-sheen: linear-gradient(
  45deg,
  transparent 40%,
  rgba(255, 255, 255, 0.05) 50%,
  transparent 60%
);
--gradient-clay-surface: linear-gradient(
  145deg,
  rgba(255, 255, 255, 0.08) 0%,
  transparent 50%,
  rgba(0, 0, 0, 0.12) 100%
);
```

  </action>
  <verify>
    <automated>grep -c "--gradient-clay-surface" apps/web/app/globals.css</automated>
    <expected>2 (one in :root, one in .dark)</expected>
  </verify>
  <done>Color extensions and gradients defined for all base colors with light/dark mode variants</done>
</task>

<task type="auto">
  <name>Task 4: Update @theme inline block with Tailwind extensions</name>
  <files>apps/web/app/globals.css</files>
  <action>
Extend the existing @theme inline block to export claymorphism tokens to Tailwind.

Find the existing @theme inline block and add these exports (after existing color exports, before closing brace):

```css
/* Claymorphism Shadow Extensions */
--shadow-clay-sm: var(--clay-sm);
--shadow-clay-md: var(--clay-md);
--shadow-clay-lg: var(--clay-lg);
--shadow-clay-xl: var(--clay-xl);
--shadow-clay-2xl: var(--clay-2xl);

/* Individual shadow components for layering */
--shadow-clay-sm-outer: var(--clay-sm-outer);
--shadow-clay-sm-inner: var(--clay-sm-inner);
--shadow-clay-md-outer: var(--clay-md-outer);
--shadow-clay-md-inner: var(--clay-md-inner);
--shadow-clay-lg-outer: var(--clay-lg-outer);
--shadow-clay-lg-inner: var(--clay-lg-inner);
--shadow-clay-xl-outer: var(--clay-xl-outer);
--shadow-clay-xl-inner: var(--clay-xl-inner);
--shadow-clay-2xl-outer: var(--clay-2xl-outer);
--shadow-clay-2xl-inner: var(--clay-2xl-inner);

/* Claymorphism Border Radius */
--radius-clay-sm: var(--clay-rounded-sm);
--radius-clay: var(--clay-rounded);
--radius-clay-md: var(--clay-rounded-md);
--radius-clay-lg: var(--clay-rounded-lg);
--radius-clay-xl: var(--clay-rounded-xl);
--radius-clay-full: var(--clay-rounded-full);

/* Claymorphism Color Extensions */
--color-clay-primary-highlight: var(--clay-primary-highlight);
--color-clay-primary-shadow: var(--clay-primary-shadow);
--color-clay-secondary-highlight: var(--clay-secondary-highlight);
--color-clay-secondary-shadow: var(--clay-secondary-shadow);
--color-clay-card-highlight: var(--clay-card-highlight);
--color-clay-card-shadow: var(--clay-card-shadow);
--color-clay-bg-highlight: var(--clay-bg-highlight);
--color-clay-bg-shadow: var(--clay-bg-shadow);

/* Claymorphism Spacing */
--spacing-clay-sm: var(--clay-padding-sm);
--spacing-clay-md: var(--clay-padding-md);
--spacing-clay-lg: var(--clay-padding-lg);
--spacing-clay-xl: var(--clay-padding-xl);
--spacing-clay-2xl: var(--clay-padding-2xl);
```

DO NOT remove existing @theme exports. Only add new claymorphism exports.
</action>
<verify>
<automated>grep -c "--shadow-clay-lg:" apps/web/app/globals.css</automated>
<expected>1 (in @theme inline block)</expected>
</verify>
<done>@theme inline block exports all claymorphism tokens (shadows, radius, colors, spacing) for Tailwind utility class generation</done>
</task>

<task type="auto">
  <name>Task 5: Add claymorphism utility classes</name>
  <files>apps/web/app/globals.css</files>
  <action>
Add utility classes at the end of globals.css (after existing animation classes, before closing).

Add these classes after the existing component classes (after .divider):

```css
/* ============================================
   CLAYMORPHISM UTILITY CLASSES
   ============================================ */

/* Clay Shadow Utilities */
.clay-shadow-sm {
  box-shadow: var(--clay-sm);
}
.clay-shadow-md {
  box-shadow: var(--clay-md);
}
.clay-shadow-lg {
  box-shadow: var(--clay-lg);
}
.clay-shadow-xl {
  box-shadow: var(--clay-xl);
}
.clay-shadow-2xl {
  box-shadow: var(--clay-2xl);
}

/* Clay Radius Utilities */
.clay-rounded-sm {
  border-radius: var(--clay-rounded-sm);
}
.clay-rounded {
  border-radius: var(--clay-rounded);
}
.clay-rounded-md {
  border-radius: var(--clay-rounded-md);
}
.clay-rounded-lg {
  border-radius: var(--clay-rounded-lg);
}
.clay-rounded-xl {
  border-radius: var(--clay-rounded-xl);
}
.clay-rounded-full {
  border-radius: var(--clay-rounded-full);
}

/* Clay Gradient Utilities */
.clay-gradient-light {
  background: var(--gradient-clay-light);
}
.clay-gradient-highlight {
  background: var(--gradient-clay-highlight);
}
.clay-gradient-sheen {
  background: var(--gradient-clay-sheen);
}
.clay-gradient-surface {
  background: var(--gradient-clay-surface);
}

/* Clay Spacing Utilities */
.clay-padding-sm {
  padding: var(--clay-padding-sm);
}
.clay-padding-md {
  padding: var(--clay-padding-md);
}
.clay-padding-lg {
  padding: var(--clay-padding-lg);
}
.clay-padding-xl {
  padding: var(--clay-padding-xl);
}
.clay-padding-2xl {
  padding: var(--clay-padding-2xl);
}

/* Clay Component Base Classes */
.clay-card {
  @apply bg-card text-card-foreground;
  border-radius: var(--clay-rounded-md);
  box-shadow: var(--clay-lg);
  padding: var(--clay-padding-lg);
}

.clay-button {
  @apply font-[var(--font-pixel)] text-xs font-medium transition-all duration-200;
  border-radius: var(--clay-rounded);
  box-shadow: var(--clay-md);
  padding: var(--clay-padding-md) calc(var(--clay-padding-md) * 2);
}

.clay-button:hover {
  box-shadow: var(--clay-lg);
}

.clay-button:active {
  box-shadow: var(--clay-sm);
}

.clay-input {
  @apply bg-background text-foreground placeholder:text-muted-foreground;
  border-radius: var(--clay-rounded);
  box-shadow: var(--clay-sm);
  padding: var(--clay-padding-md) var(--clay-padding-lg);
  border: 1px solid var(--border);
  transition: box-shadow 0.2s ease;
}

.clay-input:focus {
  @apply border-primary outline-none;
  box-shadow: var(--clay-md);
}

.clay-badge {
  @apply inline-flex items-center font-[var(--font-pixel)] text-[10px];
  border-radius: var(--clay-rounded-sm);
  box-shadow: var(--clay-sm);
  padding: 4px 10px;
}
```

  </action>
  <verify>
    <automated>grep -c "\.clay-button" apps/web/app/globals.css</automated>
    <expected>3 (.clay-button, .clay-button:hover, .clay-button:active)</expected>
  </verify>
  <done>Utility classes added for shadows, radius, gradients, spacing, and base component styles (card, button, input, badge)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify globals.css claymorphism tokens</name>
  <what-built>
Complete claymorphism design token system in globals.css:
- Shadow variables (sm, md, lg, xl, 2xl) in :root and .dark
- Border radius scale (sm, base, md, lg, xl, full)
- Color extensions for highlights and shadows
- Gradient utilities for volume effects
- Tailwind @theme inline exports
- Utility classes for rapid development
  </what-built>
  <how-to-verify>
1. Open apps/web/app/globals.css in your editor
2. Verify :root selector contains all claymorphism variables (search for "--clay-")
3. Verify .dark selector contains dark mode variants
4. Verify @theme inline block exports all tokens
5. Verify utility classes exist at end of file
6. Run dev server: `cd apps/web && bun run dev`
7. Open browser to http://localhost:3000
8. Open DevTools → Elements → inspect body
9. In Styles panel, verify CSS variables are loaded (search for "--clay-md")
10. Create test element in browser console:
    ```javascript
    const test = document.createElement('div');
    test.className = 'clay-button clay-shadow-md';
    test.textContent = 'Test';
    document.body.appendChild(test);
    ```
11. Verify test button shows claymorphism styling (rounded corners, shadows)
  </how-to-verify>
  <resume-signal>Type "approved" if all tokens are present and test button shows claymorphism styling, or describe any missing variables/issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                           |
| ---------------------- | ------------------------------------------------------------------------------------- |
| CSS variable injection | Untrusted input could theoretically inject malicious CSS (mitigated by static values) |

## STRIDE Threat Register

| Threat ID  | Category  | Component     | Disposition | Mitigation Plan                                                               |
| ---------- | --------- | ------------- | ----------- | ----------------------------------------------------------------------------- |
| T-07-02-01 | Injection | globals.css   | mitigate    | Use only static CSS variable values, no dynamic interpolation from user input |
| T-07-02-02 | Tampering | Design tokens | accept      | CSS variables are build-time constants, low-value target                      |

</threat_model>

<verification>
## File-Level Verification

1. **CSS Variable Count:**

   ```bash
   grep -c "--clay-" apps/web/app/globals.css
   ```

   Expected: 60+ occurrences (5 shadow sizes × 3 variants × 2 themes + radius + colors + gradients)

2. **@theme Exports:**

   ```bash
   grep -c "--shadow-clay" apps/web/app/globals.css
   ```

   Expected: 15+ exports in @theme block

3. **Utility Classes:**
   ```bash
   grep -c "\.clay-" apps/web/app/globals.css
   ```
   Expected: 20+ utility classes

## Functional Verification

1. Start dev server: `cd apps/web && bun run dev`
2. No CSS syntax errors in console
3. CSS variables accessible in browser DevTools
4. Test element with `.clay-button` shows rounded corners and shadows
   </verification>

<success_criteria>

- [x] All 5 claymorphism shadow presets defined (sm, md, lg, xl, 2xl)
- [x] Both outer and inner shadow variants for each preset
- [x] Light mode (:root) and dark mode (.dark) variants for all shadows
- [x] Border radius scale with 6 values (sm, base, md, lg, xl, full)
- [x] Standard radius variables (--radius, --radius-sm, etc.) updated to use clay values
- [x] Color extensions for primary, secondary, card, bg, accent colors
- [x] Gradient utilities for volume effects (light, highlight, sheen, surface)
- [x] @theme inline block exports all claymorphism tokens
- [x] Utility classes for shadows, radius, gradients, spacing
- [x] Base component classes (clay-card, clay-button, clay-input, clay-badge)
- [x] Dev server runs without CSS errors
- [x] CSS variables accessible in browser DevTools
      </success_criteria>

<output>
After completion, create `.planning/phases/07-claymorphism-redesign/07-02-SUMMARY.md` documenting:
- Token counts (shadows, radius, colors, gradients)
- Any deviations from DESIGN-TOKENS.md specification
- Browser compatibility notes
- Ready status for Plan 03 (UI Primitives - Part 1)
</output>
