---
phase: 07-claymorphism-redesign
plan: 06
type: execute
wave: 4
depends_on: ["07-02", "07-03", "07-04", "07-05"]
files_modified:
  [
    apps/web/app/page.tsx,
    apps/web/app/dashboard/page.tsx,
    apps/web/app/auth/login/page.tsx,
    apps/web/app/auth/sign-up/page.tsx,
    apps/web/app/auth/callback/page.tsx,
    apps/web/app/auth/error/page.tsx,
    apps/web/app/dashboard/eggs/page.tsx,
    apps/web/app/dashboard/referrals/page.tsx,
    apps/web/app/wallet/page.tsx,
  ]
autonomous: true
requirements: [CLAY-09, CLAY-10]
user_setup: []

must_haves:
  truths:
    - "Landing page displays with clay hero, pricing cards, and features"
    - "Dashboard shows clay widgets for stats and NFT grids"
    - "Auth pages have clay forms with proper input/button styling"
    - "Eggs page displays EggCards in clay grid layout"
    - "Referrals page shows clay referral chain hierarchy"
    - "Wallet page displays clay balance widgets"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "Landing page with clay hero, pricing, features"
      contains: ["shadow-clay-2xl", "rounded-clay-xl", "clay-gradient"]
    - path: "apps/web/app/dashboard/page.tsx"
      provides: "Dashboard with clay stat widgets and NFT grid"
      contains: ['Card variant="clay"', "clay-padding-xl"]
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/components/ui/card.tsx"
      via: "Card component usage"
      pattern: 'Card variant="clay'
---

<objective>
Apply claymorphism styling to 6 core pages (landing, dashboard, auth pages, eggs, referrals, wallet) using updated UI primitives and NFT components.

Purpose: Transforms the main user-facing pages to showcase claymorphism aesthetic at full scale.
Output: Six redesigned pages with consistent claymorphism styling throughout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-claymorphism-redesign/DESIGN-TOKENS.md
@.planning/phases/07-claymorphism-redesign/PIXEL-ART-COMPATIBILITY.md
@.planning/phases/07-claymorphism-redesign/07-03-SUMMARY.md
@.planning/phases/07-claymorphism-redesign/07-04-SUMMARY.md
@.planning/phases/07-claymorphism-redesign/07-05-SUMMARY.md
@apps/web/app/page.tsx
@apps/web/app/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update landing page (page.tsx) hero section</name>
  <files>apps/web/app/page.tsx</files>
  <action>
Read page.tsx and update the hero section with claymorphism styling.

Update hero CTA button:

```typescript
// Hero CTA button
<Button variant="clay" size="clay-xl" className="px-clay-2xl">
  Start Collecting
</Button>
```

Update hero featured NFT display:

```typescript
// Hero floating NFT (egg sprite in clay frame)
<div className={cn(
  'relative mx-auto w-64 h-64',
  'rounded-clay-xl shadow-clay-2xl',
  'bg-gradient-clay-surface',
  'animate-float' // Keep existing float animation
)}>
  <img
    src="/egg-sprite.png"
    alt="Featured Egg"
    className="pixelated w-full h-full object-cover"
  />
</div>
```

Update hero background (optional gradient for volume):

```typescript
// Hero background section
<section className={cn(
  'relative min-h-screen flex items-center justify-center',
  'bg-gradient-to-b from-background via-background to-card/20',
  'overflow-hidden'
)}>
  {/* Clay gradient orbs for depth */}
  <div className="absolute top-20 left-20 w-96 h-96 bg-primary/5 rounded-clay-full blur-3xl" />
  <div className="absolute bottom-20 right-20 w-96 h-96 bg-accent/5 rounded-clay-full blur-3xl" />
</section>
```

Key points:

- Hero CTA uses `clay-xl` size for maximum impact
- Featured NFT has `clay-xl` radius (40px) and `clay-2xl` shadow
- Maintain float animation (works with clay aesthetic)
- Background gradient orbs add depth
  </action>
  <verify>
  <automated>grep "shadow-clay-2xl\|variant=\"clay\"" apps/web/app/page.tsx | head -5</automated>
  <expected>Hero section should have clay-2xl shadow and clay button</expected>
  </verify>
  <done>Landing page hero displays with clay CTA button (xl size), featured NFT in clay frame (xl radius, 2xl shadow), and gradient depth effects</done>
  </task>

<task type="auto">
  <name>Task 2: Update landing page pricing and features sections</name>
  <files>apps/web/app/page.tsx</files>
  <action>
Update pricing cards and feature sections with clay styling.

Pricing cards:

```typescript
// Pricing card container
<Card variant="clay-lg" className={cn(
  'flex flex-col gap-clay-lg',
  'hover:shadow-clay-xl transition-shadow duration-300',
  'bg-gradient-to-br from-card to-card/80'
)}>
  <CardHeader>
    <CardTitle className="font-[var(--font-pixel)] text-sm">
      Starter Egg
    </CardTitle>
  </CardHeader>
  <CardContent>
    <div className="flex flex-col gap-clay-md">
      {/* Price with clay styling */}
      <p className="text-4xl font-bold text-primary">
        $25
      </p>
      {/* Features list */}
      <ul className="space-y-clay-sm">
        <li className="flex items-center gap-2">
          <Check className="w-4 h-4 text-primary" />
          <span>2 Food NFTs included</span>
        </li>
        {/* More features... */}
      </ul>
    </div>
  </CardContent>
  <CardFooter>
    <Button variant="clay" size="clay-lg" className="w-full">
      Purchase Now
    </Button>
  </CardFooter>
</Card>
```

Feature section icons:

```typescript
// Feature icon container
<div className={cn(
  'w-16 h-16 rounded-clay-lg',
  'bg-primary/10 shadow-clay-md',
  'flex items-center justify-center'
)}>
  <Icon className="w-8 h-8 text-primary" />
</div>
```

Key points:

- Pricing cards use `clay-lg` variant for featured depth
- Feature icons have clay containers with shadows
- Typography keeps Press Start 2P for headings
- Buttons use clay variants
  </action>
  <verify>
  <automated>grep "variant=\"clay-lg\"" apps/web/app/page.tsx</automated>
  <expected>Pricing cards should use clay-lg variant</expected>
  </verify>
  <done>Landing page pricing and features sections use clay cards, icons, and buttons with consistent styling</done>
  </task>

<task type="auto">
  <name>Task 3: Update dashboard main page</name>
  <files>apps/web/app/dashboard/page.tsx</files>
  <action>
Read dashboard/page.tsx and update with clay widgets.

Stat cards:

```typescript
// Balance card
<Card variant="clay-xl" className={cn(
  'bg-gradient-to-br from-primary/20 to-primary/5',
  'shadow-clay-xl'
)}>
  <CardHeader>
    <CardTitle className="font-[var(--font-pixel)] text-xs text-primary">
      Total Balance
    </CardTitle>
  </CardHeader>
  <CardContent>
    <p className="text-4xl font-bold text-primary">
      {balance} USDT
    </p>
  </CardContent>
</Card>

// NFT count card
<Card variant="clay" className="shadow-clay-lg">
  <CardHeader>
    <CardTitle className="font-[var(--font-pixel)] text-xs">
      NFT Collection
    </CardTitle>
  </CardHeader>
  <CardContent>
    <p className="text-2xl font-bold">{nftCount}</p>
    <p className="text-muted-foreground text-sm">
      {eggs} Eggs • {food} Food • {animals} Animals
    </p>
  </CardContent>
</Card>
```

NFT grid:

```typescript
// NFT grid container
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-clay-xl">
  {eggs.map(egg => (
    <EggCard key={egg.id} egg={egg} />
  ))}
</div>
```

Key points:

- Balance card uses `clay-xl` for prominence
- Stat cards use `clay` variant with enhanced shadows
- NFT grid uses `gap-clay-xl` for spacing
- EggCard components already clay-styled from Plan 05
  </action>
  <verify>
  <automated>grep "variant=\"clay" apps/web/app/dashboard/page.tsx | wc -l</automated>
  <expected>Multiple clay card variants for stat widgets</expected>
  </verify>
  <done>Dashboard displays with clay stat widgets (xl variant for balance), clay NFT grid, and consistent spacing</done>
  </task>

<task type="auto">
  <name>Task 4: Update auth pages (login, signup, callback, error)</name>
  <files>apps/web/app/auth/login/page.tsx, apps/web/app/auth/sign-up/page.tsx, apps/web/app/auth/callback/page.tsx, apps/web/app/auth/error/page.tsx</files>
  <action>
Update all 4 auth pages with clay form styling.

Login/Signup page form container:

```typescript
// Auth form card
<Card variant="clay-lg" className="w-full max-w-md mx-auto shadow-clay-xl">
  <CardHeader>
    <CardTitle className="font-[var(--font-pixel)] text-center text-base">
      Welcome to EggoWorld
    </CardTitle>
    <CardDescription className="text-center">
      Sign in with LINE to start collecting
    </CardDescription>
  </CardHeader>
  <CardContent className="flex flex-col gap-clay-lg">
    {/* Email input with clay styling */}
    <div className="flex flex-col gap-clay-sm">
      <Label htmlFor="email">Email</Label>
      <Input
        id="email"
        type="email"
        placeholder="your@email.com"
        className="clay-input"
      />
    </div>

    {/* Password input */}
    <div className="flex flex-col gap-clay-sm">
      <Label htmlFor="password">Password</Label>
      <Input
        id="password"
        type="password"
        placeholder="••••••••"
        className="clay-input"
      />
    </div>

    {/* Submit button */}
    <Button variant="clay" size="clay-lg" className="w-full">
      Sign In
    </Button>

    {/* LINE OAuth button */}
    <Button variant="clay-secondary" size="clay-lg" className="w-full">
      <LineIcon className="mr-2 h-5 w-5" />
      Continue with LINE
    </Button>
  </CardContent>
</Card>
```

Callback/Error pages:

```typescript
// Loading/feedback card
<Card variant="clay" className="w-full max-w-md mx-auto">
  <CardContent className="flex flex-col items-center gap-clay-lg py-clay-xl">
    <Spinner className="w-12 h-12 text-primary" />
    <p className="font-[var(--font-pixel)] text-sm">
      Completing authentication...
    </p>
  </CardContent>
</Card>
```

Key points:

- Auth forms use `clay-lg` variant for card container
- Inputs use `clay-input` utility class
- Buttons use clay variants
- Labels maintain compatibility with clay inputs
- Callback/error pages use simpler `clay` variant
  </action>
  <verify>
  <automated>grep "variant=\"clay" apps/web/app/auth/login/page.tsx</automated>
  <expected>Login page should have clay card variant</expected>
  </verify>
  <done>All 4 auth pages use clay form containers, inputs, and buttons with consistent styling</done>
  </task>

<task type="auto">
  <name>Task 5: Update eggs page and referrals page</name>
  <files>apps/web/app/dashboard/eggs/page.tsx, apps/web/app/dashboard/referrals/page.tsx</files>
  <action>
Update eggs listing and referrals pages.

Eggs page:

```typescript
// Page header with clay styling
<div className={cn(
  'rounded-clay-lg p-clay-xl',
  'bg-card shadow-clay-lg',
  'mb-clay-xl'
)}>
  <h1 className="font-[var(--font-pixel)] text-xl text-primary mb-2">
    Your Egg Collection
  </h1>
  <p className="text-muted-foreground">
    {eggs.length} Eggs • {eggs.filter(e => e.canHatch).length} Ready to Hatch
  </p>
</div>

// Egg grid (EggCard components already clay-styled)
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-clay-xl">
  {eggs.map(egg => (
    <EggCard key={egg.id} egg={egg} />
  ))}
</div>

// Empty state
{eggs.length === 0 && (
  <Card variant="clay" className="text-center py-clay-2xl">
    <CardContent>
      <p className="font-[var(--font-pixel)] text-sm mb-clay-lg">
        No eggs yet
      </p>
      <Button variant="clay" size="clay-md">
        Mint Your First Egg
      </Button>
    </CardContent>
  </Card>
)}
```

Referrals page:

```typescript
// Referral chain (ReferralChainDisplay already clay-styled)
<div className="flex flex-col gap-clay-xl">
  <div className={cn(
    'rounded-clay-lg p-clay-xl',
    'bg-card shadow-clay-lg'
  )}>
    <h1 className="font-[var(--font-pixel)] text-xl text-primary mb-2">
      Referral Chain
    </h1>
    <p className="text-muted-foreground mb-clay-lg">
      Your 4-level MLM commission structure
    </p>
    <ReferralChainDisplay referrals={referrals} />
  </div>

  // Earnings summary card
  <Card variant="clay-lg">
    <CardHeader>
      <CardTitle className="font-[var(--font-pixel)] text-sm">
        Total Earnings
      </CardTitle>
    </CardHeader>
    <CardContent>
      <p className="text-3xl font-bold text-primary">
        {totalEarnings} USDT
      </p>
    </CardContent>
  </Card>
</div>
```

Key points:

- Page headers use clay containers with lg variant
- Egg grid uses EggCard (already clay-styled)
- ReferralChainDisplay already clay-styled from Plan 05
- Empty states use clay cards
- Consistent `gap-clay-xl` spacing
  </action>
  <verify>
  <automated>grep "rounded-clay-lg\|shadow-clay-lg" apps/web/app/dashboard/eggs/page.tsx</automated>
  <expected>Page header should have clay styling</expected>
  </verify>
  <done>Eggs and referrals pages use clay containers for headers, grids, and summary cards with consistent spacing</done>
  </task>

<task type="auto">
  <name>Task 6: Update wallet page</name>
  <files>apps/web/app/wallet/page.tsx</files>
  <action>
Update wallet page with clay balance widgets and transaction history.

Wallet balance card:

```typescript
// Main balance display
<Card variant="clay-xl" className={cn(
  'bg-gradient-to-br from-primary/20 via-primary/10 to-transparent',
  'shadow-clay-2xl',
  'text-center'
)}>
  <CardHeader>
    <CardTitle className="font-[var(--font-pixel)] text-xs text-primary">
      Available Balance
    </CardTitle>
  </CardHeader>
  <CardContent className="flex flex-col items-center gap-clay-lg">
    <p className="text-5xl font-bold text-primary">
      {balance} USDT
    </p>
    <div className="flex gap-clay-md">
      <Button variant="clay" size="clay-md">
        Deposit
      </Button>
      <Button variant="clay-secondary" size="clay-md">
        Withdraw
      </Button>
    </div>
  </CardContent>
</Card>
```

Wallet details:

```typescript
// Wallet address card
<Card variant="clay">
  <CardHeader>
    <CardTitle className="font-[var(--font-pixel)] text-xs">
      Wallet Address
    </CardTitle>
  </CardHeader>
  <CardContent>
    <div className={cn(
      'rounded-clay-sm bg-secondary/20 p-clay-md',
      'font-mono text-sm break-all shadow-clay-sm'
    )}>
      {walletAddress}
    </div>
    <Button variant="ghost" size="sm" className="mt-clay-md">
      Copy Address
    </Button>
  </CardContent>
</Card>
```

Transaction history (uses Table from Plan 04):

```typescript
// Transaction history
<Card variant="clay-lg">
  <CardHeader>
    <CardTitle className="font-[var(--font-pixel)] text-sm">
      Transaction History
    </CardTitle>
  </CardHeader>
  <CardContent>
    {transactions.length > 0 ? (
      <Table>
        <TableHeader>
          <TableRow>
            <TableHead>Type</TableHead>
            <TableHead>Amount</TableHead>
            <TableHead>Date</TableHead>
            <TableHead>Status</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {transactions.map(tx => (
            <TableRow key={tx.id}>
              <TableCell>{tx.type}</TableCell>
              <TableCell className="text-primary">{tx.amount}</TableCell>
              <TableCell>{tx.date}</TableCell>
              <TableCell>
                <Badge variant={tx.status === 'completed' ? 'default' : 'secondary'}>
                  {tx.status}
                </Badge>
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
    ) : (
      <p className="text-muted-foreground text-center py-clay-xl">
        No transactions yet
      </p>
    )}
  </CardContent>
</Card>
```

Key points:

- Balance card uses `clay-xl` with enhanced `clay-2xl` shadow
- Wallet address uses subtle clay container
- Transaction history uses Table (clay-styled from Plan 04)
- Empty states use clay styling
- Buttons use clay variants
  </action>
  <verify>
  <automated>grep "shadow-clay-2xl" apps/web/app/wallet/page.tsx</automated>
  <expected>Balance card should have clay-2xl shadow</expected>
  </verify>
  <done>Wallet page displays with clay balance widget (xl variant, 2xl shadow), address card, and transaction table</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Verify all pages render correctly</name>
  <what-built>
Six pages redesigned with claymorphism:
- Landing page: Hero, pricing, features with clay depth
- Dashboard: Stat widgets, NFT grid
- Auth pages (4): Login, signup, callback, error forms
- Eggs page: Collection grid with EggCards
- Referrals page: ReferralChainDisplay with depth hierarchy
- Wallet page: Balance widget, transaction history
  </what-built>
  <how-to-verify>
1. Start dev server: `cd apps/web && bun run dev`
2. Visit each page in order:
   - http://localhost:3000 (landing)
   - http://localhost:3000/dashboard
   - http://localhost:3000/auth/login
   - http://localhost:3000/auth/sign-up
   - http://localhost:3000/dashboard/eggs
   - http://localhost:3000/dashboard/referrals
   - http://localhost:3000/wallet
3. For each page verify:
   - [ ] Cards have rounded corners (16-40px depending on variant)
   - [ ] Cards show depth via shadows (not borders)
   - [ ] Buttons have clay styling with hover states
   - [ ] Inputs have clay focus states
   - [ ] Typography maintains Press Start 2P for headings
   - [ ] Spacing uses clay gap/padding values
4. Landing page specific:
   - [ ] Hero CTA is large clay button (xl size)
   - [ ] Featured NFT floats with clay frame
   - [ ] Pricing cards have clay depth
5. Dashboard specific:
   - [ ] Balance card prominent with clay-xl variant
   - [ ] NFT grid shows EggCards with clay containers
6. Auth pages specific:
   - [ ] Form cards have clay-lg variant
   - [ ] Inputs have clay styling
   - [ ] Buttons use clay variants
7. Eggs/Referrals specific:
   - [ ] Page headers have clay containers
   - [ ] Grid spacing uses gap-clay-xl
8. Wallet specific:
   - [ ] Balance card has maximum depth (clay-2xl)
   - [ ] Transaction table readable with subtle clay styling
9. Open DevTools on any card
10. Verify computed styles show clay values (not old pixel borders)
  </how-to-verify>
  <resume-signal>Type "approved" if all pages display correctly with claymorphism styling, proper shadows, radius, and consistent visual hierarchy, or describe page-specific issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                     |
| --------------- | ----------------------------------------------- |
| Auth forms      | Visual clarity critical for phishing prevention |
| Balance display | Users must clearly see financial data           |

## STRIDE Threat Register

| Threat ID  | Category      | Component         | Disposition | Mitigation Plan                                                                                          |
| ---------- | ------------- | ----------------- | ----------- | -------------------------------------------------------------------------------------------------------- |
| T-07-06-01 | Spoofing      | Auth form styling | mitigate    | Clay-lg shadows and clear visual hierarchy distinguish legitimate forms from potential phishing overlays |
| T-07-06-02 | Confusion     | Balance display   | mitigate    | Clay-xl variant with high contrast (primary color) ensures balance prominently displayed                 |
| T-07-06-03 | Accessibility | Form inputs       | mitigate    | Clay focus states maintain WCAG-compliant focus rings for keyboard navigation                            |

</threat_model>

<verification>
## Page-Level Verification

1. **Landing page clay usage:**

   ```bash
   grep -c "clay-" apps/web/app/page.tsx
   ```

   Expected: 20+ occurrences (hero, pricing, features)

2. **Dashboard clay widgets:**

   ```bash
   grep "variant=\"clay" apps/web/app/dashboard/page.tsx | wc -l
   ```

   Expected: 3+ (balance card, stat cards)

3. **Auth pages clay forms:**

   ```bash
   grep "variant=\"clay-lg\"" apps/web/app/auth/login/page.tsx
   ```

   Expected: Present on form container

4. **Eggs page grid:**

   ```bash
   grep "gap-clay-xl" apps/web/app/dashboard/eggs/page.tsx
   ```

   Expected: Present on grid container

5. **Wallet page balance:**
   ```bash
   grep "shadow-clay-2xl" apps/web/app/wallet/page.tsx
   ```
   Expected: Present on balance card

## Visual Regression Test

1. Run dev server
2. Screenshot all 6 pages
3. Verify claymorphism styling applied consistently
4. Check for any broken layouts or overflow issues

## Responsive Test

1. Test each page at 3 breakpoints:
   - Mobile: 375px width
   - Tablet: 768px width
   - Desktop: 1440px width
2. Verify clay containers scale appropriately
3. Check shadows don't cause clipping on mobile
   </verification>

<success_criteria>

- [x] Landing page hero uses clay-xl button and clay-2xl shadow NFT frame
- [x] Landing page pricing cards use clay-lg variant
- [x] Dashboard balance card uses clay-xl variant for prominence
- [x] Dashboard NFT grid uses EggCards with clay containers
- [x] All 4 auth pages use clay-lg form containers
- [x] Auth inputs use clay styling with focus states
- [x] Eggs page header uses clay container
- [x] Eggs grid uses gap-clay-xl spacing
- [x] Referrals page displays ReferralChainDisplay with clay depth
- [x] Wallet balance card uses clay-xl with clay-2xl shadow
- [x] Wallet transaction table uses subtle clay styling
- [x] All pages maintain Press Start 2P typography for headings
- [x] All pages compile without TypeScript errors
- [x] All pages render correctly at mobile, tablet, desktop breakpoints
      </success_criteria>

<output>
After completion, create `.planning/phases/07-claymorphism-redesign/07-06-SUMMARY.md` documenting:
- Page redesign summary (6 pages updated)
- Clay variant usage patterns (which pages use clay/clay-lg/clay-xl)
- Responsive design notes
- Visual verification results
- Ready status for Plan 07 (Pages - Wave 2)
</output>
