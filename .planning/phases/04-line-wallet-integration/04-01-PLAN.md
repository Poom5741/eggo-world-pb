---
phase: 04-line-wallet-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wallet-api/package.json
  - wallet-api/tsconfig.json
  - wallet-api/src/index.ts
  - wallet-api/src/routes/createWallet.ts
  - wallet-api/src/env.ts
  - wallet-api/.env.example
autonomous: true
requirements:
  - "Phase 2.3: Wallet API Endpoints"
  - "Phase 2.2: Auto-create wallet hook"

must_haves:
  truths:
    - "Wallet API accepts POST /api/wallet/create with passwordSecretkey"
    - "Wallet API returns DACC wallet address and daccPublickey"
    - "Wallet API uses dacc-js v0.0.5 for wallet creation"
    - "Wallet API runs on Bun runtime with TypeScript"
  artifacts:
    - path: "wallet-api/package.json"
      provides: "Dependencies with dacc-js v0.0.5, TypeScript, Express"
      contains: "dacc-js, typescript, bun-types"
    - path: "wallet-api/src/index.ts"
      provides: "Express server entry point"
      exports: ["app", "health endpoint", "/api/wallet/create route"]
    - path: "wallet-api/src/routes/createWallet.ts"
      provides: "Wallet creation endpoint using dacc-js"
      exports: ["createWalletRouter"]
    - path: "wallet-api/tsconfig.json"
      provides: "TypeScript configuration for Bun"
      contains: "module: ESNext, moduleResolution: bundler"
  key_links:
    - from: "wallet-api/src/index.ts"
      to: "wallet-api/src/routes/createWallet.ts"
      via: "Express router import"
      pattern: "import.*createWalletRouter"
    - from: "wallet-api/src/routes/createWallet.ts"
      to: "dacc-js"
      via: "createDaccWallet function"
      pattern: "import.*createDaccWallet.*from.*dacc-js"
---

<objective>
Migrate wallet-api from JavaScript + ethers v6 to TypeScript + dacc-js v0.0.5

Purpose: Replace EVM wallet generation with DACC wallet generation per decision D-01, matching reference implementation structure from resources/pkbase-wallet/wallet-srv/

Output: TypeScript wallet API with dacc-js, running on Bun runtime, backward-compatible endpoint at POST /api/wallet/create
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-line-wallet-integration/04-CONTEXT.md
@wallet-api/server.js
@wallet-api/AGENTS.md
@resources/pkbase-wallet/wallet-srv/src/index.ts
@resources/pkbase-wallet/wallet-srv/src/routes/createWallet.ts
@resources/pkbase-wallet/wallet-srv/package.json
</context>

<interfaces>
<!-- Key types and contracts from reference implementation -->

From resources/pkbase-wallet/wallet-srv/src/routes/createWallet.ts:

```typescript
interface CreateWalletRequest {
  passwordSecretkey: string
  publicEncryption?: boolean
  dataStorageNetwork?: string
  pkWalletForSaveData?: string
  minPassword?: number
  maxPassword?: number
}

interface CreateWalletResponse {
  success: boolean
  data?: {
    address: string
    daccPublickey: string
  }
  error?: {
    message: string
    code: string
  }
}
```

From resources/pkbase-wallet/wallet-srv/src/index.ts:

```typescript
// Express app setup with middleware
app.use(helmet())
app.use(cors({ origin: process.env.CORS_ORIGIN || "http://localhost:3000", credentials: true }))
app.use(express.json({ limit: "10mb" }))
```

</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Set up TypeScript + Bun project structure</name>
  <files>wallet-api/package.json, wallet-api/tsconfig.json, wallet-api/.env.example</files>
  <action>
    1. Create wallet-api/package.json with:
       - name: "wallet-api", version: "2.0.0" (major bump for breaking change)
       - type: "module" (ES modules for TypeScript)
       - scripts: dev ("bun --watch src/index.ts"), start ("bun src/index.ts"), build ("bun build src/index.ts --outdir dist --target bun")
       - dependencies: dacc-js@^0.0.5, express@^4.18.2, cors@^2.8.5, helmet@^7.1.0, dotenv@^16.3.1
       - devDependencies: @types/express@^4.17.21, @types/cors@^2.8.17, @types/node@^20.10.5, typescript@^5.3.3, bun-types@latest
       - packageManager: "pnpm@8.0.0" (or "bun" if preferred)

    2. Create wallet-api/tsconfig.json with:
       - compilerOptions: target: "ES2022", module: "ESNext", moduleResolution: "bundler"
       - lib: ["ES2022"], types: ["bun-types"], strict: true
       - include: ["src/**/*"], exclude: ["node_modules", "dist"]

    3. Create wallet-api/.env.example with:
       - PORT=3001
       - CORS_ORIGIN=http://localhost:3000
       - MIN_PASSWORD_LENGTH=12
       - MAX_PASSWORD_LENGTH=120
       - PUBLIC_ENCRYPTION=false
       - DATA_STORAGE_NETWORK=opSepolia

    Per D-03: Using TypeScript + Express + Bun runtime matching reference implementation.
    Per D-01: Using dacc-js v0.0.5 instead of ethers v6.

  </action>
  <verify>
    <automated>cd wallet-api && bun install && tsc --noEmit</automated>
  </verify>
  <done>
    - package.json has dacc-js@^0.0.5, typescript, express dependencies
    - tsconfig.json configured for Bun with ES modules
    - .env.example has all required environment variables
    - TypeScript compilation succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>task 2: Create TypeScript wallet routes with dacc-js</name>
  <files>wallet-api/src/routes/createWallet.ts, wallet-api/src/env.ts</files>
  <action>
    1. Create wallet-api/src/env.ts for type-safe environment variables:
       - Export processEnv object with PORT, CORS_ORIGIN, MIN_PASSWORD_LENGTH, etc.
       - Use parseInt for numeric values with defaults

    2. Create wallet-api/src/routes/createWallet.ts:
       - Import createDaccWallet from 'dacc-js'
       - Define CreateWalletRequest and CreateWalletResponse interfaces
       - Create Express router with POST /create endpoint
       - Validate passwordSecretkey is present and 12-120 chars (per reference)
       - Call createDaccWallet with: { passwordSecretkey, publicEncryption: false }
       - Return { success: true, data: { address, daccPublickey } }
       - Error handling: return { success: false, error: { message, code } }

       Per D-01: Response must have 'address' and 'daccPublickey' fields (NOT publicKey/encryptedPrivateKey from ethers)
       Per D-07: Keep endpoint as /api/wallet/create (NOT /api/v1/wallet/create) for backward compatibility with existing hook

    3. Create wallet-api/src/index.ts:
       - Import express, cors, helmet, dotenv
       - Configure middleware (helmet, cors, express.json)
       - Add /health endpoint returning { status: 'OK', timestamp, service: 'wallet-api', version: '2.0.0' }
       - Mount /api/wallet router with createWalletRouter
       - Add 404 and error handlers
       - Start server on PORT

  </action>
  <verify>
    <automated>cd wallet-api && tsc --noEmit && echo "TypeScript compiles successfully"</automated>
  </verify>
  <done>
    - createWallet.ts uses createDaccWallet from dacc-js
    - POST /api/wallet/create accepts passwordSecretkey, returns address + daccPublickey
    - GET /health returns service status
    - Error handling returns proper JSON format
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- Wallet API compiles with TypeScript (tsc --noEmit passes)
- Dependencies installed (dacc-js, express, cors, helmet, typescript, bun-types)
- Endpoint structure matches reference: POST /api/wallet/create
- Response format: { success: true, data: { address, daccPublickey } }
- Health check: GET /health returns OK status
</verification>

<success_criteria>

- TypeScript project structure complete with proper tsconfig.json
- dacc-js v0.0.5 integrated for wallet creation
- Express server running on Bun runtime
- Backward-compatible endpoint at /api/wallet/create
- Response format changed from ethers (address, publicKey, encryptedPrivateKey) to DACC (address, daccPublickey)
  </success_criteria>

<output>
After completion, create `.planning/phases/04-line-wallet-integration/04-01-SUMMARY.md` with:
- Files created and their purposes
- Dependencies installed
- Breaking changes from v1 (ethers) to v2 (dacc-js)
- How to test the new endpoint manually
</output>
