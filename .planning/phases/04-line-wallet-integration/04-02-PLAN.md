---
phase: 04-line-wallet-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/backend/pb_hooks/01-create-wallet.pb.js
  - apps/backend/collections/users.json
  - apps/backend/.env.example
  - wallet-api/.env
autonomous: false
requirements:
  - "Phase 2.2: Auto-create wallet hook"
  - "Phase 2.3: Wallet API Endpoints"

user_setup:
  - service: wallet-api
    why: "DACC wallet generation service"
    env_vars:
      - name: PORT
        source: "Set to 3001"
      - name: CORS_ORIGIN
        source: "Set to frontend URL or http://localhost:3000"
      - name: MIN_PASSWORD_LENGTH
        source: "Set to 12 (minimum password length)"
      - name: MAX_PASSWORD_LENGTH
        source: "Set to 120 (maximum password length)"
    dashboard_config:
      - task: "Start wallet-api service"
        location: "Run: cd wallet-api && bun install && bun run dev"
      - task: "Verify wallet API health"
        location: "curl http://localhost:3001/health"

must_haves:
  truths:
    - "PocketBase hook triggers on user signup via LINE OAuth"
    - "Hook generates 20-char random passwordSecretkey"
    - "Hook calls wallet-api and receives DACC wallet data"
    - "User record saves: wallet (address), pin (passwordSecretkey), daccPublickey"
    - "Existing LINE OAuth flow unchanged"
  artifacts:
    - path: "apps/backend/pb_hooks/01-create-wallet.pb.js"
      provides: "Wallet creation hook triggered on user signup"
      contains: "onRecordCreate, $http.send to wallet-api"
    - path: "apps/backend/collections/users.json"
      provides: "User schema with wallet, pin, daccPublickey fields"
      contains: "wallet, pin, daccPublickey field definitions"
    - path: "apps/backend/.env.example"
      provides: "Environment variables for PocketBase"
      contains: "WALLET_SRV_URL"
  key_links:
    - from: "apps/backend/pb_hooks/01-create-wallet.pb.js"
      to: "wallet-api:3001/api/wallet/create"
      via: "$http.send POST request"
      pattern: "\\$http\\.send.*POST.*wallet.*create"
    - from: "apps/backend/pb_hooks/01-create-wallet.pb.js"
      to: "apps/backend/collections/users.json"
      via: "e.record.set() calls"
      pattern: "e\\.record\\.set\\(.*(wallet|pin|daccPublickey)"
---

<objective>
Update PocketBase hook to call new DACC wallet API and save wallet data to user records

Purpose: Integrate migrated wallet-api (Plan 01) with existing LINE OAuth flow, saving DACC wallet credentials to user records per decisions D-04, D-06

Output: Working wallet creation flow: LINE signup → hook generates password → calls wallet-api → saves wallet/pin/daccPublickey to user record
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-line-wallet-integration/04-CONTEXT.md
@.planning/phases/04-line-wallet-integration/04-01-SUMMARY.md
@apps/backend/pb_hooks/01-create-wallet.pb.js
@apps/backend/collections/users.json
@resources/pkbase-wallet/pkbase/pb_hooks/01-create-wallet-hook.pb.js
@apps/backend/AGENTS.md
</context>

<interfaces>
<!-- Key contracts from existing code -->

From apps/backend/collections/users.json (existing fields):

```json
{
  "name": "wallet",
  "type": "text",
  "pattern": "^0x[a-fA-F0-9]{40}$"
}
{
  "name": "pin",
  "type": "text",
  "hidden": true
}
{
  "name": "daccPublickey",
  "type": "text",
  "pattern": "^daccPublickey_"
}
```

From resources/pkbase-wallet/pkbase/pb_hooks/01-create-wallet-hook.pb.js (reference):

```javascript
// Generate 20-char random password
const charset = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"
let passwordSecretkey = ""
for (let i = 0; i < 20; i++) {
  passwordSecretkey += charset.charAt(Math.floor(Math.random() * charset.length))
}

// Call wallet API
const response = $http.send({
  url: "http://wallet-srv:3000/api/v1/wallet/create",
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ passwordSecretkey, publicEncryption: false }),
})

// Save to user record
e.record.set("wallet", responseData.data.address)
e.record.set("pin", passwordSecretkey)
e.record.set("daccPublickey", responseData.data.daccPublickey)
```

</interfaces>

<tasks>

<task type="auto">
  <name>task 1: Update PocketBase wallet creation hook</name>
  <files>apps/backend/pb_hooks/01-create-wallet.pb.js, apps/backend/.env.example</files>
  <action>
    1. Update apps/backend/pb_hooks/01-create-wallet.pb.js:
       - Set WALLET_SRV_URL = process.env.WALLET_SRV_URL || "http://wallet-api:3001"
       - Generate 20-char random passwordSecretkey (charset: a-zA-Z0-9!@#$%^&*)
       - Call wallet API: POST ${WALLET_SRV_URL}/api/wallet/create
       - Request body: { passwordSecretkey, publicEncryption: false }
       - Parse response: responseData.data.address, responseData.data.daccPublickey
       - Save to user record:
         * e.record.set("wallet", responseData.data.address)
         * e.record.set("pin", passwordSecretkey)
         * e.record.set("daccPublickey", responseData.data.daccPublickey)
       - Handle byte array to string conversion (check typeof response.body === 'object' && length !== undefined)
       - Error handling: throw if statusCode >= 300 or response empty or !responseData.success

    2. Update apps/backend/.env.example:
       - Add WALLET_SRV_URL=http://wallet-api:3001 (for Docker)
       - Add comment: "For local dev: WALLET_SRV_URL=http://localhost:3001"

    Per D-02: Keep auto-generated 20-char random passwords (not user-provided)
    Per D-06: Update hook to call wallet API with passwordSecretkey, save response fields
    Per D-07: Keep endpoint as /api/wallet/create (backward compatible, no version prefix)

  </action>
  <verify>
    <automated>cd apps/backend && grep -q "passwordSecretkey" pb_hooks/01-create-wallet.pb.js && grep -q "daccPublickey" pb_hooks/01-create-wallet.pb.js && echo "Hook updated successfully"</automated>
  </verify>
  <done>
    - Hook generates 20-char random passwordSecretkey
    - Hook calls POST /api/wallet/create with { passwordSecretkey, publicEncryption: false }
    - Hook saves wallet, pin, daccPublickey to user record
    - Hook handles byte array to string conversion
    - Hook has proper error handling
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 2: Verify wallet creation integration</name>
  <files>apps/backend/pb_hooks/01-create-wallet.pb.js</files>
  <action>
    This is a checkpoint task - no automated action. Human verification required.
  </action>
  <what-built>
    Updated PocketBase wallet creation hook (01-create-wallet.pb.js) that:
    - Generates 20-char random passwordSecretkey on user signup
    - Calls wallet-api:3001/api/wallet/create with password
    - Saves DACC wallet address, pin, and daccPublickey to user record
  </what-built>
  <verify>
    <automated>MISSING - Manual verification required via PocketBase Admin UI</automated>
  </verify>
  <done>
    - User record has wallet, pin, daccPublickey fields populated
    - Wallet API called successfully (verified in logs)
    - No errors in PocketBase or wallet-api logs
  </done>
  <how-to-verify>
    **Prerequisites:**
    1. Wallet API running: `cd wallet-api && bun install && bun run dev` (verify: curl http://localhost:3001/health)
    2. PocketBase running: `cd apps/backend && docker-compose up -d` (verify: curl http://localhost:8090/api/health)

    **Test wallet creation flow:**
    1. Open PocketBase Admin UI: http://localhost:8090/_/
    2. Navigate to Collections → users
    3. Create a test user manually (or via LINE OAuth if configured):
       - email: test@example.com
       - externalId: test-123
       - name: Test User
    4. After user creation, check the user record fields:
       - wallet: Should contain DACC address (starts with "0x" or DACC format)
       - pin: Should contain 20-char random password
       - daccPublickey: Should contain DACC public key (starts with "daccPublickey_")
    5. Verify wallet API was called: Check wallet-api logs for POST /api/wallet/create request
    6. Verify response format: { success: true, data: { address, daccPublickey } }

    **Expected behavior:**
    - ✅ User record has wallet, pin, daccPublickey fields populated
    - ✅ pin is exactly 20 characters with mixed case + numbers + special chars
    - ✅ wallet matches address returned by wallet API
    - ✅ daccPublickey matches daccPublickey returned by wallet API
    - ✅ No errors in PocketBase logs or wallet-api logs

    **If verification fails:**
    - Check wallet-api is running and responding to health check
    - Check WALLET_SRV_URL in apps/backend/.env or docker-compose.yml
    - Check PocketBase logs: docker-compose logs -f | grep "create wallet"
    - Check wallet-api logs for incoming requests

  </how-to-verify>
  <resume-signal>
    Type "approved" if:
    - User record has wallet, pin, daccPublickey populated correctly
    - Wallet API called successfully
    - No errors in logs

    OR describe issues found:
    - Missing fields: [which fields]
    - API call failed: [error from logs]
    - Other: [description]

  </resume-signal>
</task>

<task type="auto">
  <name>task 3: Add environment configuration and documentation</name>
  <files>wallet-api/.env, apps/backend/docker-compose.yml</files>
  <action>
    1. Create wallet-api/.env (gitignored) with:
       - PORT=3001
       - CORS_ORIGIN=http://localhost:3000,http://localhost:8090
       - MIN_PASSWORD_LENGTH=12
       - MAX_PASSWORD_LENGTH=120
       - PUBLIC_ENCRYPTION=false
       - DATA_STORAGE_NETWORK=opSepolia

    2. Update apps/backend/docker-compose.yml (if exists) to add wallet-api service:
       - service: wallet-api
       - build: ./wallet-api (or context: ../../wallet-api)
       - ports: "3001:3001"
       - environment: PORT, CORS_ORIGIN, MIN_PASSWORD_LENGTH, MAX_PASSWORD_LENGTH
       - networks: same as pocketbase

    3. Create wallet-api/README.md with:
       - Overview: DACC wallet generation service
       - Setup: bun install, bun run dev
       - Endpoints: POST /api/wallet/create, GET /health
       - Request/response examples
       - Environment variables

    Per D-08: This is Phase 4A (TypeScript migration) + 4B (DACC integration)

  </action>
  <verify>
    <automated>test -f wallet-api/.env && grep -q "PORT=3001" wallet-api/.env && echo ".env file created"</automated>
  </verify>
  <done>
    - wallet-api/.env exists with all required environment variables
    - docker-compose.yml has wallet-api service (if using Docker)
    - README.md documents setup and endpoints
  </done>
</task>

</tasks>

<verification>
- PocketBase hook updated to call DACC wallet API
- User record populated with wallet, pin, daccPublickey on signup
- Wallet API running and responding to health checks
- Integration test passes: LINE signup → wallet created → fields saved
- No errors in PocketBase or wallet-api logs
</verification>

<success_criteria>

- Wallet creation flow working end-to-end:
  1. User signs up via LINE OAuth
  2. Hook triggers and generates 20-char passwordSecretkey
  3. Wallet API creates DACC wallet and returns address + daccPublickey
  4. User record saved with wallet (address), pin (passwordSecretkey), daccPublickey
- Backward compatible with existing LINE OAuth flow (D-05)
- Supports both wallet types during migration (D-07): existing ethers wallets remain, new users get DACC wallets
  </success_criteria>

<output>
After completion, create `.planning/phases/04-line-wallet-integration/04-02-SUMMARY.md` with:
- Hook changes made
- User record fields verified
- Test results from manual verification
- Any issues found and how they were resolved
- Next steps for Phase 05 (Testing & Launch)
</output>
