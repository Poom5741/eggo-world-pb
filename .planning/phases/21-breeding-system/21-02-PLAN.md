---
plan_id: "21-02"
phase: 21
title: "Animal Card Action Menu"
wave: 1
depends_on: ["21-01"]
autonomous: true
estimated_hours: 3
requirements_addressed: ["BREED-01"]
files_modified:
  - "apps/web/components/animal-nft/AnimalCard.tsx"
  - "apps/web/app/animals/page.tsx"
---

# Plan 21-02: Animal Card Action Menu

## Objective

Add 'Breed' button to AnimalCard component and integrate BreedingDialog into the animals page. Implements D-08 and D-09 from CONTEXT.md.

## Context

- AnimalCard.tsx currently only has 'Sell' button
- Animals page at apps/web/app/animals/page.tsx displays AnimalCard grid
- BreedingDialog from 21-01 needs to be integrated
- No dedicated breeding route - accessed from animal cards (per D-09)

## Tasks

### Task 1: Add last_bred_at to AnimalData interface

<task id="21-02-01">
  <title>Extend AnimalData with last_bred_at field</title>
  <read_first>
    - apps/web/hooks/use-animal-poll.ts (current AnimalData interface)
    - apps/backend/pb_migrations/1775399933499_create_animal_nfts.js (schema)
  </read_first>
  <acceptance_criteria>
    - AnimalData interface in use-animal-poll.ts includes last_bred_at?: string field
    - Hook maps last_bred_at from PocketBase record
    - Type is optional string (ISO date format)
  </acceptance_criteria>
  <action>
    Modify apps/web/hooks/use-animal-poll.ts:
    - Add last_bred_at?: string to AnimalData interface
    - In fetchAnimals, map last_bred_at: record.last_bred_at
    - This enables cooldown calculation in UI
  </action>
  <verify>
    <automated>grep -n "last_bred_at" apps/web/hooks/use-animal-poll.ts</automated>
  </verify>
  <done>
    - AnimalData interface includes last_bred_at?: string field
    - Hook maps last_bred_at from PocketBase records
    - TypeScript compilation passes
  </done>
</task>

### Task 2: Add Breed button to AnimalCard

<task id="21-02-02">
  <title>Add Breed button and onBreed callback to AnimalCard</title>
  <read_first>
    - apps/web/components/animal-nft/AnimalCard.tsx (current implementation)
    - apps/web/components/eggs/egg-card.tsx (action button pattern)
  </read_first>
  <acceptance_criteria>
    - AnimalCardProps extended with onBreed?: (animal: AnimalData) => void
    - Breed button added to action buttons section
    - Button shows "Breed" text with pets icon
    - Button styling: bg-primary-container text-on-primary-container
    - Button disabled when animal is on cooldown (if last_bred_at within 48h)
    - Disabled button shows tooltip: "Available in Xh Ym"
  </acceptance_criteria>
  <action>
    Modify apps/web/components/animal-nft/AnimalCard.tsx:
    - Add onBreed to AnimalCardProps interface
    - Add helper function isOnCooldown(lastBredAt: string | undefined): boolean
    - Add helper function getCooldownRemaining(lastBredAt: string): string
    - Add Breed button after Sell button (or before if no Sell)
    - Button: onClick={() => onBreed?.(animal)}
    - Disabled state: disabled={isOnCooldown(animal.last_bred_at)}
    - Tooltip using title attribute or custom tooltip component
  </action>
  <verify>
    <automated>grep -n "onBreed\|Breed\|isOnCooldown" apps/web/components/animal-nft/AnimalCard.tsx | head -10</automated>
  </verify>
  <done>
    - AnimalCardProps includes onBreed callback
    - Breed button visible in action buttons
    - Button disabled when animal on cooldown
    - Tooltip shows remaining cooldown time
  </done>
</task>

### Task 3: Integrate BreedingDialog into Animals page

<task id="21-02-03">
  <title>Add BreedingDialog state management to animals page</title>
  <read_first>
    - apps/web/app/animals/page.tsx (current implementation)
    - apps/web/app/eggs/page.tsx (dialog state pattern)
  </read_first>
  <acceptance_criteria>
    - Animals page imports BreedingDialog from '@/components/breeding/BreedingDialog'
    - State added: breedingAnimal, breedingDialogOpen
    - handleBreed function opens dialog with selected animal as first parent
    - BreedingDialog rendered conditionally when breedingDialogOpen
    - onSuccess refreshes animals list via refresh() from useAnimalPoll
  </acceptance_criteria>
  <action>
    Modify apps/web/app/animals/page.tsx:
    - Import BreedingDialog from '@/components/breeding/BreedingDialog'
    - Add state: const [breedingAnimal, setBreedingAnimal] = useState<AnimalData | null>(null)
    - Add state: const [breedingDialogOpen, setBreedingDialogOpen] = useState(false)
    - Add handler: const handleBreed = (animal: AnimalData) => { setBreedingAnimal(animal); setBreedingDialogOpen(true); }
    - Pass onBreed={handleBreed} to AnimalCard components
    - Render BreedingDialog when breedingDialogOpen with:
      - open={breedingDialogOpen}
      - onOpenChange={setBreedingDialogOpen}
      - initialParent1={breedingAnimal} (BreedingDialog needs to accept this)
      - onSuccess={() => { refresh(); setBreedingDialogOpen(false); }}
  </action>
  <verify>
    <automated>grep -n "BreedingDialog\|breedingAnimal\|handleBreed" apps/web/app/animals/page.tsx | head -10</automated>
  </verify>
  <done>
    - BreedingDialog imported and rendered in animals page
    - State management for breedingAnimal and breedingDialogOpen
    - handleBreed callback passed to AnimalCard
    - onSuccess triggers refresh()
  </done>
</task>

### Task 4: Update BreedingDialog to accept initial parent

<task id="21-02-04">
  <title>Extend BreedingDialog to accept pre-selected first parent</title>
  <read_first>
    - apps/web/components/breeding/BreedingDialog.tsx (from 21-01)
  </read_first>
  <acceptance_criteria>
    - BreedingDialog props extended with initialParent1?: AnimalData
    - When initialParent1 provided, it's pre-selected in step 1
    - User only needs to select second parent
    - Pre-selected parent shown as disabled/locked in grid
  </acceptance_criteria>
  <action>
    Modify apps/web/components/breeding/BreedingDialog.tsx:
    - Add initialParent1?: AnimalData to props
    - In useEffect, if initialParent1 exists, setSelectedAnimals([initialParent1.animal_id])
    - In AnimalSelectionGrid, pass disabledIds={[initialParent1?.animal_id]}
    - Show "Parent 1 selected" indicator in UI
  </action>
  <verify>
    <automated>grep -n "initialParent1\|Parent 1 selected" apps/web/components/breeding/BreedingDialog.tsx</automated>
  </verify>
  <done>
    - BreedingDialog accepts initialParent1 prop
    - Pre-selected animal appears in selected state
    - Grid disables the pre-selected animal
    - User can select second parent
  </done>
</task>

## Verification

### Build Verification

- [ ] `cd apps/web && bun run build` exits 0

### Test Verification

- [ ] AnimalCard renders with Breed button
- [ ] Breed button disabled when animal on cooldown

### Functional Verification

- [ ] Clicking Breed opens BreedingDialog
- [ ] First animal pre-selected when opening from card
- [ ] After successful breeding, animals list refreshes

## must_haves

- Breed button on animal card action menu (per D-08)
- No dedicated route - accessed from animal cards (per D-09)
- Cooldown status affects button state (per D-04)
