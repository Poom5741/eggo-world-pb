---
plan_id: "21-03"
phase: 21
title: "Cooldown Display & Validation"
wave: 2
depends_on: ["21-01", "21-02"]
autonomous: true
estimated_hours: 4
requirements_addressed: ["BREED-02", "BREED-06"]
files_modified:
  - "apps/web/components/breeding/CooldownTimer.tsx"
  - "apps/web/components/animal-nft/AnimalCard.tsx"
  - "apps/backend/pb_migrations/1775399933499_create_animal_nfts.js"
  - "apps/backend/pb_hooks/18-breed-animals.pb.js"
---

# Plan 21-03: Cooldown Display & Validation

## Objective

Implement visual countdown timer for breeding cooldown and multi-layer validation (frontend filters, backend hook fast-fail). Implements D-04, D-05, D-10 from CONTEXT.md.

## Context

- BREED_COOLDOWN = 48 hours (from AnimalNFT.sol line 31)
- Smart contract has canBreed() function that checks cooldown
- Backend hook needs cooldown validation before calling wallet-api
- Frontend needs to filter animals on cooldown before dialog opens
- Countdown should be live-updating (per specific ideas)

## Tasks

### Task 1: Add last_bred_at field to animal_nfts schema

<task id="21-03-01">
  <title>Create migration to add last_bred_at field to animal_nfts</title>
  <read_first>
    - apps/backend/pb_migrations/1775399933499_create_animal_nfts.js (existing schema)
  </read_first>
  <acceptance_criteria>
    - New migration file created: apps/backend/pb_migrations/21_add_last_bred_at.js
    - Adds last_bred_at field (date type, optional) to animal_nfts collection
    - Field has no default value (null means never bred)
  </acceptance_criteria>
  <action>
    Create apps/backend/pb_migrations/21_add_last_bred_at.js:
    - Use migrate() to add field
    - Field config: { name: "last_bred_at", type: "date", required: false }
    - Include down migration to remove field
  </action>
  <verify>
    <automated>test -f apps/backend/pb_migrations/21_add_last_bred_at.js && grep -q "last_bred_at" apps/backend/pb_migrations/21_add_last_bred_at.js</automated>
  </verify>
  <done>
    - Migration file created at apps/backend/pb_migrations/21_add_last_bred_at.js
    - last_bred_at field added to animal_nfts collection
    - Down migration included
  </done>
</task>

### Task 2: Create CooldownTimer component

<task id="21-03-02">
  <title>Create live-updating cooldown countdown component</title>
  <read_first>
    - apps/web/components/ui/badge.tsx (for badge styling)
    - Any existing timer/countdown patterns in codebase
  </read_first>
  <acceptance_criteria>
    - File exists at apps/web/components/breeding/CooldownTimer.tsx
    - Props: lastBredAt: string, cooldownHours: number (default 48)
    - Returns null if no cooldown (lastBredAt is null/undefined)
    - Shows "Available now" if cooldown expired
    - Shows countdown "Available in 12h 34m" if on cooldown
    - Updates every minute (live countdown)
    - Visual indicator: green when available, amber when on cooldown
  </acceptance_criteria>
  <action>
    Create apps/web/components/breeding/CooldownTimer.tsx:
    - Helper: calculateRemainingTime(lastBredAt: string, cooldownHours: number): { hours: number; minutes: number; expired: boolean }
    - Use useEffect with setInterval(60000) for minute updates
    - Format: "Available in Xh Ym" or "Available now"
    - Styling: Badge component with variant based on status
    - Cleanup interval on unmount
  </action>
  <verify>
    <automated>test -f apps/web/components/breeding/CooldownTimer.tsx && grep -q "calculateRemainingTime" apps/web/components/breeding/CooldownTimer.tsx</automated>
  </verify>
  <done>
    - CooldownTimer.tsx created at apps/web/components/breeding/CooldownTimer.tsx
    - Component accepts lastBredAt and cooldownHours props
    - Live countdown updates every minute
  </done>
</task>

### Task 3: Update AnimalCard with cooldown display

<task id="21-03-03">
  <title>Add cooldown timer display to AnimalCard</title>
  <read_first>
    - apps/web/components/animal-nft/AnimalCard.tsx (current implementation)
    - apps/web/components/breeding/CooldownTimer.tsx (from Task 2)
  </read_first>
  <acceptance_criteria>
    - AnimalCard displays CooldownTimer when last_bred_at exists
    - Timer positioned near rarity/generation info
    - Breed button disabled with tooltip when on cooldown
    - Visual distinction: grayed out card or disabled button state
  </acceptance_criteria>
  <action>
    Modify apps/web/components/animal-nft/AnimalCard.tsx:
    - Import CooldownTimer from '@/components/breeding/CooldownTimer'
    - Add CooldownTimer display in animal info section
    - Update isOnCooldown helper to use last_bred_at
    - Update Breed button: disabled={isOnCooldown} with title="Available in Xh Ym"
    - Optional: Add opacity-75 to entire card when on cooldown
  </action>
  <verify>
    <automated>grep -q "CooldownTimer" apps/web/components/animal-nft/AnimalCard.tsx && grep -q "isOnCooldown" apps/web/components/animal-nft/AnimalCard.tsx</automated>
  </verify>
  <done>
    - AnimalCard.tsx imports and displays CooldownTimer
    - Breed button disabled when animal is on cooldown
    - Visual distinction for animals on cooldown
  </done>
</task>

### Task 4: Add cooldown validation to backend hook

<task id="21-03-04">
  <title>Add cooldown fast-fail validation to 18-breed-animals.pb.js</title>
  <read_first>
    - apps/backend/pb_hooks/18-breed-animals.pb.js (current implementation)
    - apps/backend/pb_hooks/16-feed-egg.pb.js (fast-fail pattern)
    - contracts/src/AnimalNFT.sol (BREED_COOLDOWN constant)
  </read_first>
  <acceptance_criteria>
    - Hook validates parent1 cooldown before calling wallet-api
    - Hook validates parent2 cooldown before calling wallet-api
    - Returns HTTP 400 with code ANIMAL_ON_COOLDOWN if either parent on cooldown
    - Error message includes remaining time: "Animal is on breeding cooldown — available in X hours"
    - Validation happens after ownership check, before fee deduction
  </acceptance_criteria>
  <action>
    Modify apps/backend/pb_hooks/18-breed-animals.pb.js:
    - After ownership validation (search for ownership check), add cooldown checks:
    - const BREED_COOLDOWN_MS = 48 * 60 * 60 * 1000; // 48 hours
    - Check parent1.get('last_bred_at'):
      - If exists and Date.now() - new Date(last_bred_at).getTime() < BREED_COOLDOWN_MS
      - Calculate remaining hours: Math.ceil((BREED_COOLDOWN_MS - elapsed) / (60 * 60 * 1000))
      - Return e.json(400, { success: false, error: { message: `Parent1 is on breeding cooldown — available in ${remainingHours} hours`, code: 'PARENT1_ON_COOLDOWN' }})
    - Same check for parent2 with code 'PARENT2_ON_COOLDOWN'
    - Follow same pattern as foodCount validation in 16-feed-egg.pb.js
  </action>
  <verify>
    <automated>grep -q "PARENT1_ON_COOLDOWN" apps/backend/pb_hooks/18-breed-animals.pb.js && grep -q "BREED_COOLDOWN_MS" apps/backend/pb_hooks/18-breed-animals.pb.js</automated>
  </verify>
  <done>
    - Hook validates cooldown for both parents before calling wallet-api
    - Returns 400 with PARENT1_ON_COOLDOWN or PARENT2_ON_COOLDOWN error codes
    - Error message includes remaining hours
  </done>
</task>

### Task 5: Update hook to set last_bred_at after breeding

<task id="21-03-05">
  <title>Update breeding hook to record last_bred_at timestamp</title>
  <read_first>
    - apps/backend/pb_hooks/18-breed-animals.pb.js (egg creation section)
  </read_first>
  <acceptance_criteria>
    - After successful breeding, hook updates parent1 with last_bred_at = now
    - Hook updates parent2 with last_bred_at = now
    - Uses $app.dao().saveRecord() for each parent
  </acceptance_criteria>
  <action>
    Modify apps/backend/pb_hooks/18-breed-animals.pb.js:
    - After creating breeding egg (search for breedingEgg.set), add:
    - const now = new Date().toISOString();
    - parent1.set('last_bred_at', now);
    - $app.dao().saveRecord(parent1);
    - parent2.set('last_bred_at', now);
    - $app.dao().saveRecord(parent2);
  </action>
  <verify>
    <automated>grep -q "last_bred_at.*now" apps/backend/pb_hooks/18-breed-animals.pb.js && grep -q "saveRecord(parent1)" apps/backend/pb_hooks/18-breed-animals.pb.js</automated>
  </verify>
  <done>
    - Hook updates parent1.last_bred_at after successful breeding
    - Hook updates parent2.last_bred_at after successful breeding
    - Both parents locked for 48-hour cooldown period
  </done>
</task>

## Verification

### Build Verification

- [ ] `cd apps/web && bun run build` exits 0
- [ ] PocketBase migrations apply without errors

### Test Verification

- [ ] CooldownTimer shows correct remaining time
- [ ] Hook returns 400 for animals on cooldown

### Functional Verification

- [ ] Animals on cooldown show countdown timer
- [ ] Breed button disabled during cooldown
- [ ] Hook rejects breeding with proper error message
- [ ] After breeding, parents have last_bred_at set

## must_haves

- Visual countdown timer showing remaining cooldown (per D-04)
- Multi-layer validation: frontend filters, backend hook fast-fail (per D-05, D-10)
- Both parents locked during 48-hour cooldown (per BREED-06)
- Error message includes remaining time
