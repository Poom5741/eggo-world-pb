---
plan_id: "21-06"
phase: 21
title: "Success Animation & Flow"
wave: 3
depends_on: ["21-01", "21-04"]
autonomous: true
estimated_hours: 3
requirements_addressed: ["BREED-07"]
files_modified:
  - "apps/web/components/breeding/BreedingSuccessModal.tsx"
  - "apps/web/components/breeding/BreedingDialog.tsx"
  - "apps/web/app/eggs/page.tsx"
---

# Plan 21-06: Success Animation & Flow

## Objective

Create success animation and flow after breeding completes, with redirect to /eggs page. Implements D-07 from CONTEXT.md.

## Context

- Hatch animation pattern exists in HatchAnimation component (12 seconds)
- Breeding success should show animated confirmation similar to hatch
- Should display new egg details with parent info
- "View Egg" button redirects to /eggs page
- Reuses existing patterns from hatch-reveal-modal.tsx

## Tasks

### Task 1: Create BreedingSuccessModal component

<task id="21-06-01">
  <title>Create success modal with animation for breeding completion</title>
  <read_first>
    - apps/web/components/eggs/hatch-reveal-modal.tsx (success pattern)
    - apps/web/components/eggs/hatch-animation.tsx (animation reference)
  </read_first>
  <acceptance_criteria>
    - File exists at apps/web/components/breeding/BreedingSuccessModal.tsx
    - Props: open: boolean, onOpenChange: (open: boolean) => void, breedingResult: BreedingResult, parent1: AnimalData, parent2: AnimalData
    - Shows animated success message (heart/sparkle animation or similar)
    - Displays new egg token ID
    - Displays generation: "Gen X"
    - Displays parent info: species icons and IDs
    - "View Egg" button that routes to /eggs
    - Auto-redirect option after 5 seconds
  </acceptance_criteria>
  <action>
    Create apps/web/components/breeding/BreedingSuccessModal.tsx:
    - Import Dialog components from @/components/ui/dialog
    - Create animated success icon (hearts merging or sparkles)
    - Show "Breeding Successful!" heading
    - Display egg card preview with:
      - Egg image/icon
      - "Egg #{token_id}" 
      - "Gen {generation}" badge
      - "Born from: {parent1.species} #{parent1.animal_id} + {parent2.species} #{parent2.animal_id}"
    - "View Your Egg" button: onClick={() => router.push('/eggs')}
    - Optional: Countdown "Redirecting in 5... 4..."
    - Claymorphism styling consistent with hatch modal
  </action>
  <verify>
    <automated>grep -n "Breeding Successful\|View Your Egg\|router.push.*eggs" apps/web/components/breeding/BreedingSuccessModal.tsx | head -10</automated>
  </verify>
  <done>
    - File apps/web/components/breeding/BreedingSuccessModal.tsx exists
    - Component accepts required props
    - Shows animated success message
    - Displays egg token ID, generation, and parent info
    - "View Egg" button routes to /eggs
  </done>
</task>

### Task 2: Integrate success modal into BreedingDialog

<task id="21-06-02">
  <title>Update BreedingDialog to show success modal after breeding</title>
  <read_first>
    - apps/web/components/breeding/BreedingDialog.tsx (from 21-01)
    - apps/web/components/breeding/BreedingSuccessModal.tsx (from Task 1)
  </read_first>
  <acceptance_criteria>
    - BreedingDialog manages success modal state
    - After breedAnimals succeeds, shows BreedingSuccessModal
    - Passes breeding result and parent data to modal
    - Success modal replaces breeding dialog content
    - On close/redirect, calls onSuccess callback
  </acceptance_criteria>
  <action>
    Modify apps/web/components/breeding/BreedingDialog.tsx:
    - Add state: const [showSuccess, setShowSuccess] = useState(false)
    - Add state: const [breedingResult, setBreedingResult] = useState<BreedingResult | null>(null)
    - In handleBreed, on success:
      ```javascript
      const result = await breedAnimals({ parent1AnimalId, parent2AnimalId });
      if (result.success) {
        setBreedingResult(result.data);
        setShowSuccess(true);
      }
      ```
    - Render BreedingSuccessModal when showSuccess:
      ```javascript
      {showSuccess && breedingResult && (
        <BreedingSuccessModal
          open={showSuccess}
          onOpenChange={(open) => {
            setShowSuccess(open);
            if (!open) {
              onSuccess();
              onOpenChange(false);
            }
          }}
          breedingResult={breedingResult}
          parent1={selectedParent1Data}
          parent2={selectedParent2Data}
        />
      )}
      ```
  </action>
  <verify>
    <automated>grep -n "showSuccess\|BreedingSuccessModal\|setBreedingResult" apps/web/components/breeding/BreedingDialog.tsx | head -10</automated>
  </verify>
  <done>
    - BreedingDialog manages showSuccess state
    - Success modal shown after breedAnimals succeeds
    - BreedingResult and parent data passed to modal
    - onSuccess callback triggered on close/redirect
  </done>
</task>

### Task 3: Add auto-refresh on eggs page

<task id="21-06-03">
  <title>Ensure eggs page shows new breeding egg immediately after redirect</title>
  <read_first>
    - apps/web/app/eggs/page.tsx (current implementation)
    - apps/web/hooks/use-egg-poll.ts (refresh function)
  </read_first>
  <acceptance_criteria>
    - Eggs page refreshes data when navigating from breeding success
    - New breeding egg appears in list without manual refresh
    - Breeding egg has correct badge and parent info
  </acceptance_criteria>
  <action>
    Modify apps/web/app/eggs/page.tsx:
    - useEggPoll hook already has refresh functionality
    - Ensure onSuccess callback from BreedingDialog triggers refresh
    - Optional: Add query param ?newEgg={token_id} when redirecting
    - Optional: Scroll to or highlight new egg in list
    - Verify polling picks up new egg within 30 seconds
  </action>
  <verify>
    <automated>grep -n "refresh\|onSuccess" apps/web/app/eggs/page.tsx | head -10</automated>
  </verify>
  <done>
    - Eggs page refreshes when navigating from breeding
    - New breeding egg appears in list
    - Correct badge and parent info displayed
  </done>
</task>

### Task 4: Create simple animation component

<task id="21-06-04">
  <title>Create breeding success animation component</title>
  <read_first>
    - apps/web/components/eggs/hatch-animation.tsx (reference)
    - Tailwind animation classes
  </read_first>
  <acceptance_criteria>
    - File exists at apps/web/components/breeding/BreedingAnimation.tsx
    - Shows animated hearts or sparkles
    - Duration: 2-3 seconds (shorter than hatch)
    - Loops or completes once
    - Centered in modal
  </acceptance_criteria>
  <action>
    Create apps/web/components/breeding/BreedingAnimation.tsx:
    - Simple CSS animation using Tailwind
    - Multiple heart icons floating up and fading
    - Or: Two animal emojis with heart between them
    - Duration: 2-3 seconds
    - OnComplete callback when animation finishes
    - Example:
      ```jsx
      <div className="relative">
        <span className="animate-bounce text-6xl">🐔</span>
        <span className="animate-pulse text-4xl text-red-500">❤️</span>
        <span className="animate-bounce text-6xl">🐷</span>
      </div>
      ```
  </action>
  <verify>
    <automated>grep -n "animate-bounce\|animate-pulse\|heart\|❤️" apps/web/components/breeding/BreedingAnimation.tsx | head -10</automated>
  </verify>
  <done>
    - File apps/web/components/breeding/BreedingAnimation.tsx exists
    - Animation shows hearts or sparkles
    - Duration 2-3 seconds
    - Centered in modal
  </done>
</task>

## Verification

### Build Verification

- [ ] `cd apps/web && bun run build` exits 0

### Test Verification

- [ ] BreedingSuccessModal renders without errors
- [ ] Animation plays correctly

### Functional Verification

- [ ] After breeding, success modal appears
- [ ] Shows correct egg ID, generation, parent info
- [ ] "View Egg" button routes to /eggs
- [ ] New egg visible in eggs list
- [ ] Breeding egg has special badge

## must_haves

- Success animation + egg details (per D-07)
- Animated confirmation similar to hatch animation
- Display egg details with parent animals
- "View Egg" button routes to /eggs page (per D-07)
