---
plan_id: "21-04"
phase: 21
title: "Breeding Egg Display"
wave: 2
depends_on: ["21-01"]
autonomous: true
estimated_hours: 3
requirements_addressed: ["BREED-05", "BREED-07"]
files_modified:
  - "apps/web/components/eggs/egg-card.tsx"
  - "apps/web/hooks/use-egg-poll.ts"
  - "apps/web/app/eggs/page.tsx"
---

# Plan 21-04: Breeding Egg Display

## Objective
Display breeding eggs in /eggs page with special badge and parent info. Implements D-06 from CONTEXT.md.

## Context
- Breeding eggs have is_breeding_egg = true in egg_nfts collection
- Breeding eggs have parent1_animal_id and parent2_animal_id fields
- EggCard component displays regular eggs
- Need to show special badge and parent info on hover/tooltip

## Tasks

### Task 1: Extend EggData interface with breeding fields
<task id="21-04-01">
  <title>Add breeding fields to EggData interface</title>
  <read_first>
    - apps/web/hooks/use-egg-poll.ts (current EggData interface)
    - apps/backend/pb_migrations/1775399931499_create_egg_nfts.js (schema)
  </read_first>
  <acceptance_criteria>
    - EggData interface includes is_breeding_egg?: boolean
    - EggData interface includes parent1_animal_id?: number
    - EggData interface includes parent2_animal_id?: number
    - EggData interface includes generation?: number
    - Hook maps these fields from PocketBase records
  </acceptance_criteria>
  <action>
    Modify apps/web/hooks/use-egg-poll.ts:
    - Add to EggData interface:
      - is_breeding_egg?: boolean
      - parent1_animal_id?: number
      - parent2_animal_id?: number
      - generation?: number
    - In fetchEggs, map these fields from records
  </action>
  <verify>
    <automated>grep -n "is_breeding_egg\|parent1_animal_id\|parent2_animal_id\|generation" apps/web/hooks/use-egg-poll.ts | head -10</automated>
  </verify>
  <done>
    - EggData interface extended with breeding fields
    - Hook maps all fields from PocketBase records
    - TypeScript compilation passes
  </done>
</task>

### Task 2: Add breeding badge to EggCard
<task id="21-04-02">
  <title>Add breeding egg badge and styling to EggCard</title>
  <read_first>
    - apps/web/components/eggs/egg-card.tsx (current implementation)
    - apps/web/components/ui/badge.tsx (badge component)
  </read_first>
  <acceptance_criteria>
    - EggCard shows "Breeding Egg" badge when is_breeding_egg is true
    - Badge positioned in top-left of image section
    - Badge styling: bg-tertiary text-on-tertiary
    - Shows generation: "Gen X" next to badge
    - Different background gradient for breeding eggs (e.g., purple tint)
  </acceptance_criteria>
  <action>
    Modify apps/web/components/eggs/egg-card.tsx:
    - Add is_breeding_egg check to EggCardProps (from egg prop)
    - Add badge in image section: {egg.is_breeding_egg && (
      <Badge variant="clay" className="absolute top-2 left-2 bg-tertiary text-on-tertiary">
        <span className="material-symbols-outlined text-xs">favorite</span>
        Breeding Egg
      </Badge>
    )}
    - Add generation badge: {egg.generation > 0 && <Badge>Gen {egg.generation}</Badge>}
    - Optional: Different image styling for breeding eggs
  </action>
  <verify>
    <automated>grep -n "is_breeding_egg\|Breeding Egg\|Gen.*generation" apps/web/components/eggs/egg-card.tsx | head -10</automated>
  </verify>
  <done>
    - EggCard shows "Breeding Egg" badge when is_breeding_egg is true
    - Badge positioned in top-left
    - Generation badge displayed
    - Visual distinction for breeding eggs
  </done>
</task>

### Task 3: Create parent info tooltip component
<task id="21-04-03">
  <title>Create tooltip showing parent animal info</title>
  <read_first>
    - apps/web/components/ui/tooltip.tsx (if exists, or use title attribute)
    - apps/web/hooks/use-animal-poll.ts (AnimalData for parent lookup)
  </read_first>
  <acceptance_criteria>
    - Hovering on breeding egg shows tooltip with parent info
    - Tooltip shows: "Parent 1: Species #ID (Rarity)" 
    - Tooltip shows: "Parent 2: Species #ID (Rarity)"
    - Uses existing Tooltip component or native title
    - Fetches parent data asynchronously on hover (or includes in egg query)
  </acceptance_criteria>
  <action>
    Create apps/web/components/eggs/BreedingEggTooltip.tsx:
    - Props: parent1Id?: number, parent2Id?: number
    - Uses useAnimalPoll or direct PB query to fetch parent data
    - Shows loading state while fetching
    - Displays parent species, rarity, and animal_id
    - Or: Modify EggCard to show parent info inline on hover
    
    Alternative simpler approach:
    - Add title attribute to egg card: title={`Parent 1: ${parent1?.species} #${parent1?.animal_id}\nParent 2: ${parent2?.species} #${parent2?.animal_id}`}
  </action>
  <verify>
    <automated>grep -n "parent1\|parent2\|Parent 1\|Parent 2" apps/web/components/eggs/BreedingEggTooltip.tsx 2>/dev/null || grep -n "parent1_animal_id\|parent2_animal_id" apps/web/components/eggs/egg-card.tsx | head -5</automated>
  </verify>
  <done>
    - Tooltip component exists or parent info integrated into EggCard
    - Shows parent species and IDs
    - Works on hover for breeding eggs
  </done>
</task>

### Task 4: Update EggCard with parent info display
<task id="21-04-04">
  <title>Integrate parent info into EggCard for breeding eggs</title>
  <read_first>
    - apps/web/components/eggs/egg-card.tsx
    - apps/web/components/eggs/BreedingEggTooltip.tsx (from Task 3)
  </read_first>
  <acceptance_criteria>
    - Breeding eggs show parent info on hover
    - Parent info fetched from animal_nfts collection
    - Shows species icons and IDs for both parents
    - Graceful handling if parent data not found
  </acceptance_criteria>
  <action>
    Modify apps/web/components/eggs/egg-card.tsx:
    - Add state for parent data if using async fetch
    - Add useEffect to fetch parent animals when is_breeding_egg and parent IDs exist
    - Add hover handler or use Tooltip component
    - Display: "Born from: 🐔#5 + 🦆#8" or similar format
    - Position below rarity info or in tooltip
  </action>
  <verify>
    <automated>grep -n "Born from\|parent1\|parent2" apps/web/components/eggs/egg-card.tsx | head -10</automated>
  </verify>
  <done>
    - EggCard shows parent info for breeding eggs
    - Parent data fetched from animal_nfts
    - Species icons and IDs displayed
    - Graceful handling for missing parent data
  </done>
</task>

## Verification

### Build Verification
- [ ] `cd apps/web && bun run build` exits 0

### Test Verification
- [ ] EggCard renders with breeding badge
- [ ] Parent info displays correctly

### Functional Verification
- [ ] Breeding eggs show special badge in /eggs page
- [ ] Generation displayed for breeding eggs
- [ ] Parent info visible on hover/click
- [ ] Regular eggs unchanged

## must_haves
- Breeding eggs mixed in /eggs page with special badge (per D-06)
- Shows parent info on hover/tooltip (per D-06)
- Same feed/hatch flow as normal eggs (per D-06)
