---
phase: "40"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/dashboard/RecruitmentBonusCard.tsx
  - apps/web/components/ui/KYCStatusBadge.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can see their recruitment tier status and direct recruit count"
    - "User can claim recruitment bonus if eligible tier reached"
    - "User can see their KYC verification status at a glance"
    - "KYC badge shows different states: verified, pending, not verified, not required"
  artifacts:
    - path: "apps/web/components/dashboard/RecruitmentBonusCard.tsx"
      provides: "Recruitment tier display with claim button"
      min_lines: 80
    - path: "apps/web/components/ui/KYCStatusBadge.tsx"
      provides: "KYC status badge component with 4 states"
      min_lines: 40
  key_links:
    - from: "apps/web/components/dashboard/RecruitmentBonusCard.tsx"
      to: "/api/v2/recruitment-bonus-status"
      via: "fetch GET on mount"
      pattern: "fetch.*recruitment-bonus-status"
    - from: "apps/web/components/dashboard/RecruitmentBonusCard.tsx"
      to: "/api/v2/claim-recruitment-bonus"
      via: "fetch POST on claim click"
      pattern: "fetch.*claim-recruitment-bonus"
    - from: "apps/web/components/ui/KYCStatusBadge.tsx"
      to: "/api/v2/kyc-status"
      via: "fetch GET on mount (via hook or direct)"
      pattern: "kyc.*status"
---

<objective>
Create two small display components: RecruitmentBonusCard for dashboard tier status and KYCStatusBadge for KYC verification indicator.

Purpose: Wire up the backend APIs from Phases 33 and 36 to UI components so users can see their recruitment progress and KYC status.
Output: Two reusable components with claymorphism styling, hydration safety, and API integration.
</objective>

<execution_context>
@/Users/poom-work/tokenine/eggo-pocketbase/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/poom-work/tokenine/eggo-pocketbase/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-frontend-components/40-CONTEXT.md

@apps/web/components/dashboard/balance-card.tsx
@apps/web/components/dashboard/tier-section.tsx
@apps/web/components/ui/badge.tsx
@apps/web/components/ui/button.tsx
@apps/web/components/ui/card.tsx
@apps/web/hooks/use-is-hydrated.ts
@apps/backend/pb_hooks/33-recruitment-bonus.pb.js
@apps/backend/pb_hooks/36-burn-kyc-spend.pb.js

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/web/hooks/use-is-hydrated.ts:

```typescript
export function useIsHydrated(): boolean
```

From apps/backend/pb_hooks/33-recruitment-bonus.pb.js — GET /api/v2/recruitment-bonus-status response:

```typescript
interface RecruitmentBonusStatus {
  success: boolean
  data: {
    direct_recruits: number // count of direct recruits
    current_tier: number // 0-4, highest tier reached
    claimed_tier: number // 0-4, highest tier already claimed
    can_claim: boolean // current_tier > claimed_tier
    next_tier: {
      // null if at max tier
      tier: number
      min: number // recruits needed
      food: number
      usdt: number
    } | null
  }
}
```

From apps/backend/pb_hooks/33-recruitment-bonus.pb.js — POST /api/v2/claim-recruitment-bonus response:

```typescript
interface ClaimRecruitmentBonusResponse {
  success: boolean
  data: {
    tier: number // tier claimed (1-4)
    food_rewarded: number
    usdt_bonus: number
    direct_recruits: number
  }
}
```

From apps/backend/pb_hooks/36-burn-kyc-spend.pb.js — GET /api/v2/kyc-status response:

```typescript
interface KYCStatusResponse {
  success: boolean
  data: {
    kyc_verified: boolean
    kyc_required_globally: boolean
    can_withdraw: boolean
  }
}
```

Tier definitions (from 33-recruitment-bonus.pb.js):

```
Tier 1: 10 recruits    → 2 food × 2 = 2 food, $10 USDT
Tier 2: 100 recruits   → 1 food × 4 = 4 food, $20 USDT
Tier 3: 1,000 recruits → 1 food × 6 = 6 food, $30 USDT
Tier 4: 10,000 recruits→ 1 food × 10 = 10 food, $50 USDT
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RecruitmentBonusCard component</name>
  <files>apps/web/components/dashboard/RecruitmentBonusCard.tsx</files>
  <behavior>
    - Test 1: Component renders loading skeleton when fetching status
    - Test 2: Shows current tier label, direct recruit count, and progress to next tier
    - Test 3: Shows claim button when can_claim=true, disabled when can_claim=false
    - Test 4: Clicking claim calls POST /api/v2/claim-recruitment-bonus with auth token
    - Test 5: After successful claim, refreshes status and shows success toast
    - Test 6: Handles error states (network error, already claimed) with error message
    - Test 7: Hydration-safe — returns loading state until hydrated
  </behavior>
  <action>
Create RecruitmentBonusCard.tsx in apps/web/components/dashboard/.

Component requirements:

1. "use client" directive, uses useIsHydrated() for hydration safety
2. Fetches GET /api/v2/recruitment-bonus-status on mount (via useEffect after hydration)
3. Shows Claymorphism Card with:
   - Trophy icon (Lucide: Trophy) at top
   - Current tier label: "Tier {current_tier}" or "No tier yet" if current_tier=0
   - Direct recruit count: "{direct_recruits} direct recruits"
   - Progress indicator: "{direct_recruits}/{next_tier.min} to next tier" (or "Max tier reached!" if at tier 4)
   - Tier labels with colors: Tier 1=green, Tier 2=blue, Tier 3=purple, Tier 4=gold (use CSS variables --color-tier-1 through --color-tier-4)
4. Claim button (variant="clay", size="clay-md"):
   - Shows "Claim Tier {current_tier} Bonus" when can_claim=true
   - Shows "Claimed" (disabled) when can_claim=false
   - Shows "Need {next_tier.min - direct_recruits} more recruits" (disabled) when no tier reached
   - On click: calls POST /api/v2/claim-recruitment-bonus with Authorization header
   - Uses pb.authStore.token for auth header
   - After success: shows toast.success("Claimed! {food_rewarded} food + ${usdt_bonus} USDT"), refreshes status
   - On error: shows toast.error(error.message)
5. Loading state: clay-card with animate-pulse skeleton
6. Error state: Alert with error message and retry button
7. Uses PocketBase client from @/lib/pocketbase/client.ts for auth token access

Styling: claymorphism (clay-card class), Tailwind CSS 4, Material Symbols icons optional (prefer Lucide)

Do NOT:

- Access pb.authStore.record during SSR (use useIsHydrated first)
- Create API routes (static export incompatible)
- Hardcode API URLs (use relative paths or PocketBase client baseURL)
  </action>
  <verify>
  <automated>bun test apps/web/components/dashboard/RecruitmentBonusCard.test.tsx -x 2>/dev/null || echo "Test file not yet created — manual verification: component renders without errors"</automated>
  </verify>
  <done>
  RecruitmentBonusCard.tsx exists, renders tier status with claim button, calls correct API endpoints, handles loading/error/claimed states, uses claymorphism styling, hydration-safe.
  </done>
  </task>

<task type="auto" tdd="true">
  <name>Task 2: Create KYCStatusBadge component</name>
  <files>apps/web/components/ui/KYCStatusBadge.tsx</files>
  <behavior>
    - Test 1: Shows "Verified" badge (green, ShieldCheck) when kyc_verified=true
    - Test 2: Shows "KYC Required" badge (orange, ShieldAlert) when kyc_required_globally=true but kyc_verified=false
    - Test 3: Shows "Not Verified" badge (gray, ShieldX) when kyc_verified=false and not required
    - Test 4: Shows "Verified" badge (green) when kyc_required_globally=false (KYC not needed)
    - Test 5: Hydration-safe — returns null until hydrated
  </behavior>
  <action>
Create KYCStatusBadge.tsx in apps/web/components/ui/.

Component requirements:

1. "use client" directive, uses useIsHydrated() for hydration safety
2. Accepts optional props: { status?: KYCStatusData } — if not provided, fetches GET /api/v2/kyc-status on mount
3. Four display states using shadcn Badge component:
   - Verified (kyc_verified=true): variant="default" (green), icon=ShieldCheck, text="KYC Verified"
   - Required but not verified (kyc_required_globally=true && !kyc_verified): variant="secondary" (orange/amber), icon=ShieldAlert, text="KYC Required"
   - Not verified, not required: variant="outline" (gray), icon=ShieldX, text="Not Verified"
   - Not required globally: variant="outline" (muted), icon=Shield, text="KYC Not Required"
4. Uses Lucide icons: Shield, ShieldCheck, ShieldX, ShieldAlert (if ShieldAlert not available, use AlertTriangle)
5. Compact size — suitable for header, profile, or dashboard embedding
6. If fetching status: uses pb.authStore.token for Authorization header

Styling: shadcn Badge component, Lucide icons, compact sizing

Do NOT:

- Access browser APIs without hydration check
- Create new API endpoints (use existing /api/v2/kyc-status)
- Add KYC document upload flow (deferred idea)
  </action>
  <verify>
  <automated>bun test apps/web/components/ui/KYCStatusBadge.test.tsx -x 2>/dev/null || echo "Test file not yet created — manual verification: component renders without errors"</automated>
  </verify>
  <done>
  KYCStatusBadge.tsx exists, shows correct badge for all 4 KYC states, uses shadcn Badge + Lucide icons, hydration-safe, accepts optional status prop or fetches automatically.
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                   |
| ---------------------------- | ----------------------------------------------------------------------------- |
| Client → PocketBase API      | Auth token required for recruitment and KYC status endpoints                  |
| Claim button → POST endpoint | User can only claim their own bonus (server validates userId from auth token) |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                    |
| --------- | ---------------------- | -------------------------- | ----------- | -------------------------------------------------------------------------------------------------- |
| T-40-01   | Spoofing               | RecruitmentBonusCard claim | mitigate    | Auth token sent via Authorization header; server validates userId from token (not client-provided) |
| T-40-02   | Tampering              | KYCStatusBadge display     | mitigate    | Status fetched from server, not stored client-side; no client-side state mutation                  |
| T-40-03   | Information Disclosure | KYCStatusBadge             | accept      | Badge only shows verification status (boolean), no PII or document details exposed                 |

</threat_model>

<verification>
- Both components render without hydration errors
- RecruitmentBonusCard calls correct API endpoints with auth
- KYCStatusBadge shows correct state for all 4 scenarios
- Claymorphism styling consistent with existing components
- No TypeScript errors or lint failures
</verification>

<success_criteria>

- RecruitmentBonusCard.tsx created in apps/web/components/dashboard/ with tier display, claim button, and API integration
- KYCStatusBadge.tsx created in apps/web/components/ui/ with 4-state badge display
- Both components are hydration-safe with useIsHydrated()
- Both components use claymorphism styling consistent with existing design system
- No new API routes created (uses existing PocketBase hooks)
- Components can be imported and embedded in dashboard/profile pages
  </success_criteria>

<output>
After completion, create `.planning/phases/40-frontend-components/40-01-SUMMARY.md`
</output>
