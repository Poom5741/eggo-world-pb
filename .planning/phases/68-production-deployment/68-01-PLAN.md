---
phase: 68-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEPLOY_RUNBOOK.md
  - .planning/phases/68-production-deployment/SIGN-OFF.md
autonomous: false
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-03
must_haves:
  truths:
    - "wallet-api route GET /api/v1/admin/pool-balances is reachable from inside the production network and returns NOT_OWNER for non-owner wallets"
    - "PocketBase hook 39-pool-balance.pb.js is loaded after restart: log line 'Pool balance proxy endpoint registered: GET /api/v2/admin/pool-balances' appears in pocketbase container logs"
    - "Public URL https://pb.eggoworld.io/api/v2/admin/pool-balances?wallet=<addr> returns JSON shaped { success, data | error.code }"
    - "Frontend /admin/treasury route returns 2xx on the production hostname, with Cloudflare Pages deployed commit SHA matching the deploy branch HEAD"
    - "End-to-end admin journey passes: sign in → /admin/treasury → connect MetaMask → ownership grid (6 contracts) renders → pool balance card renders → withdrawal form reaches gas estimation"
    - "No real treasury withdrawal tx is broadcast during verification — MetaMask prompt is rejected"
    - "Documentation evidence is captured: docs/DEPLOY_RUNBOOK.md gains an 'Admin Treasury (v0.10.0) Deployment' section, and SIGN-OFF.md is filled in and committed"
    - "No application code is modified by this phase (zero changes under apps/, wallet-api/, contracts/)"
  artifacts:
    - path: "docs/DEPLOY_RUNBOOK.md"
      provides: "Operator-facing runbook section for v0.10.0 admin treasury deployment"
      contains: "## Admin Treasury (v0.10.0) Deployment"
    - path: ".planning/phases/68-production-deployment/SIGN-OFF.md"
      provides: "Deployment sign-off doc capturing pre-flight, per-layer evidence, E2E results, and approval"
      contains: "Phase 68 Deployment Sign-Off"
  key_links:
    - from: "Frontend /admin/treasury (Cloudflare Pages)"
      to: "PocketBase https://pb.eggoworld.io/api/v2/admin/pool-balances"
      via: "fetch from usePoolBalances hook"
      pattern: "pool-balances"
    - from: "PocketBase hook 39-pool-balance.pb.js"
      to: "wallet-api http://wallet-api:3001/api/v1/admin/pool-balances"
      via: "$http.send proxy"
      pattern: "WALLET_SRV_URL"
    - from: "wallet-api /api/v1/admin/pool-balances"
      to: "CommissionDistribution 0x18b486086f4414500398276766697ad0fc1a43cf on BSC mainnet"
      via: "ethers.JsonRpcProvider + commissionBalances() read"
      pattern: "commissionBalances"
---

# Phase 68: Production Deployment & Verification — Plan

**Created:** 2026-05-26
**Planner:** orchestrator (inline; Task tool unavailable in this runtime)
**Research:** 68-RESEARCH.md (HIGH confidence)
**Context:** 68-CONTEXT.md

<objective>
Deploy and verify the v0.10.0 Admin Treasury surface (Phases 64–67) on production. Phase 68 ships no application code. It produces (a) an updated `docs/DEPLOY_RUNBOOK.md` section, (b) a per-deployment `SIGN-OFF.md` with captured evidence, and (c) explicit verification that all three layers (wallet-api, PocketBase hook `39-pool-balance.pb.js`, frontend `/admin/treasury`) are live on `pb.eggoworld.io` + Cloudflare Pages against BSC mainnet (chain 56).

Purpose: close the v0.10.0 milestone by proving the production deployment works end-to-end and producing the operator artifacts (runbook + sign-off) needed for future redeploys and audits.
Output: docs/DEPLOY_RUNBOOK.md (extended), SIGN-OFF.md (new), and a clean production state with all three DEPL requirements verified.
</objective>

<execution_context>
@.planning/phases/68-production-deployment/68-CONTEXT.md
@.planning/phases/68-production-deployment/68-RESEARCH.md
@docs/DEPLOY_RUNBOOK.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@apps/backend/pb_hooks/39-pool-balance.pb.js
@apps/backend/docker-compose.yml
@apps/web/app/admin/treasury/page.tsx
@apps/web/.env.production
@contracts/contract-addresses.json

# Cross-cutting truths

- No real withdrawals during verification (reject in MetaMask)
- Read actual filenames and log strings — ROADMAP text drifted from shipped code
- All tasks are operator-driven (SSH or browser) — autonomous: false on every task
- Sequential layer order per D-32: wallet-api → PocketBase hook → frontend → E2E
  </context>

<tasks>

<task type="manual">
  <name>Task 1: Append "Admin Treasury (v0.10.0) Deployment" section to docs/DEPLOY_RUNBOOK.md</name>
  <files>docs/DEPLOY_RUNBOOK.md</files>
  <read_first>
    <file>docs/DEPLOY_RUNBOOK.md</file>
    <file>.planning/phases/68-production-deployment/68-CONTEXT.md</file>
    <file>.planning/phases/68-production-deployment/68-RESEARCH.md</file>
    <file>apps/backend/pb_hooks/39-pool-balance.pb.js</file>
    <file>apps/backend/docker-compose.yml</file>
  </read_first>
  <action>
Append a new `## Admin Treasury (v0.10.0) Deployment` section to `docs/DEPLOY_RUNBOOK.md` (do NOT rewrite earlier sections). The section must contain these subsections in this order, each as a level-3 heading:

1. `### Scope` — one paragraph summarizing what ships (Phases 64–67) and what does not (no DB migration, no contract redeploy, no new env vars).
2. `### Pre-flight checklist` — bullet list:
   - Confirm BscScan: `CommissionDistribution.owner()` and `.pendingOwner()` for chain-56 address `0x18b486086f4414500398276766697ad0fc1a43cf` match the admin MetaMask wallet to be used.
   - SSH into prod host, run `docker compose -f apps/backend/docker-compose.yml exec wallet-api env | grep -E "^(RPC_URL|CHAIN_ID|NODE_ENV)="` and confirm `CHAIN_ID=56`.
   - Confirm `git rev-parse HEAD` on the deploy branch matches the SHA intended to ship.
3. `### Step-by-step deployment` — numbered list covering sequential order from D-32:
   1. wallet-api: `git pull` on prod host, then `docker compose -f apps/backend/docker-compose.yml up -d --build wallet-api`; wait for `docker compose ps wallet-api` to show `Up (healthy)`.
   2. wallet-api smoke test from inside the network: `docker compose -f apps/backend/docker-compose.yml exec pocketbase wget -qO- "http://wallet-api:3001/api/v1/admin/pool-balances?wallet=0x0000000000000000000000000000000000000000"` — expect JSON containing `"NOT_OWNER"`.
   3. PocketBase hook: `docker compose -f apps/backend/docker-compose.yml exec pocketbase ls /pb_hooks | grep 39-pool-balance.pb.js` to confirm mount, then `docker compose -f apps/backend/docker-compose.yml restart pocketbase`.
   4. PocketBase hook log check: `docker compose -f apps/backend/docker-compose.yml logs --tail=400 pocketbase | grep -F "Pool balance proxy endpoint registered"`. The actual log line is `Pool balance proxy endpoint registered: GET /api/v2/admin/pool-balances` (verbatim, from `apps/backend/pb_hooks/39-pool-balance.pb.js:60`).
   5. PocketBase public reachability: `curl -i "https://pb.eggoworld.io/api/v2/admin/pool-balances?wallet=0x0000000000000000000000000000000000000000"` returns JSON containing `"code":"NOT_OWNER"`.
   6. Frontend: push the deploy branch to the Cloudflare-Pages-connected branch; verify in Cloudflare dashboard that the new deployment commit SHA matches local `git rev-parse HEAD`.
   7. Frontend reachability: `curl -I https://eggoworld.io/admin/treasury` returns 2xx/3xx (not 404 / 5xx).
4. `### Troubleshooting` (per D-37 and D-40):
   - "Hook log line missing after restart" → confirm volume mount lists `39-pool-balance.pb.js`, confirm file is valid JS, re-restart.
   - "wallet-api returns RPC error" → check `RPC_URL` env, fall back to `https://bsc-dataseed1.defibit.io`.
   - "Wrong network badge in UI" → user must switch MetaMask to BSC Mainnet (chainId 56).
   - "Cloudflare deployed wrong commit" → check connected branch in dashboard, trigger manual rebuild.
5. `### Rollback` (per D-34, document but do not execute):
   - Identify previous good commit SHA: `git log --oneline -n 5`.
   - Frontend rollback: `git revert <sha> && git push <deploy-branch>` triggers Cloudflare auto-redeploy.
   - wallet-api rollback: `git revert <sha> && git push`, then `docker compose up -d --build wallet-api` on prod.
   - PocketBase hook rollback: remove `39-pool-balance.pb.js` from `apps/backend/pb_hooks/` and `docker compose restart pocketbase`.

Do not modify existing content of the runbook. New section anchor must be `## Admin Treasury (v0.10.0) Deployment` so it can be linked from SIGN-OFF.md.
</action>
<verify>
<automated>node -e "const fs=require('fs'); const t=fs.readFileSync('docs/DEPLOY_RUNBOOK.md','utf8'); const ok = t.includes('## Admin Treasury (v0.10.0) Deployment') && t.includes('Pool balance proxy endpoint registered: GET /api/v2/admin/pool-balances') && t.includes('39-pool-balance.pb.js') && t.includes('### Scope') && t.includes('### Pre-flight checklist') && t.includes('### Step-by-step deployment') && t.includes('### Troubleshooting') && t.includes('### Rollback'); console.log(ok ? 'PASS' : 'FAIL')"</automated>
</verify>
<done>

- docs/DEPLOY_RUNBOOK.md contains the level-2 heading `## Admin Treasury (v0.10.0) Deployment`
- Section contains five level-3 subsections in order: Scope, Pre-flight checklist, Step-by-step deployment, Troubleshooting, Rollback
- Step-by-step list cites the actual hook log string `Pool balance proxy endpoint registered: GET /api/v2/admin/pool-balances` verbatim
- Step-by-step list cites the actual hook filename `39-pool-balance.pb.js`
- No prior runbook content was deleted (line count after edit > before edit)
  </done>
  </task>

<task type="manual">
  <name>Task 2: Create deployment SIGN-OFF.md skeleton in the phase directory</name>
  <files>.planning/phases/68-production-deployment/SIGN-OFF.md</files>
  <read_first>
    <file>.planning/phases/68-production-deployment/68-CONTEXT.md</file>
    <file>.planning/phases/68-production-deployment/68-RESEARCH.md</file>
    <file>docs/DEPLOY_RUNBOOK.md</file>
  </read_first>
  <action>
Create `.planning/phases/68-production-deployment/SIGN-OFF.md` with the skeleton sections below. Operator will fill in actual values during Tasks 3–7. Use empty markdown checkboxes (`- [ ]`) on every check item so the operator can tick them as evidence is captured.

Required structure (every heading must be present, in this order):

- `# Phase 68 Deployment Sign-Off — v0.10.0 Admin Treasury`
- Frontmatter table with rows: `Deploy date`, `Operator`, `Deploy branch`, `Deploy commit SHA`, `Production PocketBase URL` (pre-fill `https://pb.eggoworld.io`), `Production frontend URL` (operator fills), `BSC chain ID` (pre-fill `56`), `CommissionDistribution address` (pre-fill `0x18b486086f4414500398276766697ad0fc1a43cf`).
- `## 1. Pre-flight Evidence` — empty checkboxes covering BscScan owner, BscScan pendingOwner, wallet-api env redacted dump, deploy branch SHA match.
- `## 2. wallet-api Deployment (DEPL-01)` — empty checkboxes for build, health, internal smoke test; reserved space for the JSON response snippet.
- `## 3. PocketBase Hook Deployment (DEPL-02)` — empty checkboxes for mount confirmation, restart, log line capture, public reachability; reserved space for the exact log line captured and the curl response.
- `## 4. Frontend Deployment (DEPL-03)` — empty checkboxes for Cloudflare commit SHA match, `/admin/treasury` HEAD response, browser render confirmation.
- `## 5. End-to-End Verification` — empty checkboxes mirroring the 8-step E2E task (admin login → page nav → MetaMask connect → ownership grid → pool balance card → withdrawal form gas estimate → reject in MetaMask → network 200 confirmed → no console errors).
- `## 6. Documentation Drift Captured` — pre-filled with three drift bullets:
  - ROADMAP filename `admin-pool-balance.pb.js` vs actual `39-pool-balance.pb.js`
  - ROADMAP log phrase `endpoint registered` vs actual `Pool balance proxy endpoint registered: GET /api/v2/admin/pool-balances`
  - REQUIREMENTS BACK-01 route `/api/v2/admin/pool/balance` vs actual `/api/v2/admin/pool-balances`
- `## 7. Issues Found & Resolution` (empty list, operator fills).
- `## 8. Rollback Status` — pre-fill `Not executed — verification passed` (operator updates if rollback ran).
- `## 9. Approval` — `Approver`, `Approval date`, `Signature / commit reference` (operator fills).

Sections 1–5 must collectively contain at least 15 empty checkboxes.
</action>
<verify>
<automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/phases/68-production-deployment/SIGN-OFF.md','utf8'); const headings=(t.match(/^## /gm)||[]).length; const boxes=(t.match(/^- \[ \]/gm)||[]).length; const hasAddr=t.includes('0x18b486086f4414500398276766697ad0fc1a43cf'); const hasDrift1=t.includes('39-pool-balance.pb.js'); const hasDrift2=t.includes('Pool balance proxy endpoint registered'); const hasDrift3=t.includes('/api/v2/admin/pool-balances'); console.log((headings>=9 && boxes>=15 && hasAddr && hasDrift1 && hasDrift2 && hasDrift3) ? 'PASS' : 'FAIL ('+headings+' headings, '+boxes+' boxes)')"</automated>
</verify>
<done>

- File `.planning/phases/68-production-deployment/SIGN-OFF.md` exists
- Nine `## ` headings (Sections 1–9) present in order
- At least 15 empty `- [ ]` checkboxes across sections 1–5
- CommissionDistribution chain-56 address `0x18b486086f4414500398276766697ad0fc1a43cf` appears in the frontmatter table
- Section 6 contains the three documentation-drift bullets pre-filled verbatim
  </done>
  </task>

<task type="manual">
  <name>Task 3: Pre-flight verification on production host (operator/SSH) — DEPL-01,02,03</name>
  <files>.planning/phases/68-production-deployment/SIGN-OFF.md</files>
  <read_first>
    <file>docs/DEPLOY_RUNBOOK.md</file>
    <file>.planning/phases/68-production-deployment/SIGN-OFF.md</file>
    <file>contracts/contract-addresses.json</file>
  </read_first>
  <action>
Operator performs the pre-flight steps from the runbook's `Pre-flight checklist` and records evidence into `SIGN-OFF.md § 1`. Specifically:

1. Open BscScan for `0x18b486086f4414500398276766697ad0fc1a43cf` on BSC mainnet. Read `owner()` and `pendingOwner()` via the Contract → Read tab. Record both addresses in SIGN-OFF.md § 1. Confirm one of them matches the admin MetaMask address that will be used.
2. SSH into the production host. Run `docker compose -f apps/backend/docker-compose.yml exec wallet-api env | grep -E '^(RPC_URL|CHAIN_ID|NODE_ENV)='` and paste the redacted output into SIGN-OFF.md § 1 (mask any secret values). Confirm `CHAIN_ID=56`.
3. On the prod host, `git -C <path-to-repo> rev-parse HEAD` on the deploy branch; record the SHA in the SIGN-OFF.md frontmatter `Deploy commit SHA` field.
4. Confirm the deploy-branch HEAD matches the local commit you intend to ship; if not, abort and reconcile before continuing.

If `CHAIN_ID` is not `56` or the admin wallet does not match either `owner()` or `pendingOwner()`, mark the task BLOCKED in SIGN-OFF.md `## 7. Issues Found & Resolution` and stop the workflow. Do not continue to Task 4.
</action>
<verify>
<automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/phases/68-production-deployment/SIGN-OFF.md','utf8'); const m=t.match(/Deploy commit SHA[^\n|]_\|\s_([0-9a-fA-F]{7,40})/); const hasChain=t.includes('CHAIN_ID=56'); console.log((m && hasChain) ? 'PASS' : 'FAIL (sha='+(m?m[1]:'missing')+', chain='+hasChain+')')"</automated>
</verify>
<done>

- SIGN-OFF.md § 1 has all checkboxes ticked OR every unticked box has a matching BLOCKED entry in § 7
- `CHAIN_ID=56` appears verbatim in the recorded env output in § 1
- `Deploy commit SHA` field in the frontmatter is populated with a 7–40 char hex SHA
- Admin MetaMask address recorded in § 1 matches either `owner()` or `pendingOwner()` per BscScan
  </done>
  </task>

<task type="manual">
  <name>Task 4: Build & deploy wallet-api container to production, confirm reachable — DEPL-01</name>
  <files>.planning/phases/68-production-deployment/SIGN-OFF.md</files>
  <read_first>
    <file>docs/DEPLOY_RUNBOOK.md</file>
    <file>apps/backend/docker-compose.yml</file>
    <file>.planning/phases/68-production-deployment/SIGN-OFF.md</file>
  </read_first>
  <action>
Operator follows runbook step 1–2:

1. SSH into prod host; `git pull` on the deploy branch.
2. `docker compose -f apps/backend/docker-compose.yml up -d --build wallet-api`.
3. Wait until `docker compose -f apps/backend/docker-compose.yml ps wallet-api` shows the container `Up (healthy)`. If it never reaches healthy after 2 minutes, capture the last 200 log lines into SIGN-OFF.md § 7 and stop.
4. Internal smoke test:
   `docker compose -f apps/backend/docker-compose.yml exec pocketbase wget -qO- "http://wallet-api:3001/api/v1/admin/pool-balances?wallet=0x0000000000000000000000000000000000000000"`
5. Capture the JSON response in SIGN-OFF.md § 2. It MUST contain the substring `"code":"NOT_OWNER"` — that proves the route is registered and ownership-checking is active.
6. Optional but recommended: rerun the smoke test with the real admin wallet address; the response MUST be `success: true` with `data.treasury` and `data.coinstor` present. Capture the response in SIGN-OFF.md § 2 (numeric balances can be redacted if sensitive).
7. Tick all wallet-api checkboxes in SIGN-OFF.md § 2.

Do not modify any wallet-api source code in this task — Phase 68 ships zero code.
</action>
<verify>
<automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/phases/68-production-deployment/SIGN-OFF.md','utf8'); const hasNotOwner=t.includes('\"code\":\"NOT_OWNER\"') || t.includes('code\":\"NOT_OWNER'); const sec=t.split(/^## 2\./m)[1]||''; const sec3=sec.split(/^## /m)[0]||''; const unchecked=(sec3.match(/^- \[ \]/gm)||[]).length; console.log((hasNotOwner && unchecked===0) ? 'PASS' : 'FAIL (notOwner='+hasNotOwner+', unchecked='+unchecked+')')"</automated>
</verify>
<done>

- Production `docker compose ps wallet-api` shows status `Up (healthy)` (recorded in SIGN-OFF § 2)
- Smoke-test JSON response with zero address contains `"code":"NOT_OWNER"`
- SIGN-OFF.md § 2 has every checkbox ticked
- No file under `wallet-api/` was modified by this task
  </done>
  </task>

<task type="manual">
  <name>Task 5: Restart PocketBase, confirm pool-balance hook loaded and publicly reachable — DEPL-02</name>
  <files>.planning/phases/68-production-deployment/SIGN-OFF.md</files>
  <read_first>
    <file>apps/backend/pb_hooks/39-pool-balance.pb.js</file>
    <file>apps/backend/docker-compose.yml</file>
    <file>docs/DEPLOY_RUNBOOK.md</file>
    <file>.planning/phases/68-production-deployment/SIGN-OFF.md</file>
  </read_first>
  <action>
Operator follows runbook step 3–5:

1. Confirm mount: `docker compose -f apps/backend/docker-compose.yml exec pocketbase ls /pb_hooks | grep 39-pool-balance.pb.js` — must print `39-pool-balance.pb.js`. If missing, abort and check the volume mount in `apps/backend/docker-compose.yml` (`./pb_hooks:/pb_hooks`).
2. Restart PocketBase: `docker compose -f apps/backend/docker-compose.yml restart pocketbase`.
3. Wait for the PocketBase health check to pass (compose `healthcheck` interval is 5s). Then read logs:
   `docker compose -f apps/backend/docker-compose.yml logs --tail=400 pocketbase | grep -F "Pool balance proxy endpoint registered"`
4. The output MUST contain the line `Pool balance proxy endpoint registered: GET /api/v2/admin/pool-balances` (from `apps/backend/pb_hooks/39-pool-balance.pb.js:60`). Paste the matching line verbatim into SIGN-OFF.md § 3.
5. Public reachability test:
   `curl -i "https://pb.eggoworld.io/api/v2/admin/pool-balances?wallet=0x0000000000000000000000000000000000000000"`
   The response body MUST contain `"code":"NOT_OWNER"` (the hook forwards wallet-api's status code via `response.status_code || 200`). Capture status code + body in SIGN-OFF.md § 3.
6. Tick all PocketBase checkboxes in SIGN-OFF.md § 3.

If the log line never appears, the hook did not load. Capture the last 200 log lines, follow the runbook's troubleshooting bullets, and re-restart. If still failing, mark BLOCKED in SIGN-OFF § 7 and stop.
</action>
<verify>
<automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/phases/68-production-deployment/SIGN-OFF.md','utf8'); const hasLog=t.includes('Pool balance proxy endpoint registered: GET /api/v2/admin/pool-balances'); const hasNotOwner=t.includes('NOT_OWNER'); const sec=t.split(/^## 3\./m)[1]||''; const sec3=sec.split(/^## /m)[0]||''; const unchecked=(sec3.match(/^- \[ \]/gm)||[]).length; console.log((hasLog && hasNotOwner && unchecked===0) ? 'PASS' : 'FAIL (log='+hasLog+', notOwner='+hasNotOwner+', unchecked='+unchecked+')')"</automated>
</verify>
<done>

- The exact log line `Pool balance proxy endpoint registered: GET /api/v2/admin/pool-balances` is captured verbatim in SIGN-OFF § 3
- Public `curl https://pb.eggoworld.io/api/v2/admin/pool-balances?wallet=0x0000...` response body contains `"code":"NOT_OWNER"`
- SIGN-OFF.md § 3 has every checkbox ticked
- No file under `apps/backend/pb_hooks/` was modified by this task
  </done>
  </task>

<task type="manual">
  <name>Task 6: Push deploy branch, confirm Cloudflare Pages built correct SHA, verify /admin/treasury loads — DEPL-03</name>
  <files>.planning/phases/68-production-deployment/SIGN-OFF.md</files>
  <read_first>
    <file>apps/web/.env.production</file>
    <file>apps/web/app/admin/treasury/page.tsx</file>
    <file>.planning/phases/68-production-deployment/SIGN-OFF.md</file>
  </read_first>
  <action>
Operator follows runbook step 6–7:

1. From local clone: `git push <remote> <deploy-branch>` (the Cloudflare-Pages-connected branch confirmed in Task 3 § 1). If the branch was already pushed earlier, skip the push.
2. Open the Cloudflare Pages dashboard for the project. Wait for the latest build to finish with status `Success`.
3. Confirm the build's commit SHA in Cloudflare matches the SHA recorded in SIGN-OFF.md frontmatter `Deploy commit SHA`. Paste the Cloudflare deployment URL into SIGN-OFF.md § 4.
4. Run `curl -I https://eggoworld.io/admin/treasury` (or the actual production hostname — confirm with operator). Expect HTTP/2 200 (or 308 → 200). Capture status code in SIGN-OFF.md § 4.
5. In a real browser:
   - Sign in to the PocketBase admin (Google OAuth).
   - Navigate to `/admin/treasury`.
   - Confirm page header reads "Admin Treasury" and MetaMask Connection card is visible.
   - Open browser devtools → Console; confirm no red error logs on initial load (warnings OK).
6. Tick all frontend checkboxes in SIGN-OFF.md § 4. Record the browser, OS, and any visual anomalies in § 4.

Do not modify frontend code in this task. If Cloudflare's built SHA does not match, troubleshoot (wrong branch connected, cached build) before continuing.
</action>
<verify>
<automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/phases/68-production-deployment/SIGN-OFF.md','utf8'); const sec=t.split(/^## 4\./m)[1]||''; const sec3=sec.split(/^## /m)[0]||''; const unchecked=(sec3.match(/^- \[ \]/gm)||[]).length; const hasCurl=/HTTP\/[12](.[01])?\s+2\d\d/.test(sec3) || /\b200\b/.test(sec3) || /\b30[18]\b/.test(sec3); console.log((unchecked===0 && hasCurl) ? 'PASS' : 'FAIL (unchecked='+unchecked+', hasCurlStatus='+hasCurl+')')"</automated>
</verify>
<done>

- Cloudflare Pages dashboard shows latest deployment status `Success` with commit SHA matching SIGN-OFF frontmatter
- `curl -I https://eggoworld.io/admin/treasury` returned 2xx (or 3xx redirect to 2xx) and the status code is recorded in § 4
- SIGN-OFF.md § 4 records production URL, Cloudflare deployment URL, curl status code, and at least one browser/OS pair tested
- SIGN-OFF.md § 4 has every checkbox ticked
- No file under `apps/web/` was modified by this task
  </done>
  </task>

<task type="manual">
  <name>Task 7: End-to-end admin treasury journey on production (no real withdrawals) — DEPL-01,02,03</name>
  <files>.planning/phases/68-production-deployment/SIGN-OFF.md</files>
  <read_first>
    <file>apps/web/app/admin/treasury/page.tsx</file>
    <file>apps/web/components/admin/ContractOwnershipGrid.tsx</file>
    <file>apps/web/components/admin/treasury/TreasuryWithdrawalSection.tsx</file>
    <file>apps/web/hooks/use-pool-balances.ts</file>
    <file>apps/web/hooks/use-treasury-withdrawal.ts</file>
    <file>.planning/phases/68-production-deployment/SIGN-OFF.md</file>
  </read_first>
  <action>
Run the full E2E journey on production (`https://eggoworld.io/admin/treasury` or the verified hostname). For each numbered step, capture a screenshot or short note as evidence in SIGN-OFF.md § 5:

1. Sign in to PocketBase admin (Google OAuth flow).
2. Navigate to `/admin/treasury`. Confirm page shell loads (header, MetaMask Connection card, two `TreasuryGuard`-wrapped sections).
3. Click "Connect MetaMask". Approve in MetaMask. Confirm:
   - Connected wallet address (truncated `6...4`) appears in the connection card.
   - Network badge shows `BSC Mainnet` (green); if MetaMask is on the wrong chain, click `Switch Network` and confirm the badge turns green.
4. Confirm Contract Ownership grid renders 6 cards (CommissionDistribution, EggNFT, FoodNFT, AnimalNFT, Marketplace, TierBadge). Each card shows the current owner address. CommissionDistribution shows "Accept Ownership" button only if the connected wallet matches `pendingOwner()`.
5. Confirm Pool Balance Card renders Total / Treasury / CoinStor with `$X,XXX.XX USDT` formatting. Click the refresh button; balances re-fetch (skeleton placeholder briefly visible).
6. In the Withdrawal Form:
   - Enter `0.01` into the amount input.
   - Confirm real-time validation does NOT show an error (assuming treasury > 0.01).
   - Confirm Withdraw button is enabled only if ownership was accepted (per WDRW-04); if not yet accepted, the button is disabled with the Phase-66-link tooltip — that is expected and is a PASS.
   - If button is enabled, click Withdraw. MetaMask MUST pop up showing the transaction. **DO NOT CLICK CONFIRM IN METAMASK.** Click "Reject" in MetaMask.
   - Confirm the page handles rejection gracefully (no white screen; error alert or no-op).
7. Open browser devtools Network tab. Confirm at least one request to `https://pb.eggoworld.io/api/v2/admin/pool-balances?wallet=<your-addr>` returned 200 during the session.
8. Open browser devtools Console. Confirm no red error logs during the full journey (warnings about MetaMask reject are OK).

Tick every checkbox in SIGN-OFF.md § 5. Record any unexpected behavior in § 7.
</action>
<verify>
<automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/phases/68-production-deployment/SIGN-OFF.md','utf8'); const sec=t.split(/^## 5\./m)[1]||''; const sec3=sec.split(/^## /m)[0]||''; const unchecked=(sec3.match(/^- \[ \]/gm)||[]).length; const ticked=(sec3.match(/^- \[x\]/gmi)||[]).length; console.log((unchecked===0 && ticked>=7) ? 'PASS' : 'FAIL (unchecked='+unchecked+', ticked='+ticked+')')"</automated>
</verify>
<done>

- SIGN-OFF.md § 5 has every checkbox ticked OR every unticked box has a matching § 7 entry
- No transaction was submitted to BSC mainnet (confirmed by inspecting MetaMask activity log — no new outgoing tx with treasury withdrawal selector)
- At least one `/api/v2/admin/pool-balances` 200 response is recorded in § 5
- Console-error count during the full journey is 0 (warnings allowed); the count is recorded in § 5
  </done>
  </task>

<task type="manual">
  <name>Task 8: Finalize SIGN-OFF.md, capture approval, commit Phase 68 artifacts — DEPL-01,02,03</name>
  <files>
    .planning/phases/68-production-deployment/SIGN-OFF.md
    docs/DEPLOY_RUNBOOK.md
  </files>
  <read_first>
    <file>.planning/phases/68-production-deployment/SIGN-OFF.md</file>
    <file>docs/DEPLOY_RUNBOOK.md</file>
    <file>.planning/STATE.md</file>
  </read_first>
  <action>
1. Re-read SIGN-OFF.md end-to-end. Confirm every checkbox in §§ 1–5 is ticked (or has a § 7 BLOCKED/RESOLVED entry).
2. Fill in § 8 `Rollback Status`:
   - If verification passed without rollback: leave the pre-filled `Not executed — verification passed`.
   - If rollback was executed: replace with the SHA reverted to, the cmd used, and the post-rollback verification result.
3. Fill in § 9 `Approval`:
   - `Approver`: single approver name (per D-46).
   - `Approval date`: YYYY-MM-DD.
   - `Signature / commit reference`: leave blank for now; will be the commit SHA after this task lands.
4. Commit Phase 68 artifacts as a single atomic commit:
   `git add docs/DEPLOY_RUNBOOK.md .planning/phases/68-production-deployment/SIGN-OFF.md .planning/phases/68-production-deployment/68-RESEARCH.md .planning/phases/68-production-deployment/68-01-PLAN.md`
   `git commit -m "docs(68): production deployment runbook + v0.10.0 sign-off"`
5. After the commit lands, paste the new commit SHA into SIGN-OFF.md § 9 `Signature / commit reference` and create a follow-up commit `docs(68): record sign-off commit SHA`.
6. Do not push or merge — that is operator discretion outside this phase.
  </action>
  <verify>
  <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/phases/68-production-deployment/SIGN-OFF.md','utf8'); const sec9=t.split(/^## 9\./m)[1]||''; const hasApprover=/Approver[^\n|]*\|[^\n|]+\S/.test(sec9); const hasDate=/Approval date[^\n|]*\|[^\n|]+\S/.test(sec9) || /\d{4}-\d{2}-\d{2}/.test(sec9); const hasSig=/Signature[^\n|]*\|[^\n|]+\S/.test(sec9) || /[0-9a-f]{7,40}/.test(sec9); console.log((hasApprover && hasDate && hasSig) ? 'PASS' : 'FAIL (approver='+hasApprover+', date='+hasDate+', sig='+hasSig+')')"</automated>
  </verify>
  <done>
- SIGN-OFF.md § 9 contains populated `Approver`, `Approval date`, and `Signature / commit reference`
- `git log -n 5 --oneline -- docs/DEPLOY_RUNBOOK.md .planning/phases/68-production-deployment/SIGN-OFF.md` shows a commit whose message starts with `docs(68):` and modifies both files
- SIGN-OFF.md has no remaining `- [ ]` checkboxes in §§ 1–5 (or every unticked box is documented in § 7)
- `git status .planning/phases/68-production-deployment/` is clean (no untracked or modified files after the commit + amendment)
  </done>
</task>

</tasks>

---

## Decision Coverage (CONTEXT.md → plan tasks)

Every locked decision in `68-CONTEXT.md` is implemented by one or more tasks above. This list exists so the post-planning gap analysis sees explicit token coverage:

- **D-31** (auto Cloudflare frontend deploy on git push) → Task 6 (push triggers Cloudflare Pages build).
- **D-32** (sequential wallet-api → PocketBase → frontend) → Tasks 4 → 5 → 6 (wave ordering).
- **D-33** (deploy immediately, no maintenance window) → Tasks 4–7 (no scheduled-window step).
- **D-34** (git-based rollback) → Task 1 documents rollback procedure in runbook §Rollback.
- **D-35** (verify existing production config, do not reconfigure) → Task 3 pre-flight env-var check.
- **D-36** (SSH-based `.env` verification) → Task 3 step 2 (`docker compose exec wallet-api env | grep …`).
- **D-37** (configuration troubleshooting documentation) → Task 1 runbook §Troubleshooting.
- **D-38** (no database migrations needed) → All tasks omit any migration step; objective explicitly states zero DB migration.
- **D-39** (verify hook loaded via log line) → Task 5 step 4 captures the registration log line.
- **D-40** (PocketBase hook troubleshooting documentation) → Task 1 runbook §Troubleshooting bullets.
- **D-41** (comprehensive E2E testing) → Task 7 (8-step admin journey).
- **D-42** (test directly on production) → Task 7 runs against `https://eggoworld.io/admin/treasury`; no staging.
- **D-43** (existing admin credentials) → Task 7 step 1 uses existing PocketBase admin Google OAuth.
- **D-44** (E2E verification checklist) → Task 2 creates SIGN-OFF § 5 checklist; Task 7 fills it.
- **D-45** (core functionality success criteria) → Task 7 acceptance steps mirror D-45's "admin can access treasury, view contract ownership, see pool balances, MetaMask connects, form validation works".
- **D-46** (deployment sign-off document) → Task 2 creates SIGN-OFF.md; Task 8 finalizes approval.

## Verification Criteria (phase-level)

When all 8 tasks above pass their `<verify>` and `<done>` blocks, the phase is verified iff:

- [ ] `docs/DEPLOY_RUNBOOK.md` contains the `## Admin Treasury (v0.10.0) Deployment` section
- [ ] `.planning/phases/68-production-deployment/SIGN-OFF.md` exists, is filled out, and § 9 has approval
- [ ] `https://pb.eggoworld.io/api/v2/admin/pool-balances?wallet=0x0000...` returns a `"code":"NOT_OWNER"` response (proves PB hook + wallet-api alive)
- [ ] `https://eggoworld.io/admin/treasury` returns 2xx and renders the admin treasury surface
- [ ] No new wallet-api or PocketBase env vars were introduced; no DB migration ran
- [ ] No real treasury withdrawal transaction was broadcast during verification

---

## Dependency Notes

- This phase **depends on** Phases 64, 65, 66, 67 being complete in the deploy branch (per ROADMAP). Per STATE.md, Phase 67 is the last code-shipping phase and is Complete.
- This phase **blocks** marking v0.10.0 as shipped — DEPL-01/02/03 are the last open requirements in v0.10.0.

---

_Phase: 68-production-deployment_
_Plan created: 2026-05-26_
