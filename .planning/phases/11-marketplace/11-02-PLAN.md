---
phase: 11-marketplace
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  [
    apps/web/components/marketplace/SellDialog.tsx,
    apps/web/components/marketplace/CreateListingDialog.tsx,
    apps/web/components/referrals/CommissionBreakdown.tsx,
  ]
autonomous: true
requirements: [MKT-04, MKT-05]

must_haves:
  truths:
    - "User can list NFT for sale from inventory page"
    - "Sell dialog accepts asking price in USDT"
    - "Created listing appears in marketplace immediately"
    - "Commission breakdown shows G1 20%, G2-G4 10% each"
  artifacts:
    - path: "apps/web/components/marketplace/CreateListingDialog.tsx"
      provides: "Complete sell flow dialog with price input"
      min_lines: 250
    - path: "apps/web/components/referrals/CommissionBreakdown.tsx"
      provides: "4-level commission distribution display"
      min_lines: 100
  key_links:
    - from: "apps/web/components/marketplace/CreateListingDialog.tsx"
      to: "apps/web/lib/contracts/marketplace.ts"
      via: "createListing function call"
      pattern: "createListing"
    - from: "apps/web/components/referrals/CommissionBreakdown.tsx"
      to: "apps/web/hooks/use-referral-earnings.ts"
      via: "earnings data propagation"
      pattern: "useReferralEarnings"
---

<objective>
Implement complete NFT sell flow with commission breakdown visualization showing 4-level referral distribution.

Purpose: Complete MKT-04 (sell flow) and MKT-05 (commission display) — users can currently only view earnings but need clearer visualization of the 20%/10%/10%/10% distribution.

Output: Working sell flow with escrow integration and commission breakdown component with percentage fills.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-marketplace/11-CONTEXT.md
@apps/web/components/marketplace/SellDialog.tsx
@apps/web/components/marketplace/CreateListingDialog.tsx
@apps/web/app/dashboard/commissions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>task 0: Refactor sell dialog with TDD</name>
  <files>apps/web/components/marketplace/CreateListingDialog.tsx</files>
  <action>
    Create/Update CreateListingDialog with TDD approach:
    
    1. First, write tests (apps/web/components/marketplace/CreateListingDialog.test.tsx):
       - Test dialog opens with NFT details pre-filled
       - Test price validation (min 0.0001, max 1000000)
       - Test "Create Listing" button calls createListing contract
       - Test success state after listing created
       - Test error state on rejection
       - Test loading state during transaction
    
    2. Implement dialog:
       - Step 1: Show NFT preview (image, name, rarity)
       - Step 2: Price input field (USDT, 4 decimals)
       - Step 3: Confirmation: "List [NFT Name] for [price] USDT?"
       - Step 4: Call approveUSDT for marketplace escrow
       - Step 5: Call createListing(tokenId, price)
       - Step 6: Success with "View Listing" button
    
    3. Error handling:
       - Insufficient USDT balance → show error
       - User rejects transaction → show "Cancelled" message
       - Network error → show retry button
    
    4. Thai comments throughout
  </action>
  <verify>
    <automated>bun test apps/web/components/marketplace/CreateListingDialog.test.tsx --filter "create listing"</automated>
  </verify>
  <done>
    Sell dialog functional with price validation, escrow approval, listing creation
  </done>
</task>

<task type="auto">
  <name>task 1: Commission breakdown with buddy chain visualization</name>
  <files>apps/web/components/referrals/CommissionBreakdown.tsx</files>
  <action>
    Create CommissionBreakdown component showing 4-level distribution:
    
    Data structure:
    ```typescript
    interface CommissionLevel {
      level: number  // 1-4
      label: string  // "G1", "G2", "G3", "G4"
      percentage: number  // 20, 10, 10, 10
      count: number  // Number of referrals at this level
      earned: number  // Total USDT earned
      expected: number  // Expected earnings (for percentage fill)
    }
    ```
    
    Visual design (Buddy Chain cards from Phase 9):
    - 4 square cards in grid (2x2 on mobile, 4x1 desktop)
    - Each card shows:
      - Level label (G1, G2, G3, G4)
      - Percentage fill overlay (earned/expected * 100%)
      - Color gradient based on percentage
      - Buddy count: "5 Buddies"
      - Earned amount: "50.00 USDT"
    
    Features:
    - Claymorphism styling consistent with Phase 8-10
    - Percentage fill animation (@keyframes)
    - Tooltip on hover showing calculation: "G1 earns 20% of direct referrals"
    - Empty state: "No referrals yet. Share your link!"
    
    Props:
    ```typescript
    interface CommissionBreakdownProps {
      userId: string
      className?: string
    }
    ```
    
    Data fetching:
    - Call PocketBase `users` collection with expand
    - Calculate G1-G4 from referral chain
    - Fetch commission earnings from `commissions` collection
  </action>
  <verify>
    <automated>bun test apps/web/components/referrals/CommissionBreakdown.test.tsx</automated>
  </verify>
  <done>
    CommissionBreakdown renders 4-level distribution with percentage fills, correct G1 20% / G2-G4 10% calculations
  </done>
</task>

<task type="auto">
  <name>task 2: Integrate commission breakdown</name>
  <files>apps/web/app/dashboard/commissions/page.tsx</files>
  <action>
    Update commissions dashboard page:
    
    1. Import CommissionBreakdown component
    2. Replace existing earnings table with:
       - CommissionBreakdown cards at top (4 cards grid)
       - Existing transaction history table below
    3. Add auto-refresh polling (reuse useWalletPoll pattern):
       - Poll commission data every 30 seconds
       - Show "Updating..." badge during polling
    4. Loading state: Show skeleton during initial fetch
    5. Error state: Error boundary with retry button
    
    Keep existing transaction history, add summary totals if space allows.
  </action>
  <verify>
    <automated>bun run build 2>&1 | grep -i error</automated>
  </verify>
  <done>
    Commissions page displays buddy chain cards + transaction table, builds without errors
  </done>
</task>

</tasks>

<verification>
## Overall Verification

1. **Build succeeds**: `bun run build` completes without errors
2. **Sell tests pass**: 6/6 CreateListingDialog tests passing
3. **Commission tests pass**: 4/4 CommissionBreakdown tests passing
4. **Etherscan link works**: Sale transaction hash appears in success state
5. **Percentage calculations correct**: G1 shows 20%, G2-G4 shows 10% each
   </verification>

<success_criteria>

- [x] User can set asking price in USDT (4 decimals)
- [x] Sell flow requires USDT approval for escrow
- [x] createListing contract function called with correct parameters
- [x] New listing appears in marketplace immediately
- [x] CommissionBreakdown shows 4 levels: G1 (20%), G2 (10%), G3 (10%), G4 (10%)
- [x] Buddy chain cards have percentage fill visualization
- [x] All tests pass (10/10 total)
- [x] Thai comments throughout
      </success_criteria>

<output>
After completion, create `.planning/phases/11-marketplace/11-02-SUMMARY.md`
</output>
