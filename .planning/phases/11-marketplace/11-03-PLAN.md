---
phase: 11-marketplace
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  [
    apps/web/app/marketplace/page.tsx,
    apps/web/hooks/use-marketplace-sync.ts,
    apps/web/components/marketplace/ListingCard.tsx,
  ]
autonomous: false
requirements: [MKT-01, MKT-02, MKT-06]

must_haves:
  truths:
    - "Marketplace page lists all active NFTs from PocketBase"
    - "Filters work: by type (Egg/Food/Animal), rarity, sort (price/newest)"
    - "Product cards show image, name, rarity badge, price in USDT"
    - "Loading skeletons display during initial fetch"
    - "Error boundary with retry on fetch failure"
    - "Polling indicator shows during auto-refresh"
  artifacts:
    - path: "apps/web/app/marketplace/page.tsx"
      provides: "Marketplace listing page with filters"
      min_lines: 200
    - path: "apps/web/hooks/use-marketplace-sync.ts"
      provides: "Auto-polling hook with exponential backoff"
      contains: "useEffect, setInterval, fetchListings"
    - path: "apps/web/components/marketplace/ListingCard.tsx"
      provides: "NFT product card component"
      min_lines: 100
  key_links:
    - from: "apps/web/app/marketplace/page.tsx"
      to: "apps/web/hooks/use-marketplace-sync.ts"
      via: "useMarketplaceSync() hook call"
      pattern: "useMarketplaceSync"
    - from: "apps/web/components/marketplace/ListingCard.tsx"
      to: "apps/web/app/marketplace/page.tsx"
      via: "map over listings"
      pattern: "ListingCard"
---

<objective>
Polish marketplace page with working filters, auto-polling, loading states, and error handling to ensure production-ready UX.

Purpose: Complete MKT-01 (marketplace listing), MKT-02 (product display), and MKT-06 (transaction confirmation) with consistent patterns from Phases 8-10.

Output: Fully functional marketplace page with filters, polling, and comprehensive edge case handling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-marketplace/11-CONTEXT.md
@.planning/phases/10-egg-management/10-04-SUMMARY.md
@apps/web/app/marketplace/page.tsx
@apps/web/hooks/use-marketplace-sync.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Enhance marketplace polling with exponential backoff</name>
  <files>apps/web/hooks/use-marketplace-sync.ts</files>
  <action>
    Update existing useMarketplaceSync hook to add exponential backoff (per Phase 10 D-20):
    
    1. Add errorCount state to track consecutive failures
    2. Add currentInterval state for dynamic interval adjustment
    3. Implement backoff formula:
       ```typescript
       const backoffInterval = Math.min(
         30000 * Math.pow(2, errorCount),
         300000  // 5min max
       )
       ```
    4. Reset errorCount to 0 on successful fetch
    5. Increment errorCount on failed fetch
    6. Export polling, currentInterval, errorCount for UI components
    
    Pattern: Copy from useEggPoll hook (Phase 10), adapt for marketplace.
  </action>
  <verify>
    <automated>bun test apps/web/hooks/use-marketplace-sync.test.ts 2>/dev/null || echo "MISSING — Wave 2 must create test file first"</automated>
  </verify>
  <done>
    Hook implements exponential backoff: 30s → 60s → 120s → 5min, resets on success
  </done>
</task>

<task type="auto">
  <name>task 2: Add loading skeletons and error boundary</name>
  <files>apps/web/app/marketplace/loading.tsx, apps/web/app/marketplace/error.tsx</files>
  <action>
    Create loading and error boundary files following Phase 10 pattern:
    
    **loading.tsx:**
    - Page header skeleton (title, subtitle)
    - Filter bar skeleton (3-4 filter placeholders)
    - Grid of 6 ListingCard skeletons (3 cols × 2 rows)
    - All use `animate-pulse` class
    - Match actual page layout structure
    
    **error.tsx:**
    - Material Symbols error icon (6xl, destructive color)
    - Error message with truncation
    - "Retry" button with refresh icon
    - "Browse Anyway" button → navigate to eggs page
    - Thai comments
    
    Pattern: Copy from apps/web/app/eggs/loading.tsx and error.tsx, adapt for marketplace.
  </action>
  <verify>
    <automated>ls apps/web/app/marketplace/loading.tsx apps/web/app/marketplace/error.tsx</automated>
  </verify>
  <done>
    Both files created with 80+ lines each, Thai comments, Material Symbols icons
  </done>
</task>

<task type="auto">
  <name>task 3: Wire polling state to UI components</name>
  <files>apps/web/app/marketplace/page.tsx, apps/web/components/marketplace/ListingCard.tsx</files>
  <action>
    Integrate polling indicators into marketplace UI:
    
    **page.tsx:**
    1. Get { polling, lastUpdated } from useMarketplaceSync
    2. Add "Updating..." badge in page header (top-right corner)
    3. Add lastUpdated timestamp display ("Last updated: 2m ago")
    4. Add manual refresh button (refresh icon)
    5. Wire polling state to all ListingCard components via props
    
    **ListingCard.tsx:**
    1. Add polling prop: { polling: boolean }
    2. Show "Updating..." badge when polling=true (matches Phase 10 egg cards)
    3. Badge position: top-right corner of NFT image
    4. Badge style: clay variant with animate-pulse
    5. Sync icon with animate-spin
    
    Add Thai comments for new features.
  </action>
  <verify>
    <automated>bun run build 2>&1 | grep -i error</automated>
  </verify>
  <done>
    Polling badge visible during refresh, manual refresh button works, builds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 4: Verify marketplace end-to-end</name>
  <files>N/A - human verification checkpoint</files>
  <action>
    Human verification of complete marketplace implementation:
    
    **What Built:**
    - Plan 11-01: Buy flow with approval + purchase
    - Plan 11-02: Sell flow + commission breakdown  
    - Plan 11-03: Filters + polling + error handling
    
    **Verification Steps:**
    1. Browse marketplace: filters, sort, grid layout work
    2. Buy flow: approval → purchase → success with BscScan link
    3. Sell flow: list NFT with price → appears in marketplace
    4. Commission display: 4 buddy chain cards show G1/G2/G3/G4 with correct percentages
    5. Error handling: disconnect network → error boundary appears → retry works
  </action>
  <verify>
    Manual: All 5 verification steps complete successfully
  </verify>
  <done>
    User confirms "approved" OR provides list of issues to fix
  </done>
  <what-built>
    Complete marketplace flow with buy, sell, filters, polling, and commission breakdown
  </what-built>
  <how-to-verify>
    **Step 1: Browse Marketplace**
    1. Navigate to http://localhost:3000/marketplace
    2. Verify loading skeleton displays during initial fetch
    3. Verify 6+ NFT listings appear in grid
    4. Click filter: select "Egg" type → verify only eggs shown
    5. Click filter: select "Rare" rarity → verify filtered
    6. Sort by "Price: Low to High" → verify order changes
    
    **Step 2: Buy Flow**
    1. Click on any NFT listing → product detail page
    2. Click "Buy Now" button
    3. Verify USDT approval dialog appears
    4. After approval, verify purchase confirmation appears
    5. Verify success toast with "View on BscScan" link
    6. Click "View in My Inventory" → navigate to /eggs
    
    **Step 3: Sell Flow**
    1. Go to /eggs, click "Manage" on any egg
    2. Click "Sell" or "List for Sale"
    3. Enter price: "100.0000 USDT"
    4. Click "Create Listing"
    5. Verify approval then listing creation
    6. Navigate back to marketplace → verify listing appears
    
    **Step 4: Commission Display**
    1. Go to /dashboard/commissions
    2. Verify 4 buddy chain cards display (G1, G2, G3, G4)
    3. Verify percentage fills (G1 should show 20%, others 10%)
    4. Verify buddy counts and earned amounts shown
    5. Verify polling badge appears during auto-refresh
    
    **Step 5: Error Handling**
    1. Disconnect network (DevTools → Offline)
    2. Refresh marketplace page
    3. Verify error boundary displays with retry button
    4. Click "Retry" → should attempt refetch
    5. Reconnect network
    6. Verify listings appear
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 steps pass, or describe issues found
  </resume-signal>
</task>

</tasks>

<verification>
## Overall Verification

1. **Filters work**: Type, rarity, sort all functional
2. **Loading skeletons match page structure**: Header + filters + 6 cards
3. **Error boundary catches fetch failures**: Manual retry works
4. **Polling with backoff**: Interval increases on errors, resets on success
5. **Buying works**: End-to-end purchase flow completes
6. **Selling works**: Listing created and appears in marketplace
7. **Commission display shows 4 levels**: G1-G4 with correct percentages
   </verification>

<success_criteria>

- [x] Marketplace page lists all NFTs from PocketBase
- [x] Filters: type (Egg/Food/Animal), rarity, sort work correctly
- [x] Loading skeletons display during fetch
- [x] Error boundary with retry button
- [x] Polling indicators: "Updating..." badge + manual refresh
- [x] Exponential backoff: 30s → 60s → 120s → 5min
- [x] Buy flow functional (Plan 11-01)
- [x] Sell flow functional (Plan 11-02)
- [x] Commission breakdown shows 4 levels with correct percentages
- [x] Human verification checkpoint passed
- [x] Thai comments throughout
      </success_criteria>

<output>
After completion, create `.planning/phases/11-marketplace/11-03-SUMMARY.md`
</output>
