---
phase: 11-marketplace
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [apps/web/app/marketplace/[id]/page.tsx, apps/web/components/marketplace/BuyFlow.tsx, apps/web/lib/contracts/marketplace.ts]
autonomous: true
requirements: [MKT-02, MKT-03, MKT-06]

must_haves:
  truths:
    - "User can click Buy Now button on product detail page"
    - "Buy flow executes USDT approval transaction first"
    - "Buy flow executes marketplace purchase after approval"
    - "Transaction confirmation shows success with Etherscan link"
  artifacts:
    - path: "apps/web/app/marketplace/[id]/page.tsx"
      provides: "Product detail page with working Buy Now button"
      min_lines: 350
    - path: "apps/web/components/marketplace/BuyFlow.tsx"
      provides: "Complete buy flow with approval + purchase"
      exports: ["BuyFlow component"]
    - path: "apps/web/lib/contracts/marketplace.ts"
      provides: "Marketplace contract ABI and buy functions"
      contains: "buyNFT, approveUSDT"
  key_links:
    - from: "apps/web/app/marketplace/[id]/page.tsx"
      to: "apps/web/components/marketplace/BuyFlow.tsx"
      via: "import and render"
      pattern: "import.*BuyFlow"
    - from: "apps/web/components/marketplace/BuyFlow.tsx"
      to: "apps/web/lib/contracts/marketplace.ts"
      via: "contract function calls"
      pattern: "buyNFT|approveUSDT"
---

<objective>
Implement complete NFT buy flow with USDT approval and marketplace purchase transaction, replacing the "coming soon" stub with production-ready TDD implementation.

Purpose: Close critical gap identified in Phase 03 verification (MKT-03 incomplete) — users can currently view product details but cannot purchase.

Output: Working buy flow with 2-step transaction (approve → buy), loading states, error handling, and success confirmation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-marketplace/11-CONTEXT.md
@.planning/phases/03-frontend-marketplace/03-VERIFICATION.md
@apps/web/app/marketplace/[id]/page.tsx
@apps/web/components/marketplace/BuyFlow.tsx
</context>

<tasks>

<task type="auto">
  <name>task 0: Create marketplace contract integration layer</name>
  <files>apps/web/lib/contracts/marketplace.ts</files>
  <action>
    Create contract integration file with:
    1. Import ethers v6 and wallet-api client
    2. Export MARKETPLACE_ADDRESS from env or config
    3. Create marketplaceABI array with buyNFT, createListing, cancelListing functions
    4. Create approveUSDT function:
       - Get USDT contract instance
       - Call approve(marketplaceAddress, amount)
       - Wait for transaction receipt
       - Return success boolean
    5. Create buyNFT function:
       - Get marketplace contract instance
       - Call buyNFT(listingId, { value: price })
       - Wait for transaction receipt with tx.wait()
       - Parse transaction logs for NFT transfer event
       - Return { success: true, tokenId, txHash }

    Thai comments throughout. Include error handling with try/catch.

  </action>
  <verify>
    <automated>bun test apps/web/lib/contracts/marketplace.test.ts</automated>
  </verify>
  <done>
    Contract integration functions exported with TypeScript types, test file created with mock tests
  </done>
</task>

<task type="auto">
  <name>task 1: Red — Write failing tests for buy flow</name>
  <files>apps/web/components/marketplace/BuyFlow.test.tsx</files>
  <action>
    Create test file following TDD red-green-refactor pattern:
    
    Test 1: Buy button disabled when wallet not connected
    - Render BuyFlow with isHydrated=false
    - Expect button to be disabled or show "Connect wallet"
    
    Test 2: Buy button triggers approval flow
    - Mock USDT.approve() to resolve successfully
    - Simulate button click
    - Expect approve() called with correct amount
    
    Test 3: Buy flow proceeds to purchase after approval
    - Mock USDT.approve() → resolved
    - Mock marketplace.buyNFT() → resolved
    - Simulate complete flow
    - Expect buyNFT() called after approve()
    
    Test 4: Success toast shown after purchase
    - Mock both approve and buyNFT to resolve
    - Complete flow
    - Expect toast.success() called with "Purchase successful"
    
    Test 5: Error toast shown on failure
    - Mock buyNFT() to reject with error
    - Complete flow
    - Expect toast.error() called with error message
    
    Test 6: Transaction confirmation shows Etherscan link
    - Mock buyNFT() → resolves with txHash
    - Complete flow
    - Expect link to contain txHash and "BscScan" or "Etherscan"
    
    Use bun:test, @testing-library/react. Mock wallet-api client.
  </action>
  <verify>
    <automated>bun test apps/web/components/marketplace/BuyFlow.test.tsx --filter "buy flow"</automated>
  </verify>
  <done>
    6 tests written, all failing (RED state confirmed)
  </done>
</task>

<task type="auto">
  <name>task 2: Green — Implement buy flow component</name>
  <files>apps/web/components/marketplace/BuyFlow.tsx</files>
  <action>
    Refactor existing BuyFlow.tsx (currently 6708 bytes) to implement full flow:
    
    1. State variables:
       - isApproving: boolean (USDT approval in progress)
       - isBuying: boolean (purchase in progress)
       - error: string | null
       - txHash: string | null (for success state)
    
    2. handleBuyNow function:
       - Validate wallet connected (check pb.authStore.record?.wallet)
       - Step 1: Call approveUSDT(price) → wait for receipt
       - Update state: isApproving=false, show "Approval confirmed"
       - Step 2: Call buyNFT(listingId, price) → wait for receipt
       - Parse receipt for NFT token ID
       - Update state: isBuying=false, txHash=receipt.hash
    
    3. Success state:
       - Show "Purchase Successful!" message
       - Display NFT name and image
       - Transaction link: `<a href={`https://bscscan.com/tx/${txHash}`}>View on BscScan</a>`
       - Button: "View in My Inventory" → navigate to /eggs
    
    4. Error handling:
       - Catch errors at each step
       - Show user-friendly error message (Thai + English)
       - "Retry" button to restart flow
    
    5. Loading states:
       - Approval: "Approving USDT..." with spinner
       - Purchase: "Confirming Purchase..." with spinner
       - Both states disable the buy button
    
    6. Claymorphism styling consistent with Phase 8-10
  </action>
  <verify>
    <automated>bun test apps/web/components/marketplace/BuyFlow.test.tsx --filter "buy flow" --bail</automated>
  </verify>
  <done>
    All 6 tests pass (GREEN state), buy flow functional with approval → purchase → success
  </done>
</task>

<task type="auto">
  <name>task 3: Wire buy flow to product detail page</name>
  <files>apps/web/app/marketplace/[id]/page.tsx</files>
  <action>
    Update product detail page to integrate BuyFlow component:
    
    1. Find existing TODO/alert placeholder (line 129-130 per verification):
       ```tsx
       // TODO: Implement buy functionality
       alert('Buy functionality coming soon')
       ```
    
    2. Replace with BuyFlow component:
       ```tsx
       <BuyFlow
         listingId={listing.id}
         price={listing.price}
         nftName={listing.name}
         nftImage={listing.image_url}
         onSuccess={() => {
           toast.success('NFT purchased!')
           router.push('/eggs')
         }}
       />
       ```
    
    3. Add conditions:
       - Only show BuyFlow if status === 'active'
       - Hide/show based on user being buyer (can't buy own listing)
       - Hydration check before rendering interactive components
    
    4. Add loading skeleton for BuyFlow section (matches page structure)
    5. Add error boundary around BuyFlow (per Phase 10 pattern)
    
    Keep existing Thai comments, add comments for new buy flow integration.
  </action>
  <verify>
    <automated>bun run build 2>&1 | grep -i error</automated>
  </verify>
  <done>
    Product detail page builds without errors, BuyFlow component renders on active listings
  </done>
</task>

<task type="auto">
  <name>task 4: Refactor — Clean up and optimize</name>
  <files>apps/web/components/marketplace/BuyFlow.tsx, apps/web/lib/contracts/marketplace.ts</files>
  <action>
    Refactor for code quality:
    
    1. Extract approval logic to separate useUSDTApproval hook
    2. Extract purchase logic to useNFTPurchase hook
    3. Add TypeScript error types (ApprouvalError, PurchaseError)
    4. Consolidate duplicate error messages
    5. Add JSDoc comments to exported functions
    6. Ensure consistent Thai comments
    
    Verify no functionality changed — tests must still pass.
  </action>
  <verify>
    <automated>bun test apps/web/components/marketplace/BuyFlow.test.tsx && bun test apps/web/lib/contracts/marketplace.test.ts</automated>
  </verify>
  <done>
    Code refactored with hooks, all tests pass, no linting errors
  </done>
</task>

</tasks>

<verification>
## Overall Verification

1. **Build succeeds**: `bun run build` completes without errors
2. **All tests pass**: 6/6 BuyFlow tests passing
3. **Etherscan link works**: Transaction hash appears in success state with valid BscScan URL
4. **TypeScript satisfied**: No type errors in modified files
   </verification>

<success_criteria>

- [x] Buy button on product detail page triggers actual purchase flow (not alert)
- [x] USDT approval executes before purchase transaction
- [x] Marketplace buyNFT contract called after approval
- [x] Transaction confirmation shows with Etherscan link
- [x] Success state navigates user to /eggs page
- [x] All 6 tests pass
- [x] Thai comments throughout
      </success_criteria>

<output>
After completion, create `.planning/phases/11-marketplace/11-01-SUMMARY.md`
</output>
