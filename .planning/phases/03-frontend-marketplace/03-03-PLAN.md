---
phase: 03-frontend-marketplace
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - apps/web/app/marketplace/[nftId]/page.tsx
  - apps/web/app/dashboard/referrals/page.tsx
  - apps/web/components/DownlineTable.tsx
  - apps/web/components/EarningsBreakdown.tsx
autonomous: true
requirements: [UI-05, UI-06]

must_haves:
  truths:
    - "User can view NFT details on product page"
    - "User can see referral downline in table format"
    - "User can see commission earnings breakdown by level"
    - "User can copy referral link"
  artifacts:
    - path: "apps/web/app/marketplace/[nftId]/page.tsx"
      provides: "Product detail page"
      min_lines: 80
    - path: "apps/web/app/dashboard/referrals/page.tsx"
      provides: "Referral dashboard with table"
      min_lines: 90
    - path: "apps/web/components/DownlineTable.tsx"
      provides: "Downline display component"
      min_lines: 50
    - path: "apps/web/components/EarningsBreakdown.tsx"
      provides: "Earnings by level component"
      min_lines: 50
  key_links:
    - from: "apps/web/app/marketplace/[nftId]/page.tsx"
      to: "pb.collection('nfts')"
      via: "PocketBase query"
      pattern: "pb\\.collection\\(['\\\"]nfts"
    - from: "apps/web/app/dashboard/referrals/page.tsx"
      to: "pb.collection('users')"
      via: "PocketBase query with referral_chain filter"
      pattern: "pb\\.collection\\(['\\\"]users"
---

<objective>
Build Product Detail page and Referral Dashboard
Purpose: Better UX for NFT discovery and referral tracking
Output: Product detail page with metadata display, referral dashboard with downline table
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-frontend-marketplace/03-CONTEXT.md
@.planning/phases/03-frontend-marketplace/03-RESEARCH.md
@.planning/phases/03-frontend-marketplace/03-01-SUMMARY.md
@.planning/phases/03-frontend-marketplace/03-02-SUMMARY.md
@apps/web/app/marketplace/food/page.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: Create product detail page</name>
  <files>apps/web/app/marketplace/[nftId]/page.tsx</files>
  <read_first>
    - apps/web/app/marketplace/[nftId]/page.tsx (create new)
    - apps/web/app/marketplace/food/page.tsx (marketplace pattern)
    - apps/web/app/mint/page.tsx (transaction UI pattern)
    - .planning/phases/03-frontend-marketplace/03-RESEARCH.md (metadata sources)
  </read_first>
  <action>
    Create apps/web/app/marketplace/[nftId]/page.tsx with:
    
    1. Imports:
       - Card, CardHeader, CardTitle, CardContent from @/components/ui/card
       - Badge from @/components/ui/badge
       - Button from @/components/ui/button
       - Image from @/components/ui/image (or next/image)
       - pb from @/lib/pocketbase/client
       - useParams, useRouter from next/navigation
       - useState, useEffect from react
    
    2. Fetch NFT data from PocketBase:
       ```typescript
       const fetchNft = async (nftId: string) => {
         const nft = await pb.collection('nfts').getOne(nftId, {
           expand: 'owner'
         })
         return nft
       }
       ```
    
    3. Page structure:
       - Get nftId from useParams()
       - Fetch NFT data on mount
       - Show loading state while fetching
       - Two-column layout on desktop (image left, details right)
    
    4. Left column (image):
       - NFT image in Card
       - Use next/image for optimization
    
    5. Right column (details):
       - NFT name (h1, text-3xl)
       - Type badge (egg/food/animal)
       - Rarity badge with color (common/rare/epic/legendary)
       - PriceCard if listed for sale (reuse from mint page)
       - Stats grid: Owner, Food Count, Generation, Species
       - Action buttons:
         - If owner: "List for Sale" button
         - If not owner and listed: "Buy Now" button
         - If not owner and not listed: "Not for sale" (disabled)
    
    6. Handle not found state with 404 page
  </action>
  <verify>
    <automated>test -f apps/web/app/marketplace/[nftId]/page.tsx && grep -q "pb.collection('nfts')" apps/web/app/marketplace/[nftId]/page.tsx && echo "PASS: Product detail created"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/marketplace/[nftId]/page.tsx exists
    - Uses useParams to get nftId
    - Fetches from nfts collection with expand: 'owner'
    - Shows loading state while fetching
    - Two-column layout (grid or flex)
    - Left column has NFT image
    - Right column has NFT name in h1 tag
    - Shows type badge
    - Shows rarity badge with color
    - Stats grid with Owner, Food Count, Generation, Species
    - Buy Now or List for Sale button based on ownership
    - Handles not found state
    - File has 70+ lines
  </acceptance_criteria>
  <done>
    Product detail page complete:
    - Fetches NFT metadata from PocketBase
    - Displays image, name, type, rarity
    - Shows stats grid
    - Buy/list buttons based on ownership
    - Loading and not found states handled
  </done>
</task>

<task type="auto">
  <name>task 2: Build referral dashboard with table</name>
  <files>apps/web/app/dashboard/referrals/page.tsx</files>
  <read_first>
    - apps/web/app/dashboard/referrals/page.tsx (create new)
    - apps/web/app/dashboard/commissions/page.tsx (dashboard pattern)
    - apps/web/app/dashboard/page.tsx (dashboard layout)
    - .planning/phases/03-frontend-marketplace/03-CONTEXT.md (D-06: simple list first)
  </read_first>
  <action>
    Create apps/web/app/dashboard/referrals/page.tsx with:
    
    1. Imports:
       - Card, CardHeader, CardTitle, CardContent from @/components/ui/card
       - Table components from @/components/ui/table
       - Badge from @/components/ui/badge
       - Button from @/components/ui/button
       - Input from @/components/ui/input
       - pb from @/lib/pocketbase/client
       - useIsHydrated, getUser from @/lib/pocketbase/client
    
    2. Fetch referral data:
       ```typescript
       const fetchReferralData = async (userId: string) => {
         // Get current user
         const user = await pb.collection('users').getOne(userId)
         
         // Get direct recruits (G1)
         const g1Recruits = await pb.collection('users').getList(1, 50, {
           filter: `referral_chain.g1 = "${user.wallet_address}"`,
           sort: '-created'
         })
         
         return {
           user,
           g1: g1Recruits.items,
           totalDirect: user.total_direct_recruits || 0,
           lifetimeEarnings: user.total_earned_usdt || 0
         }
       }
       ```
    
    3. Page structure:
       - Check hydration
       - Get current user
       - Fetch referral data
       - Three sections: Summary cards, Referral link, Downline table
    
    4. Summary cards (3 columns):
       - Direct Recruits (G1): count
       - Total Downline: count
       - Lifetime Earnings: USDT amount
    
    5. Referral link generator:
       - Show input with referral link
       - Copy button
       - Shows "Copied!" toast on click
    
    6. Downline table:
       - Columns: Wallet, Joined, Egg Purchases, Food Purchases, Your Earnings
       - Show G1 recruits only (per D-06: simple list first)
       - Truncate wallet addresses (0x1234...abcd)
       - Format dates
  </action>
  <verify>
    <automated>test -f apps/web/app/dashboard/referrals/page.tsx && grep -q "referral_chain" apps/web/app/dashboard/referrals/page.tsx && echo "PASS: Referral dashboard created"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/dashboard/referrals/page.tsx exists
    - Contains "use client" directive
    - Uses useIsHydrated hook
    - Fetches from users collection
    - Filters by referral_chain.g1 = user.wallet_address
    - Shows 3 summary cards (Direct Recruits, Total Downline, Lifetime Earnings)
    - Has referral link input field
    - Has copy button for referral link
    - Has downline table with columns: Wallet, Joined, Egg Purchases, Food Purchases, Your Earnings
    - Wallet addresses truncated (0x... notation)
    - Shows G1 recruits only (not full tree)
    - File has 80+ lines
  </acceptance_criteria>
  <done>
    Referral dashboard complete:
    - Summary cards showing stats
    - Referral link with copy button
    - Downline table (G1 only, per D-06)
    - Simple list, not tree visualization
  </done>
</task>

<task type="auto">
  <name>task 3: Build earnings breakdown and downline components</name>
  <files>apps/web/components/EarningsBreakdown.tsx, apps/web/components/DownlineTable.tsx</files>
  <read_first>
    - apps/web/components/EarningsBreakdown.tsx (create new)
    - apps/web/components/DownlineTable.tsx (create new)
    - apps/web/components/ui/table.tsx
    - .planning/phases/03-frontend-marketplace/03-RESEARCH.md (component structure)
  </read_first>
  <action>
    Create apps/web/components/EarningsBreakdown.tsx:
    
    1. Imports: Card, CardHeader, CardTitle, CardContent, Table components, Badge
    
    2. Props interface:
       ```typescript
       interface EarningsBreakdownProps {
         earnings: {
           byLevel: Array<{
             level: number
             percentage: string
             count: number
             earned: string
           }>
           total: string
         }
       }
       ```
    
    3. Component renders:
       - Card with "Commission Earnings" title
       - Table with columns: Level, Count, Earnings
       - Rows for G1 (20%), G2 (10%), G3 (10%), G4 (10%)
       - Total row at bottom
    
    Create apps/web/components/DownlineTable.tsx:
    
    1. Imports: Card, CardHeader, CardTitle, CardContent, Table components
    
    2. Props interface:
       ```typescript
       interface DownlineTableProps {
         downline: Array<{
           id: string
           wallet_address: string
           created: string
           egg_purchases: number
           food_purchases: number
           earned_for_you: string
         }>
       }
       ```
    
    3. Helper function to truncate address:
       ```typescript
       const truncateAddress = (address: string) => {
         return `${address.slice(0, 6)}...${address.slice(-4)}`
       }
       ```
    
    4. Component renders:
       - Card with "Direct Recruits" title
       - Table with columns: Wallet, Joined, Egg Purchases, Food Purchases, Your Earnings
       - Map over downline array
       - Format dates with toLocaleDateString()
       - Show "No recruits yet" if empty
  </action>
  <verify>
    <automated>test -f apps/web/components/EarningsBreakdown.tsx && test -f apps/web/components/DownlineTable.tsx && echo "PASS: Components created"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/EarningsBreakdown.tsx exists
    - Accepts earnings prop with byLevel array
    - Shows table with Level, Count, Earnings columns
    - Displays G1 (20%), G2 (10%), G3 (10%), G4 (10%) rows
    - Shows total earnings
    - apps/web/components/DownlineTable.tsx exists
    - Accepts downline array prop
    - Has truncateAddress helper function
    - Shows table with Wallet, Joined, Egg Purchases, Food Purchases, Your Earnings
    - Shows "No recruits yet" when downline is empty
    - Both files have 40+ lines each
  </acceptance_criteria>
  <done>
    Components complete:
    - EarningsBreakdown shows G1/G2/G3/G4 breakdown
    - DownlineTable shows direct recruits
    - Both reusable across pages
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
- [ ] Product detail page accessible at /marketplace/[nftId]
- [ ] Shows NFT metadata correctly
- [ ] Referral dashboard accessible at /dashboard/referrals
- [ ] Shows downline table with G1 recruits
- [ ] Shows commission earnings breakdown
- [ ] Referral link copy button works
</verification>

<success_criteria>
Measurable completion:

- User can view NFT details by clicking on marketplace listing
- Product page shows all metadata (species, rarity, food count, etc.)
- User can view referral dashboard
- Dashboard shows direct recruits in table
- Commission earnings breakdown by level displayed
- Referral link can be copied to clipboard
  </success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-marketplace/03-03-SUMMARY.md` with:
- What was built
- Files modified
- Any blockers or follow-ups needed
</output>
