---
phase: 15-feed-feature
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/eggs/feed-dialog.tsx
  - apps/web/hooks/use-egg-feed.ts
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
must_haves:
  truths:
    - "FeedDialog calls backend API (POST /api/v2/feed-egg) instead of direct contract call"
    - "Food NFTs are marked as consumed in PocketBase after successful feed"
    - "Egg food_count is updated in both smart contract and PocketBase"
    - "Consumption log is created for each feed transaction"
  artifacts:
    - path: "apps/web/components/eggs/feed-dialog.tsx"
      provides: "Feed dialog with backend API integration"
      exports: ["FeedDialog"]
  key_links:
    - from: "apps/web/components/eggs/feed-dialog.tsx"
      to: "apps/backend/pb_hooks/16-feed-egg.pb.js"
      via: "POST /api/v2/feed-egg API call"
      pattern: "fetch.*feed-egg"
---

<objective>
Wire FeedDialog to call backend hook API instead of direct smart contract call
Purpose: Complete feed flow that properly updates DB (mark food consumed, create logs, update user stats)
Output: FeedDialog calls POST /api/v2/feed-egg → validations → wallet-api → contract + DB updates
</objective>

<execution_context>
@.planning/phases/15-feed-feature/15-CONTEXT.md
@.planning/phases/15-feed-feature/15-DISCUSSION-LOG.md
</execution_context>

<context>
@apps/web/components/eggs/feed-dialog.tsx
@apps/web/hooks/use-egg-feed.ts
@apps/backend/pb_hooks/16-feed-egg.pb.js
</context>

<tasks>

<task type="auto">
  <name>task 1: Modify useEggFeed hook to support backend API call path</name>
  <files>apps/web/hooks/use-egg-feed.ts</files>
  <read_first>
    - apps/web/hooks/use-egg-feed.ts (current direct contract call)
    - apps/web/lib/pocketbase/client.ts (PocketBase client for auth token)
    - apps/web/app/eggs/page.tsx (PocketBase URL pattern)
  </read_first>
  <action>
    Modify `useEggFeed` hook to add an alternative flow that calls the backend API instead of direct contract:

    1. Add new optional parameter `useBackendApi?: boolean` (default false for backward compat)
    2. When `useBackendApi` is true:
       - Get auth token from PocketBase client: `const pb = createClient(); const token = pb.authStore.token`
       - Call `fetch('/api/v2/feed-egg')` with POST, auth header, body `{ egg_token_id: eggId, food_ids: foodIds }`
       - Parse response JSON, check `response.success`
       - Show success/error toast based on API response
    3. When `useBackendApi` is false, keep existing direct contract call path unchanged
    4. Update the return interface to expose `useBackendApi` option via parameter

  </action>
  <acceptance_criteria>
    - useEggFeed accepts optional backend API mode flag
    - When enabled, calls POST /api/v2/feed-egg with auth token
    - Backward compatible: existing direct contract path unchanged without flag
    - Shows success toast on API success, error toast on API failure
  </acceptance_criteria>
  <verify>
    <automated>grep -q "feed-egg" apps/web/hooks/use-egg-feed.ts && echo "✓ API call path added"</automated>
  </verify>
  <done>
    useEggFeed hook now supports both direct contract call (default) and backend API call path
  </done>
</task>

<task type="auto">
  <name>task 2: Update FeedDialog to use backend API path</name>
  <files>apps/web/components/eggs/feed-dialog.tsx</files>
  <read_first>
    - apps/web/components/eggs/feed-dialog.tsx (current flow)
    - apps/web/hooks/use-egg-feed.ts (modified in task 1)
  </read_first>
  <action>
    Update FeedDialog to use backend API path:

    1. Change the `useEggFeed` hook call to enable backend API mode:
       ```tsx
       const { feedEgg, loading } = useEggFeed(true) // true = use backend API
       ```
       Or if modifying the hook interface differently, adjust accordingly.

    2. After successful feed (`handleSubmit`):
       - The `feedEgg` function now calls backend API
       - Backend hook handles: validation → wallet-api → DB updates
       - Existing success/failure toast handling still works
       - `onSuccess` callback triggers egg list refresh as before

    3. No other UI changes needed — the two-step dialog flow remains:
       - Step 1: Quick-fill auto-select (unchanged)
       - Step 2: Confirmation (unchanged)
       - Submit calls updated `feedEgg` which now uses API

    Note: This task depends on task 1 (hook modification) being complete.

  </action>
  <acceptance_criteria>
    - FeedDialog calls backend API via modified useEggFeed hook
    - Food NFTs are marked consumed in DB after successful feed
    - Egg food_count updates in both contract and DB
    - Consumption log created in egg_consumption_logs
    - User total_food_consumed increments
    - No regression: dialog still shows same UI, same two-step flow
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useEggFeed" apps/web/components/eggs/feed-dialog.tsx && echo "✓ FeedDialog still uses hook"</automated>
  </verify>
  <done>
    FeedDialog now calls backend API on submit, ensuring all DB updates happen alongside contract call
  </done>
</task>

</tasks>

<success_criteria>

- FeedDialog submit → POST /api/v2/feed-egg with auth token ✓
- Backend hook validates egg ownership, food ownership, not consumed ✓
- Backend hook calls wallet-api feed-egg (real contract call) ✓
- Backend hook updates: egg food_count, food is_consumed, consumption log, user stats ✓
- Response returned to FeedDialog → success/error toast ✓
- Egg list refreshed via onSuccess → refresh() ✓
- Existing direct contract call path in useEggFeed still works (backward compat) ✓
  </success_criteria>

<output>
After completion, create `.planning/phases/15-feed-feature/15-01-SUMMARY.md` with:
- What was changed (useEggFeed hook + FeedDialog API integration)
- Files modified
- Flow diagram (submit → API → validation → wallet-api → contract → DB update → response → toast)
- Testing notes
</output>
