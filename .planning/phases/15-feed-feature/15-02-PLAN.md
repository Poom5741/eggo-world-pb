---
phase: 15-feed-feature
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/eggs/feed-dialog.tsx
autonomous: true
requirements:
  - FEAT-02
  - FEAT-04
must_haves:
  truths:
    - "FeedDialog only fetches unconsumed food NFTs (is_consumed = false)"
    - "FeedDialog shows 'No food available' message when user has fewer than 10 unconsumed food NFTs"
    - "FeedDialog shows X/10 counter or status during selection"
    - "Consumed food NFTs are never selectable in the picker"
  artifacts:
    - path: "apps/web/components/eggs/feed-dialog.tsx"
      provides: "Feed dialog with consumed food filtering and empty state"
      exports: ["FeedDialog"]
---

<objective>
Add consumed food filtering and proper empty/no-food state to FeedDialog
Purpose: Users should only see unconsumed food NFTs; if insufficient food, show clear messaging per Q2 decision
Output: FeedDialog filters is_consumed=false, shows "No food available" when insufficient
</objective>

<execution_context>
@.planning/phases/15-feed-feature/15-CONTEXT.md
@.planning/phases/15-feed-feature/15-DISCUSSION-LOG.md
</execution_context>

<context>
@apps/web/components/eggs/feed-dialog.tsx
@apps/backend/pb_hooks/16-feed-egg.pb.js
</context>

<tasks>

<task type="auto">
  <name>task 1: Add is_consumed filter to food NFT fetch in FeedDialog</name>
  <files>apps/web/components/eggs/feed-dialog.tsx</files>
  <read_first>
    - apps/web/components/eggs/feed-dialog.tsx lines 47-76 (handleQuickFill)
  </read_first>
  <action>
    Update the food NFT fetch to filter out consumed items:

    1. In `handleQuickFill`, add `is_consumed = false` to the PocketBase filter:
       ```tsx
       const records = await pb.collection('food_nfts').getList(1, 10, {
         filter: `owner = "${pb.authStore.record?.id}" && is_consumed = false`,
         sort: '+created',
         headers: {
           'Authorization': token
         }
       })
       ```

    2. Change the insufficient food error message to a user-friendly display:
       - Instead of throwing an error, set state to show "No food available" view
       - Add new state variable `noFoodAvailable: boolean`
       - When `records.items.length < 10`, set `noFoodAvailable = true` and DON'T proceed
       - User sees a message like "You need at least 10 food NFTs. You have X." with Cancel button

  </action>
  <acceptance_criteria>
    - PocketBase query filters `is_consumed = false`
    - Consumed food NFTs never appear in selection
    - When fewer than 10 unconsumed food NFTs exist, show "No food available" message with count
    - User can cancel from the "No food available" state
    - No crash or error toast for insufficient food — just graceful messaging
  </acceptance_criteria>
  <verify>
    <automated>grep -q "is_consumed" apps/web/components/eggs/feed-dialog.tsx && echo "✓ Consumed filter added"</automated>
  </verify>
  <done>
    FeedDialog filters out consumed food NFTs and shows friendly "No food available" state when insufficient
  </done>
</task>

<task type="auto">
  <name>task 2: Add X/10 selection counter to dialog UI</name>
  <files>apps/web/components/eggs/feed-dialog.tsx</files>
  <read_first>
    - apps/web/components/eggs/feed-dialog.tsx lines 128-155 (step 1 UI)
  </read_first>
  <action>
    Enhance the dialog's step 1 UI to show food count status:

    1. In the quick-fill step (before confirmation), add a visible counter:
       - When food is being fetched: show "Fetching your food inventory..."
       - When food is found (10 items): show "10 food items ready ✓"
       - When food is insufficient: show the "No food available" state

    2. The "No food available" state should include:
       - A heading: "Not Enough Food"
       - Explanation: "You need at least 10 unconsumed food NFTs. You currently have X."
       - A Cancel button to close the dialog

  </action>
  <acceptance_criteria>
    - X/10 food counter visible during selection step
    - "No food available" state with count and Cancel button
    - Loading indicator during food fetch
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Not Enough Food\|noFoodAvailable\|No food" apps/web/components/eggs/feed-dialog.tsx && echo "✓ Empty state UI added"</automated>
  </verify>
  <done>
    FeedDialog shows food count status and properly handles insufficient food with clear messaging
  </done>
</task>

</tasks>

<success_criteria>

- FeedDialog queries `food_nfts` with `is_consumed = false` filter ✓
- Consumed food NFTs never shown as selectable ✓
- Insufficient food (< 10 items) shows "Not Enough Food" state with Cancel ✓
- X/10 food count label visible during selection ✓
- No console errors or unhandled promise rejections for insufficient food ✓
  </success_criteria>

<output>
After completion, create `.planning/phases/15-feed-feature/15-02-SUMMARY.md` with:
- What was changed (food filter, no-food state)
- Files modified
- Testing notes
</output>
