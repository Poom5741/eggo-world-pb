---
phase: 15-feed-feature
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/eggs/page.tsx
autonomous: true
requirements:
  - FEAT-01
must_haves:
  truths:
    - "Featured egg hero 'FEED ME' button opens FeedDialog (not a TODO stub)"
    - "Egg card 'Manage Egg' button opens FeedDialog (unchanged)"
    - "Both feed entry points use the same FeedDialog component"
  artifacts:
    - path: "apps/web/app/eggs/page.tsx"
      provides: "Eggs page with working feed entry points"
      exports: ["default function Eggs"]
  key_links:
    - from: "apps/web/app/eggs/page.tsx"
      to: "apps/web/components/eggs/feed-dialog.tsx"
      via: "FeaturedEggHero onFeed and EggCard onManage both open FeedDialog"
      pattern: "FeedDialog|handleFeedEgg|handleManageEgg"
---

<objective>
Fix featured egg hero "FEED ME" button to open FeedDialog instead of being a TODO stub
Purpose: Both feed entry points (hero "FEED ME", egg card "Manage Egg") should open the same FeedDialog
Output: handleFeedEgg opens FeedDialog, creating a single consistent feed entry flow
</objective>

<execution_context>
@.planning/phases/15-feed-feature/15-CONTEXT.md
@.planning/phases/15-feed-feature/15-DISCUSSION-LOG.md
</execution_context>

<context>
@apps/web/app/eggs/page.tsx
@apps/web/components/eggs/featured-egg-hero.tsx
</context>

<tasks>

<task type="auto">
  <name>task 1: Fix handleFeedEgg to open FeedDialog</name>
  <files>apps/web/app/eggs/page.tsx</files>
  <read_first>
    - apps/web/app/eggs/page.tsx lines 87-91 (handleFeedEgg stub)
    - apps/web/app/eggs/page.tsx lines 79-84 (handleManageEgg pattern to follow)
  </read_first>
  <action>
    Replace the TODO stub in `handleFeedEgg` with actual FeedDialog-open logic:

    ```tsx
    // Handle feed action from featured egg hero
    const handleFeedEgg = (eggId: number) => {
      const egg = eggs.find(e => e.egg_id === eggId)
      if (egg) {
        setFeedingEgg(egg)
        setFeedDialogOpen(true)
      }
    }
    ```

    This follows the exact same pattern as `handleManageEgg` (line 79-84).
    After this change, the "FEED ME" button on FeaturedEggHero will open FeedDialog just like "Manage Egg" on egg cards.

  </action>
  <acceptance_criteria>
    - handleFeedEgg finds the matching egg from state
    - Sets feedingEgg state to the found egg
    - Opens feedDialogOpen to true
    - Featured egg hero "FEED ME" button opens FeedDialog
    - TODO comment and console.log are removed
  </acceptance_criteria>
  <verify>
    <automated>grep -q "handleFeedEgg" apps/web/app/eggs/page.tsx && ! grep -q "TODO: Implement feed flow" apps/web/app/eggs/page.tsx && echo "✓ handleFeedEgg implemented"</automated>
  </verify>
  <done>
    Featured egg hero "FEED ME" button now opens FeedDialog via handleFeedEgg
  </done>
</task>

</tasks>

<success_criteria>

- Featured egg hero "FEED ME" button opens FeedDialog ✓
- Egg card "Manage Egg" button opens FeedDialog (unchanged) ✓
- Both entry points use the same feedingEgg state + feedDialogOpen pattern ✓
- TODO comment removed ✓
- console.log removed ✓
  </success_criteria>

<output>
After completion, create `.planning/phases/15-feed-feature/15-03-SUMMARY.md` with:
- What was changed (handleFeedEgg from stub to real handler)
- File modified
- Testing notes
</output>
